[project]
name = "google-ad-research-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28",
    "httpx-retries>=0.5",
    "tavily-python>=0.7.24",
    "python-dotenv>=1.0",
    "python-slugify>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
