---
phase: 16-ad-group-mapping-token-bag-enrichment
plan: 05
type: execute
wave: 3
depends_on:
  - "16-04"
files_modified:
  - .claude/skills/google-ad-research/references/phase11-account-structure-mapping.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - ADGM-11
must_haves:
  truths:
    - "phase11-account-structure-mapping.md Phase 16 section reflects the per-source max-Jaccard algorithm + final calibration evidence"
    - "REQUIREMENTS.md ADGM-11 row is marked Complete with no deferral footnote"
    - "STATE.md ADGM-11 structural-algorithm open question is closed (moved from open-questions to decisions)"
    - "SKILL.md remains at 497/500 lines (≤500 invariant)"
    - "Reference doc remains scannable (< 350 lines total) under progressive disclosure"
  artifacts:
    - path: ".claude/skills/google-ad-research/references/phase11-account-structure-mapping.md"
      provides: "Phase 16 section updated with per-source max-Jaccard algorithm rationale + final calibration evidence + live e2e closeout result"
      contains: "per-source max-Jaccard"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADGM-11 row no longer carries deferral language"
      contains: "ADGM-11"
    - path: ".planning/STATE.md"
      provides: "ADGM-11 open question closed; decisions block records the structural-fix outcome"
      contains: "ADGM-11"
  key_links:
    - from: "phase11-account-structure-mapping.md Phase 16 section"
      to: "ad_group_match.py build_mapping (max-Jaccard)"
      via: "Algorithm rationale paragraph + per-source attribution table"
      pattern: "max\\(name.*crit.*term\\)"
    - from: "SKILL.md (line ~495 Phase 11 pointer)"
      to: "phase11-account-structure-mapping.md"
      via: "Existing progressive-disclosure pointer (no SKILL.md edit)"
      pattern: "phase11-account-structure-mapping"
---

<objective>
**Goal:** Update the operator-facing reference doc + planning artifacts to record the ADGM-11 closure via per-source max-Jaccard. Pure documentation work — no code changes.

**Why this is its own plan (not folded into 16-04):** 16-04 ships the algorithm + tests + live e2e (autonomous: false). This plan handles the documentation delta cleanly and can run autonomously once 16-04 closes. Separating the docs concern keeps 16-04's checkpoint surface tight (live e2e only) and avoids context bloat from docs+impl+calibration+live-verify in a single plan.

**Output:**
- `references/phase11-account-structure-mapping.md` Phase 16 section UPDATED to reflect the per-source max-Jaccard structural fix (not just the token-bag enrichment from 16-01/16-02). Specifically:
  - Algorithm-shift paragraph: full-union jaccard → per-source max-jaccard, with live-evidence citation
  - Calibration table: shows the threshold sweep from 16-04 Task 2 (one row per pair tried)
  - Live e2e closeout: observed coverage on real Lake Worth OAuth account post-fix
  - Open question retired: ADGM-11 floor met, follow-up plan no longer needed
- `REQUIREMENTS.md` ADGM-11 entry updated — drop the "Pending → Complete (with deferral)" implicit footnote; mark cleanly Complete with reference to 16-04.
- `STATE.md` ADGM-11 open question moved from "Open Questions / Todos" to "Decisions" block with the final algorithm + calibration evidence.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-03-SUMMARY.md
@.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-04-SUMMARY.md
@.claude/skills/google-ad-research/references/phase11-account-structure-mapping.md
@.claude/skills/google-ad-research/SKILL.md
@./CLAUDE.md

<interfaces>
**Current `references/phase11-account-structure-mapping.md` Phase 16 section (lines ~242-262):**
The section currently documents:
1. Why Phase 16 exists (Lake Worth dogfood 0% pre-enrichment)
2. Bag composition (name ∪ kw_criteria ∪ top-10 search-terms)
3. Per-source reason-field attribution
4. Threshold calibration (Phase 11 0.7/0.4 → Phase 16 0.30/0.10)
5. Backward-compat contract
6. Open-question carry-forward (next-account calibration + structural-fix follow-up)

**What needs to change:**
- Section title stays `## Phase 16: Token-Bag Enrichment (ADGM-07..11)` — it now covers all 5 requirements cleanly closed.
- Add a new subsection after the existing "Threshold calibration" block: `### Phase 16 Plan 16-04: Per-Source Max-Jaccard Structural Fix (ADGM-11)`
- Inside: short narrative (why max-Jaccard solves the bag-vs-query asymmetry), live evidence citation from 16-02 (`name=0.33 kw-criterion=0.00 search-term=0.00`), the algorithm one-liner, calibration sweep table from 16-04 Task 2, live e2e closeout observation
- Retire the "open question" closing the section — replace with "ADGM-11 closed: floor met via structural fix"

**STATE.md changes needed (Decisions / Open Questions blocks):**

In `### Decisions`, append a new entry:
```
- [Phase 16-ad-group-mapping-token-bag-enrichment]: [16-04] ADGM-11 closed via per-source max-Jaccard structural fix — `raw_j = max(name_j, crit_j, term_j)` replacing full-union Jaccard directly addresses the bag-vs-query token-count asymmetry observed in 16-02's live e2e reason field (`name=0.33 kw-criterion=0.00 search-term=0.00 -> full-union=0.10`). Final `_THRESHOLDS = {H, M}` (filled in from 16-04 SUMMARY) lock C1 (Lake Worth >=50%) + C2 (Phase 11 80%) + C5 (garbage low). Live e2e observed coverage N% on real Lake Worth OAuth account (operator-verified).
```

In `### Open Questions / Todos`, REMOVE the `[v1.5] ADGM-11 structural-algorithm follow-up plan` bullet (the long one starting "Live Lake Worth OAuth e2e observed 16.42% coverage..."). Replace with a smaller follow-up:
```
- [v1.5] **Next-account calibration cycle (carried from Phase 16 closeout):** ADGM-11 closed via per-source max-Jaccard (Plan 16-04); thresholds locked at {H, M} based on Lake Worth (one account) + offline goldenfile. Once a 2nd OAuth-enabled real account is added, re-run calibration to confirm thresholds generalize. `references/phase11-account-structure-mapping.md` Phase 16 section carries the calibration audit trail.
```

**REQUIREMENTS.md changes:**
- Line 208 (`**ADGM-11**: Test coverage — golden mapping fixture from Lake Worth real-account data asserts ≥50% high+medium coverage`) keeps the `- [x]` checkbox but stays clean. No content change needed.
- Line 353 (traceability table `| ADGM-11 | Phase 16 | Complete |`) stays as-is.
- However, the VERIFICATION.md report (line 76) had a note: "REQUIREMENTS.md traceability table note: ADGM-11 is marked `[x] Complete` ... should be read as 'partial delivery + structured deferral'." That note can now be removed since ADGM-11 is actually complete. If there's anywhere in REQUIREMENTS.md that carries deferral language about ADGM-11, strip it. (Spot check: search for "deferr" or "16-02" / "16-04" in REQUIREMENTS.md.)
- **No structural REQUIREMENTS.md change is expected if the file is already clean.** The validation step here is: confirm REQUIREMENTS.md does NOT carry stale deferral text after the 16-04 implementation lands.

**SKILL.md invariant:**
- Currently 497/500 lines. Plan 16-05 must NOT add any new content to SKILL.md. The existing Phase 11 progressive-disclosure pointer at line 495 carries the updated reference doc content on demand — same pattern as 16-02.
- VERIFY post-edit: `wc -l SKILL.md` returns 497.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update reference doc Phase 16 section with structural-fix algorithm + final calibration</name>
  <files>
    .claude/skills/google-ad-research/references/phase11-account-structure-mapping.md
  </files>
  <action>
    1. Read `references/phase11-account-structure-mapping.md` lines 242-262 (the current `## Phase 16: Token-Bag Enrichment (ADGM-07..11)` section). Read its full contents through end-of-file to understand the structure.

    2. Read `.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-04-SUMMARY.md` to extract:
       - Final `_THRESHOLDS` values
       - Calibration sweep table (one row per {high, medium} pair tried with C1/C2/C5/backward-compat columns)
       - Offline Lake Worth coverage % post-fix
       - Live e2e observed coverage % (operator-verified)
       - Whether `test_backward_compat_keywords_absent` ceiling was raised + rationale

    3. **Append (do NOT replace earlier 16-01/16-02 content) a new subsection** after the existing "Threshold calibration" table within the Phase 16 section, titled:
       ```
       ### Plan 16-04: Per-Source Max-Jaccard Structural Fix (ADGM-11)
       ```

    4. Inside the new subsection, include exactly these elements (concise — aim for +30-50 lines, well under the 100-line scannability cap; reference doc currently at 262 lines, should land below 320):

       a. **One-paragraph rationale.** Cite the 16-02 live evidence (the reason-field snapshot `jaccard=0.10 (name=0.33 kw-criterion=0.00 search-term=0.00) intent_match=True`). Explain: under full-union Jaccard, name's strong 0.33 contribution diluted to 0.10 because the union bag was ~34 tokens; under per-source max, the same match scores 0.33 directly (clears the threshold).

       b. **Algorithm one-liner** (code-fenced block):
       ```python
       # 16-04 structural fix: per-source max replaces full-union jaccard.
       name_j = _jaccard(kw_tokens, ag["name_tokens"])
       crit_j = _jaccard(kw_tokens, ag["criterion_tokens"])
       term_j = _jaccard(kw_tokens, ag["search_term_tokens"])
       raw_j  = max(name_j, crit_j, term_j)
       # intent_multiplier still applies multiplicatively to raw_j
       ```

       c. **Calibration sweep table.** Same shape as the 16-01 sweep table in `16-01-SUMMARY.md` lines 90-95, but populated with 16-04's empirical sweep. Columns: `{high, medium}` | Lake Worth coverage | Phase 11 coverage | C5 garbage-low | Outcome.

       d. **Live e2e observation.** One-paragraph note: real Lake Worth OAuth account post-16-04 observed `mapping_coverage_pct = N%` (vs offline golden M%; vs 16.42% under full-union Jaccard at floor). Confirms structural fix generalizes from goldenfile to real OAuth data.

       e. **ADGM-11 status:** explicit one-liner — "ADGM-11 floor met (>=50%); 16-01's `xfail` decorator removed in Plan 16-03; per-source max-Jaccard shipped Plan 16-04."

    5. **Update or remove the existing "Open question carry-forward" section** at the end of the Phase 16 section (if present in current 262-line content). The previous text about ADGM-11 needing a structural follow-up plan is now historical — replace with: "Open question retired: ADGM-11 closed via Plan 16-04 per-source max-Jaccard. Future calibration concern: next-account cycle (2nd OAuth account) to confirm thresholds generalize beyond Lake Worth."

    6. **DO NOT remove the existing Phase 16 content** for 16-01/16-02 (bag composition, per-source reason attribution, original {0.30, 0.10} calibration table, backward-compat contract). The reference doc serves as a historical audit trail — 16-01's option-a deferral story is still valuable context for understanding why 16-04 exists. Keep it; just stop characterizing ADGM-11 as "deferred" in the trailing sections.

    7. **Confirm reference doc total line count.** After edit, `wc -l references/phase11-account-structure-mapping.md` should be in the 290-320 range (added 30-50 lines to a 262-line file). If significantly over 350, content has bloated — trim. The file is fully on-demand via progressive disclosure so the ceiling is "scannability" not "global rule budget" — but operators shouldn't have to read 400+ lines to understand the algorithm history.

    8. **Confirm SKILL.md unchanged.** `wc -l .claude/skills/google-ad-research/SKILL.md` returns 497. No edits to SKILL.md.
  </action>
  <verify>
    <automated>powershell -Command "(Get-Content '.claude/skills/google-ad-research/references/phase11-account-structure-mapping.md' -Raw) -match 'Per-Source Max-Jaccard'; (Get-Content '.claude/skills/google-ad-research/references/phase11-account-structure-mapping.md' -Raw) -match 'max\(name_j, crit_j, term_j\)'; (Get-Content '.claude/skills/google-ad-research/SKILL.md').Count"</automated>

    Acceptance:
    - First regex returns True (subsection title present)
    - Second regex returns True (algorithm one-liner present)
    - SKILL.md line count returns 497 exactly

    Manual subchecks:
    - Reference doc total line count between 290-340 (use `wc -l` or PowerShell `.Count`)
    - Calibration sweep table present with final {H, M} values from 16-04-SUMMARY
    - Live e2e coverage % present in the new subsection
    - ADGM-11 status line says "floor met (>=50%)" not "deferred"
  </verify>
  <done>
    - `references/phase11-account-structure-mapping.md` has a new `### Plan 16-04: Per-Source Max-Jaccard Structural Fix (ADGM-11)` subsection with rationale + algorithm + sweep table + live observation.
    - Old "open question" text in Phase 16 section retired (replaced with "ADGM-11 closed").
    - 16-01 and 16-02 historical content preserved (audit trail intact).
    - SKILL.md unchanged at 497 lines.
    - Reference doc total under 350 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md decisions + open questions to close ADGM-11</name>
  <files>
    .planning/STATE.md
  </files>
  <action>
    1. Read `.planning/STATE.md` current `### Decisions` and `### Open Questions / Todos` blocks.

    2. **In `### Decisions`,** append a new bullet at the end of the Phase 16 decisions chain (after the existing 16-01 and 16-02 entries):
       ```
       - [Phase 16-ad-group-mapping-token-bag-enrichment]: [16-04] ADGM-11 closed via per-source max-Jaccard structural fix — `raw_j = max(name_j, crit_j, term_j)` replacing full-union Jaccard directly addresses the bag-vs-query token-count asymmetry observed in 16-02's live e2e reason field (`name=0.33 kw-criterion=0.00 search-term=0.00 → full-union=0.10`). Final `_THRESHOLDS = {<H>, <M>}` (from 16-04-SUMMARY) lock C1 (Lake Worth ≥50%) + C2 (Phase 11 80%) + C5 (garbage low) simultaneously. Live e2e observed coverage `<N>%` on real Lake Worth OAuth account.
       ```
       Fill `<H>`, `<M>`, `<N>` from 16-04-SUMMARY.md.

    3. **In `### Open Questions / Todos`,** REMOVE the bullet starting with `[v1.5] **ADGM-11 structural-algorithm follow-up plan (carried from Phase 16 closeout):**` (the long one ending "...sweep table in 16-01-SUMMARY documents the threshold-pair exhaustion that rules out re-calibration as a fix."). Replace it with a smaller, retained follow-up:
       ```
       - [v1.5] **Next-account calibration cycle for ADGM-11 thresholds:** Plan 16-04 ships per-source max-Jaccard; thresholds locked at {<H>, <M>} on Lake Worth + offline goldenfile (one OAuth account). Once a 2nd OAuth-enabled real account is added, re-run the calibration to confirm thresholds generalize. `references/phase11-account-structure-mapping.md` Phase 16 section carries the audit trail.
       ```
       Fill `<H>`, `<M>` from 16-04-SUMMARY.

    4. **Update `## Current Position`** table — set "Stopped at" to `Plan 16-05 complete — Phase 16 fully closed (ADGM-07..11 shipped; per-source max-Jaccard structural fix lifted Lake Worth coverage to <N>%)`.

    5. **Update the `progress` block in the frontmatter:**
       - `stopped_at`: update to match the "Current Position" stopped-at line.
       - `last_updated`: today's date in ISO-Z format.
       - `total_plans`: increment by 3 (16-03, 16-04, 16-05).
       - `completed_plans`: increment by 3.

    6. **Update Performance Metrics table** — add rows for Phase 16-03, 16-04, 16-05 with rough durations (e.g. ~5min / ~30min / ~5min — fill best estimates).
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path '.planning/STATE.md' -Pattern 'ADGM-11 closed via per-source max-Jaccard').Count"</automated>

    Acceptance: returns 1 (the new decisions bullet is present).

    Manual subchecks:
    - Old `ADGM-11 structural-algorithm follow-up plan` bullet is REMOVED from Open Questions.
    - New `Next-account calibration cycle` bullet is present.
    - Frontmatter progress.completed_plans and total_plans incremented by 3 each.
    - "Current Position" stopped-at reflects Plan 16-05 closeout.
  </verify>
  <done>
    - STATE.md decisions block has 16-04 ADGM-11 closure entry.
    - STATE.md open questions no longer carries the structural-algorithm follow-up bullet (replaced with smaller next-account-calibration follow-up).
    - Frontmatter and current-position table updated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip stale ADGM-11 deferral language from REQUIREMENTS.md (if present) + final commit</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    1. Read `.planning/REQUIREMENTS.md`. Spot-check by searching for any of:
       - "deferr" (case-insensitive)
       - "16-02" / "16-04" reference in deferral context
       - "structural-algorithm" or "follow-up" text near ADGM-11

    2. Today (per 16-VERIFICATION.md line 76 + STATE.md line 173), REQUIREMENTS.md may NOT carry deferral language — it just shows `ADGM-11 | Phase 16 | Complete` in the traceability table. The verification report noted this is technically misleading but didn't flag it as actionable. After 16-04 ships, the row IS accurate. **No edit needed** unless spot-check finds explicit deferral text.

    3. **If no deferral text found**: SKIP the REQUIREMENTS.md edit. Note in SUMMARY: "REQUIREMENTS.md already accurate post-16-04; no changes needed."

    4. **If deferral text found**: remove it. Replace with a clean reference to Plan 16-04 (e.g. add a date suffix to the ADGM-11 line: "Complete (shipped via Plan 16-04 — per-source max-Jaccard structural fix)").

    5. **Final commit** — all three plans' doc updates in one commit:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(16-05): close ADGM-11 — reference-doc algorithm rationale + STATE.md decisions update" --files .claude/skills/google-ad-research/references/phase11-account-structure-mapping.md .planning/STATE.md .planning/REQUIREMENTS.md
       ```
       (If REQUIREMENTS.md was not modified per step 3, drop it from the --files list.)
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path '.planning/REQUIREMENTS.md' -Pattern 'deferr' -CaseSensitive:$false -SimpleMatch).Count"</automated>

    Acceptance: returns 0 OR returns matches that are unrelated to ADGM-11 (e.g. unrelated future-work mentions). Specifically: no "ADGM-11" + "deferral" co-occurrence.

    Manual:
    - REQUIREMENTS.md ADGM-11 line shows `- [x] **ADGM-11**: ...` with no apologetic footnote.
    - Final commit landed via gsd-tools.
  </verify>
  <done>
    - REQUIREMENTS.md verified clean OR edited to drop stale deferral language.
    - All Plan 16-05 docs committed in one commit.
  </done>
</task>

</tasks>

<verification>
After Tasks 1+2+3 complete:

1. **Reference doc updated:**
   - `references/phase11-account-structure-mapping.md` has `### Plan 16-04: Per-Source Max-Jaccard Structural Fix (ADGM-11)` subsection.
   - Algorithm one-liner present.
   - Calibration sweep table with final {H, M} populated.
   - Live e2e closeout observation captured.

2. **STATE.md updated:**
   - Decisions block contains 16-04 ADGM-11 closure entry.
   - Open questions no longer carries structural-fix follow-up; carries shorter next-account-calibration follow-up instead.
   - Frontmatter progress + stopped_at + last_updated updated.

3. **REQUIREMENTS.md verified clean** (no edit if already accurate; edit if deferral text remained).

4. **SKILL.md unchanged at 497/500.**

5. **Commit landed.**
</verification>

<success_criteria>
- Reference doc Phase 16 section now reflects per-source max-Jaccard algorithm + final calibration evidence + live e2e closeout.
- STATE.md decisions/open-questions accurately reflect ADGM-11 closure.
- REQUIREMENTS.md verified clean of stale deferral text.
- SKILL.md untouched at 497/500.
- Reference doc total under 350 lines.
- Commit landed.
- Phase 16 fully closed: ADGM-07..11 all Complete with no carry-forward deferrals.
</success_criteria>

<output>
After completion, create `.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-05-SUMMARY.md` per `summary.md` template. Include:
- Which lines of `references/phase11-account-structure-mapping.md` were added (`### Plan 16-04: Per-Source Max-Jaccard Structural Fix (ADGM-11)` subsection)
- New reference doc line count
- STATE.md edits (new decision, removed open question, replaced with shorter follow-up)
- Whether REQUIREMENTS.md needed editing (likely no)
- SKILL.md line count confirmation (497/500)
- requirements-completed: [] (ADGM-11 was closed by 16-04 implementation; 16-05 is docs-only)
- patterns-established: "Reference-doc-as-calibration-record: structural-fix rationale + sweep table + live evidence live in the operator-facing reference, NOT in code comments"
- Closes Phase 16 entirely.
</output>
