---
phase: 16-ad-group-mapping-token-bag-enrichment
plan: 02
type: execute
wave: 3
depends_on: ["16-01"]
files_modified:
  - .claude/skills/google-ad-research/references/phase11-account-structure-mapping.md
autonomous: false
requirements:
  - ADGM-10
must_haves:
  truths:
    - "`references/phase11-account-structure-mapping.md` carries a `Phase 16 Token-Bag Enrichment` section explaining bag composition + per-source attribution"
    - "`references/phase11-account-structure-mapping.md` records the empirical `_THRESHOLDS` calibration: before/after values + Lake Worth coverage before/after"
    - "ADGM-10 rationale is auditable — operator reading the reference doc can re-derive why high tightened from 0.7 to {X} and medium from 0.4 to {Y}"
    - "SKILL.md stays at ≤500 lines (no SKILL.md edit needed — reference doc is loaded on demand by existing Phase 11 pointer)"
    - "Live e2e on real Lake Worth OAuth account confirms post-Phase-16 mapping coverage >= 50% (operator manual verification)"
  artifacts:
    - path: ".claude/skills/google-ad-research/references/phase11-account-structure-mapping.md"
      provides: "Calibration rationale + bag composition diagram + before/after numbers"
      contains: "Phase 16 Token-Bag Enrichment"
  key_links:
    - from: ".claude/skills/google-ad-research/SKILL.md"
      to: ".claude/skills/google-ad-research/references/phase11-account-structure-mapping.md"
      via: "Existing Phase 11 progressive-disclosure pointer (no SKILL.md edit needed)"
      pattern: "phase11-account-structure-mapping"
---

<objective>
Document Phase 16 calibration in the existing Phase 11 reference doc + close out
the phase with a live human-verify e2e on the real Lake Worth OAuth account.

Purpose: Make ADGM-10 threshold recalibration auditable so a future operator
re-tuning thresholds (per the open question in STATE.md — "collect mapping
coverage on Lake Worth + 1 other real account before locking values") knows
why the current values exist and what evidence supports them.
Output: One reference doc updated + one human-verified live e2e result.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-CONTEXT.md
@.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-00-SUMMARY.md
@.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-01-SUMMARY.md

@.claude/skills/google-ad-research/references/phase11-account-structure-mapping.md
@.claude/skills/google-ad-research/scripts/ad_group_match.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend phase11-account-structure-mapping.md with Phase 16 calibration rationale</name>
  <files>.claude/skills/google-ad-research/references/phase11-account-structure-mapping.md</files>
  <action>
Read the existing `references/phase11-account-structure-mapping.md` first to understand its current structure (Pitfall section + open questions). Locate a sensible insertion point — likely AFTER the existing Pitfall section and BEFORE any "Open questions" trailer.

Append a new section titled `## Phase 16: Token-Bag Enrichment (ADGM-07..11)` containing:

1. **Why Phase 16 exists** — one paragraph: Phase 11 used search-terms-only bags; Lake Worth dogfood (2026-05-15) showed 0% coverage despite Phase 15 narrowing because Lake Worth AGs have sparse 30-day search history. Phase 16 unions AG name + active kw_criteria + top-10 search terms to lift coverage.

2. **Bag composition diagram** (markdown ASCII):
   ```
   bag(ad_group) =
       _tokens(ag.name)
     ∪ _tokens(kw.text) for kw in active kw_criteria where ad_group_name == ag.name
     ∪ _tokens(st.search_term) for st in top-10 search_terms by clicks desc
                                       (tiebreak impressions desc, drop impressions==0)
   ```

3. **Per-source attribution table** explaining the new `match.reason` format:
   | Component | Meaning |
   |---|---|
   | `jaccard=0.42` | Best score (union bag) × intent multiplier |
   | `name=0.10` | Partial Jaccard against AG name tokens only |
   | `kw-criterion=0.32` | Partial Jaccard against active kw_criteria tokens |
   | `search-term=0.20` | Partial Jaccard against top-10 search-term tokens |
   | `intent_match=True` | Inferred ad-group intent == ranked-keyword intent |

4. **Threshold calibration table** — pull final values + observed coverage from 16-01-SUMMARY.md:
   | Threshold | Phase 11 | Phase 16 | Rationale |
   |---|---|---|---|
   | high | 0.7 | {final_high_from_16-01-SUMMARY} | {coverage_observed}% Lake Worth golden vs 0% pre-enrichment |
   | medium | 0.4 | {final_medium_from_16-01-SUMMARY} | Larger bags compress raw Jaccard scores — recalibrated empirically |

5. **Backward-compat contract** — one paragraph: when `raw/google-ads-keywords.json` is absent (no Phase 14 OAuth, pre-v1.4 account), bag degrades to `_tokens(ag.name) ∪ top-10 search-term tokens`. Coverage targets Phase 11 ballpark (<20% on Lake Worth-shape accounts). Test fixture: `tests/test_ad_group_match.py::test_backward_compat_keywords_absent`.

6. **Open question (carry forward to STATE.md)** — final threshold lock requires calibration against 1 more real account beyond Lake Worth. Current values are evidence-based for Lake Worth only; operator should re-run calibration after the next OAuth-enabled real account ships.

Pull the literal final values + observed coverage from `.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-01-SUMMARY.md` — do NOT invent numbers. If SUMMARY values disagree with what's in test_lake_worth_coverage_floor's golden fixture, prefer the SUMMARY (it's the empirical truth from the calibration run).

Do NOT exceed 100 added lines (reference doc stays scannable).
  </action>
  <verify>
    <automated>grep -c "Phase 16: Token-Bag Enrichment" .claude/skills/google-ad-research/references/phase11-account-structure-mapping.md</automated>
  </verify>
  <done>New section exists in reference doc; calibration table populated with the final values from 16-01-SUMMARY (not placeholders); backward-compat contract documented; SKILL.md untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live e2e on real Lake Worth OAuth account — confirm post-Phase-16 mapping coverage</name>
  <files>.runs/&lt;latest-timestamp&gt;-&lt;slug&gt;/ad-group-mapping.json (read-only verification)</files>
  <action>Human-verify checkpoint — see &lt;how-to-verify&gt; for exact steps. Operator runs the skill end-to-end against the real Lake Worth OAuth account and inspects the resulting ad-group-mapping.json + report.md mapping section.</action>
  <verify>Operator manual verification — see &lt;how-to-verify&gt; steps 1-7.</verify>
  <done>Operator types resume signal with observed coverage % (e.g. "approved 63.8%"); coverage observed &gt;= 50.0 on real Lake Worth account.</done>
  <what-built>
Phase 16 token-bag enrichment is in production code. The Lake Worth golden fixture
test asserts >=50% coverage offline. This checkpoint confirms it lands the same
way against the REAL OAuth-wired Lake Worth account, end-to-end through the skill.
  </what-built>
  <how-to-verify>
1. From the repo root, launch a fresh run via the skill: in Claude Code, invoke the google-ad-research skill with a Lake Worth brief that includes:
   ```
   Industry: urgent care + auto accident injury
   Product: PIP / no-fault injury care services
   Location: Florida
   Geo focus: Palm Beach County, Lake Worth
   Language: English
   Audience: post-accident patients
   Campaign focus: Search | Lake Worth Accident Exams | Manual CPC
   ```
2. Let the skill run end-to-end through Phase 14 (OAuth pull) + Phase 15 (campaign narrowing) + Phase 16 (token-bag enrichment).
3. Open the new run folder: `.runs/<latest-timestamp>-<slug>/ad-group-mapping.json`.
4. Inspect `mapping_coverage_pct`:
   - Expected: >= 50.0
   - Acceptable: 50.0 - 75.0 (within calibration window)
   - Fail signal: < 50.0 OR > 90.0 (over-fitting to fixture)
5. Inspect a sample `matches[]` entry — confirm `reason` field carries `name=X.XX kw-criterion=Y.YY search-term=Z.ZZ` format (not the old Phase 11 `jaccard=X.XX intent_match=B` format).
6. Open `report.md`, scroll to Ad Group Mapping section. Confirm:
   - The 3 Lake Worth AGs ("Accident Exams – Lake Worth", "Lake Worth Auto Accident Injury Doctor", "Lake Worth Personal Injury Doctor" — or whatever the 3 actually are in the OAuth account) appear with keyword counts >= 1 each.
   - Operator could plausibly paste the resulting positives.csv and the keywords would land in the right existing AGs.
7. (Sanity) Run the full test suite once more: `uv run --with pytest --with python-dotenv --with python-slugify pytest .claude/skills/google-ad-research/scripts/tests/ -x` — confirm all green, no regressions outside Phase 16.

Record observed coverage % in the resume-signal message.
  </how-to-verify>
  <resume-signal>
Type "approved" + the observed coverage % (e.g. "approved 63.8%") to close out Phase 16. If coverage < 50%, type "diagnose" + the actual % + the run-dir path; this triggers gap-closure plan creation.
  </resume-signal>
</task>

</tasks>

<verification>
- `references/phase11-account-structure-mapping.md` gains the Phase 16 section with literal final values from 16-01-SUMMARY
- Reference doc remains scannable (<=100 lines added)
- SKILL.md untouched — existing Phase 11 progressive-disclosure pointer carries the new content
- Live Lake Worth OAuth run produces mapping_coverage_pct >= 50.0 — operator manually verified
</verification>

<success_criteria>
- ADGM-10 documentation requirement satisfied: calibration rationale + before/after numbers in reference doc
- Operator can audit any future threshold change by reading one file
- Live e2e confirms the offline golden fixture predicts real-account behavior (no fixture overfit)
- All 17 tests still GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/16-ad-group-mapping-token-bag-enrichment/16-02-SUMMARY.md` recording:
- Live Lake Worth observed coverage %
- Any operator-reported friction in the mapping section render
- Open-question handoff to STATE.md (next-account calibration target)
</output>
