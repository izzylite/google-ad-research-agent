---
phase: 15-campaign-focus
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
  - .claude/skills/google-ad-research/references/phase8-account-data.md
autonomous: true
requirements:
  - CAMP-03
  - CAMP-04
must_haves:
  truths:
    - "SKILL.md Phase 8 Step 33 documents the optional `[--campaign-filter '<focus>']` flag and instructs Claude to auto-read `campaign_focus` from brief.md before invoking perf_fetch.py"
    - "Step 33 narration includes a `Narrowing to campaign: <focus>` line surfaced to the operator before fetch when campaign_focus is set"
    - "SKILL.md Step 3 (optional fields prompt) is extended with a `**campaign_focus**` trigger row mirroring the existing `**geo_focus**` row"
    - "SKILL.md Step 4 brief template includes a `- **Campaign focus:** {campaign_focus}` line in the `## Optional` block (omitted when no value)"
    - "references/phase8-account-data.md Step 33 section documents CAMP-04 graceful-degrade contract: omit field → account-wide (v1.4 behavior preserved bit-for-bit)"
    - "references/phase8-account-data.md downstream-contract block notes that all 4 raw artifacts inherit the narrowing automatically (Positives Sync / Negatives Sync / Ad Group Mapping derive from raw — no per-script wiring needed)"
    - "SKILL.md stays ≤ 500 lines after edits (per CLAUDE.md constraint)"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Step 3 (optional fields table row) + Step 4 (brief template line) + Step 33 (perf_fetch invocation with --campaign-filter auto-pass)"
      contains: "campaign_focus"
    - path: ".claude/skills/google-ad-research/references/phase8-account-data.md"
      provides: "Step 33 documentation block extended with --campaign-filter contract + CAMP-04 graceful-degrade note + downstream-inheritance pointer"
      contains: "--campaign-filter"
  key_links:
    - from: "SKILL.md Step 33 perf_fetch invocation"
      to: "perf_fetch.py --campaign-filter CLI flag (Plan 15-01)"
      via: "Bash snippet auto-passes `--campaign-filter \"<value>\"` when brief carries `**Campaign focus:**` line"
      pattern: "--campaign-filter"
    - from: "SKILL.md Step 3 optional-fields table"
      to: "SKILL.md Step 4 brief template Optional block"
      via: "campaign_focus row in Step 3 → `- **Campaign focus:** {campaign_focus}` in Step 4 template"
      pattern: "Campaign focus"
---

<objective>
Wire the new `--campaign-filter` plumbing (Plans 15-01 + 15-02) into the operator-facing workflow so a brief with `Campaign focus:` auto-narrows the Phase 8 fetch without per-run typing (CAMP-03). Document the CAMP-04 graceful-degrade contract in `references/phase8-account-data.md` so future operators understand omitting the field preserves v1.4 account-wide behavior bit-for-bit.

Mirrors Phase 11's wiring pattern: brief.md `Geo focus:` → SKILL.md Step 8 auto-passes `--geo-focus` to `serp_fetch.py`. Same shape, different field, different script.

Purpose: Close the operator UX loop — pasting `Campaign focus:` into the brief is the entire opt-in. No CLI memorization, no per-script reasoning, no manual flag passing.

Output: Modified `SKILL.md` (3 step edits) + modified `references/phase8-account-data.md` (Step 33 extension + downstream-contract block). Final closeout for v1.5 Phase 15.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-campaign-focus/15-CONTEXT.md
@.planning/phases/15-campaign-focus/15-01-PLAN.md
@.planning/phases/15-campaign-focus/15-02-PLAN.md
@.claude/skills/google-ad-research/SKILL.md
@.claude/skills/google-ad-research/references/phase8-account-data.md
@.claude/skills/google-ad-research/scripts/perf_fetch.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md edits — Step 3 trigger row + Step 4 template line + Step 33 perf_fetch invocation</name>
  <files>.claude/skills/google-ad-research/SKILL.md</files>
  <action>
    1. **Step 3 (Optional fields table, around line 73-80)**: After the existing `**geo_focus**` row (line 80), add a new row:

    ```
    | **campaign_focus** | The brief targets ONE specific existing Google Ads campaign in the operator's account (rather than account-wide research). Single name OR pipe-separated list (no spaces around pipes for list form). Example: `Search | Lake Worth Accident Exams | Manual CPC` (single) or `Campaign A|Campaign B` (list). Drives `perf_fetch.py --campaign-filter` — narrows all 4 GAQL queries to the named campaign(s); Positives Sync / Negatives Sync / Ad Group Mapping inherit the narrowed raw data automatically. Omit when researching account-wide (CAMP-04 graceful degrade). |
    ```

    2. **Step 4 brief template (around line 105-117)**: After the `- **Geo focus:** {geo_focus}` line (~line 111), add:

    ```
    - **Campaign focus:** {campaign_focus}
    ```

    Inside the `## Optional` block. Comment stays at "Include only the optional fields that were filled".

    3. **Step 33 (Google Ads account data fetch)**: Find the section currently at SKILL.md describing `perf_fetch.py` invocation (search for "perf_fetch.py" — if SKILL.md does NOT yet contain a Phase 8 Step 33 block, this edit lives in `references/phase8-account-data.md` instead; check both files first).

    Most likely Step 33 is in `references/phase8-account-data.md` (per the file layout shown in context). If so, this Task 1 limits SKILL.md edits to Step 3 + Step 4 only. The references/phase8-account-data.md Step 33 update is owned by Task 2 below.

    However, if SKILL.md has a brief `### Step 33` pointer block that invokes perf_fetch directly (search for `--customer-id` in SKILL.md to confirm), then extend that invocation to:
    ```bash
    uv run "${CLAUDE_SKILL_DIR}/scripts/perf_fetch.py" \
      --run-dir "{run_dir}" \
      [--customer-id <id>] \
      [--login-customer-id <id>] \
      [--days 30] \
      [--campaign-filter "{campaign_focus}"]
    ```
    and add narration: "When brief has `**Campaign focus:**`, append `--campaign-filter \"<value>\"` (one quoted arg). Skill announces `Narrowing to campaign: <focus>` to the operator before fetch."

    4. **Line-count discipline**: After edits, verify `wc -l .claude/skills/google-ad-research/SKILL.md` ≤ 500. If exceeded, the Step 33 narration above must move entirely into `references/phase8-account-data.md` (Task 2 — already planned). Step 3 row + Step 4 line are non-negotiable for SKILL.md.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; lines = pathlib.Path('.claude/skills/google-ad-research/SKILL.md').read_text(encoding='utf-8').splitlines(); assert len(lines) <= 500, f'SKILL.md is {len(lines)} lines, exceeds 500-line cap'; print(f'SKILL.md OK at {len(lines)} lines'); text = '\n'.join(lines); assert 'campaign_focus' in text, 'campaign_focus token missing'; assert 'Campaign focus' in text, 'Campaign focus template line missing'; print('SKILL.md edits verified')"</automated>
  </verify>
  <done>SKILL.md contains the new Step 3 trigger row + Step 4 template line; SKILL.md ≤ 500 lines; full test suite still passes (no test changes expected).</done>
</task>

<task type="auto">
  <name>Task 2: references/phase8-account-data.md — Step 33 extension + downstream-contract block (CAMP-03 + CAMP-04)</name>
  <files>.claude/skills/google-ad-research/references/phase8-account-data.md</files>
  <action>
    1. **Step 33 section update**: Locate the `## Step 33: Google Ads account data fetch` block. Update the bash invocation to include the new optional flag:

    ```bash
    uv run "${CLAUDE_SKILL_DIR}/scripts/perf_fetch.py" \
      --run-dir "{run_dir}" \
      [--customer-id 4580213643] \
      [--login-customer-id 4580213643] \
      [--days 30] \
      [--campaign-filter "{campaign_focus}"]
    ```

    Below the invocation, add a new paragraph:

    > **CAMP-03 (v1.5):** When `brief.md` carries a `**Campaign focus:**` line, read it via `_parse_brief_fields` (or grep) and auto-append `--campaign-filter "<value>"` (one quoted arg — pipe-separated list form `'A|B|C'` for multi-campaign). The flag adds `AND campaign.name = '<focus>'` (single) or `IN (...)` (list) to all 4 GAQL queries (`keyword_view`, `search_term_view`, `campaign`+`ad_group`, `campaign_criterion`+`ad_group_criterion`). Announce to the operator: `"Narrowing to campaign: <focus>"` before fetch.
    >
    > **CAMP-04 graceful degrade:** Omit `--campaign-filter` and `perf_fetch.py` runs account-wide — current v1.4 behavior preserved bit-for-bit. No code path changes; the GAQL queries simply omit the `campaign.name` clause when the kwarg is `None`/empty. Re-running an existing pre-v1.5 brief produces identical raw artifacts.
    >
    > **Single-quote escape:** Campaign names containing apostrophes (e.g. `O'Brien Auto`) are SQL-escaped by `perf_fetch.py` automatically (`'O''Brien Auto'`). Operator passes the raw name; the script handles GAQL string escaping.
    >
    > **Pipe-list parsing rule:** ` | ` (space-pipe-space) inside a single quoted value is part of a Google-Ads campaign-naming convention (e.g. `Search | Lake Worth Accident Exams | Manual CPC` is ONE campaign name). Use the bare-pipe form `'A|B|C'` (no spaces) for the multi-campaign list form.

    2. **Downstream-contract block**: At the end of the file (or in the existing "Anti-patterns" / "Failure modes" sections), add a new subsection:

    ```markdown
    ## Phase 15 downstream contract (CAMP-04 inheritance)

    The `--campaign-filter` flag narrows the 4 raw artifacts at the source.
    Every downstream consumer reads `raw/google-ads-*.json` as-is — no
    per-script wiring needed:

    - **`perf_synth.py` → `negatives-sync.json`** (Phase 8 GADS-04): cross-refs
      use the already-narrowed `raw/google-ads-negatives.json`; sync section
      reflects only target-campaign negatives.
    - **`perf_synth.py` → `positives-sync.json`** (Phase 14 POS-02): same —
      `already_active` / `paused_in_account` buckets reflect only the
      target campaign's keywords.
    - **`ad_group_match.py` → `ad-group-mapping.json`** (Phase 11 ADGM-01..04):
      maps against ad groups inside the target campaign only (e.g. 3 AGs
      under "Lake Worth Accident Exams" instead of 35 account-wide).
    - **`render_report.py`**: `## Campaign Focus` callout renders in the
      report header beside `## Geographic Focus` (CAMP-05); typo warning
      fires when brief's focus name not in `raw/google-ads-perf.json`
      campaigns list.

    No script needs to know about `campaign_focus` — narrowing is a property
    of the raw data, not the synth layer. This is the same architectural
    pattern as Phase 11 `geo_focus` (filters at SERP harvest + city-token
    merge_signals layer; downstream rank/cluster/render unaware).
    ```

    3. **Anti-pattern addition**: In the existing `## Anti-patterns` section, add:

    > - **Don't manually filter raw artifacts by campaign after fetching account-wide.** That defeats CAMP-04's clean separation. If you forgot the `--campaign-filter` flag, re-run `perf_fetch.py` with the flag set — the fetch is free quota; no penalty.
    > - **Don't quote the pipe-list value with surrounding pipes.** `'A|B|C'` (correct) vs `'|A|B|C|'` (wrong — produces empty-name list entries that get filtered out, silently turning a 3-campaign list into a 0-clause / account-wide fetch).
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; text = pathlib.Path('.claude/skills/google-ad-research/references/phase8-account-data.md').read_text(encoding='utf-8'); assert '--campaign-filter' in text, '--campaign-filter docs missing'; assert 'CAMP-03' in text, 'CAMP-03 doc-tag missing'; assert 'CAMP-04' in text, 'CAMP-04 doc-tag missing'; assert 'graceful degrade' in text.lower(), 'graceful degrade narrative missing'; print('phase8-account-data.md edits verified')"</automated>
  </verify>
  <done>references/phase8-account-data.md Step 33 block documents `--campaign-filter` auto-pass + downstream-contract block + anti-patterns. CAMP-03 and CAMP-04 doc tags both present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-eyes verification — end-to-end Lake Worth narrowing smoke</name>
  <what-built>
    Plans 15-00 + 15-01 + 15-02 + 15-03 together deliver:
    - Brief field parser for `Campaign focus:`
    - `perf_fetch.py --campaign-filter` CLI flag
    - GAQL clause threaded into all 4 fetch functions
    - `## Campaign Focus` callout in report.md + report.json key
    - Typo warning when focus name not in perf.json campaigns
    - SKILL.md Step 3/4 trigger + template wiring
    - references/phase8-account-data.md Step 33 documentation
  </what-built>
  <how-to-verify>
    Operator runs an end-to-end Lake Worth smoke (uses real Google Ads OAuth from `.env`):

    1. Create a fresh brief with `**Campaign focus:** Search | Lake Worth Accident Exams | Manual CPC` in the `## Optional` block.
    2. Walk through the skill end-to-end (Steps 1-35).
    3. At Step 33, verify Claude announces: `Narrowing to campaign: Search | Lake Worth Accident Exams | Manual CPC` before invoking `perf_fetch.py`.
    4. After Step 33 completes, open `raw/google-ads-perf.json` — verify the `campaigns` list contains ONLY entries with name `"Search | Lake Worth Accident Exams | Manual CPC"` (or empty if the campaign was paused during the 30d window).
    5. Open `raw/google-ads-keywords.json` — verify every item's `campaign_name` field equals the focus name.
    6. Open `raw/google-ads-search-terms.json` — verify every item's `campaign_name` field equals the focus name.
    7. Open `raw/google-ads-negatives.json` — verify every item's `campaign` field equals the focus name OR `null` (campaign-level negatives may not have campaign association in older accounts).
    8. After Step 35, open `report.md` — verify `## Campaign Focus` callout renders with `**Campaign:** Search | Lake Worth Accident Exams | Manual CPC` in the header section.
    9. Open `report.json` — verify top-level `"campaign_focus": ["Search | Lake Worth Accident Exams | Manual CPC"]`.
    10. **Typo smoke**: edit brief.md to misspell the campaign (e.g. `Search | Lake Worth Accident Examzz | Manual CPC`); re-run from Step 35 (`render_report.py --run-dir <existing>`); verify `report.md` shows `⚠ Campaign name not found in account: 'Search | Lake Worth Accident Examzz | Manual CPC' — check for typo`.
    11. **Backward-compat smoke**: create a brief WITHOUT `Campaign focus:` line; walk through Steps 1-35; verify `raw/google-ads-perf.json` contains ALL account campaigns (account-wide v1.4 behavior preserved) and `report.md` has NO `## Campaign Focus` section.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "step 5 keywords.json still has Hybrid campaign data")</resume-signal>
</task>

</tasks>

<verification>
SKILL.md ≤ 500 lines:
```bash
uv run python -c "import pathlib; print(len(pathlib.Path('.claude/skills/google-ad-research/SKILL.md').read_text(encoding='utf-8').splitlines()))"
```

Doc tags present in references/phase8-account-data.md:
```bash
grep -E "CAMP-0[34]|--campaign-filter" .claude/skills/google-ad-research/references/phase8-account-data.md
```

Full test suite still passes:
```bash
uv run --with pytest --with python-dotenv --with python-slugify --with tabulate \
  pytest .claude/skills/google-ad-research/scripts/tests/ -x
```

Operator-eyes Lake Worth smoke (Task 3) approved.
</verification>

<success_criteria>
- SKILL.md Step 3 carries campaign_focus trigger row
- SKILL.md Step 4 brief template carries `- **Campaign focus:** {campaign_focus}` line
- SKILL.md stays ≤ 500 lines (CLAUDE.md hard cap)
- references/phase8-account-data.md Step 33 documents `--campaign-filter` auto-pass + CAMP-04 graceful degrade
- references/phase8-account-data.md downstream-contract block explains automatic inheritance
- Operator-eyes smoke approved on real Lake Worth account
</success_criteria>

<output>
Create `.planning/phases/15-campaign-focus/15-03-SUMMARY.md` documenting:
- SKILL.md final line count after edits
- Step 33 documentation location (SKILL.md inline vs references/ pointer — whichever the existing structure dictated)
- Operator-eyes smoke outcome (approved / issues + resolution)
- CAMP-03 and CAMP-04 doc tags landed
- Phase 15 closeout: all 6 CAMP requirements (CAMP-01..06) Complete
- Phase 16 unblocked (calibrates against now-narrowed dataset)
</output>
