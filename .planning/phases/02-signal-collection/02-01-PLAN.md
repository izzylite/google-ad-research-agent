---
phase: 02-signal-collection
plan: "01"
type: tdd
wave: 1
depends_on: ["02-00"]
files_modified:
  - .claude/skills/google-ad-research/scripts/lib/http.py
  - .claude/skills/google-ad-research/scripts/lib/canon.py
  - .claude/skills/google-ad-research/scripts/tests/test_lib_http.py
  - .claude/skills/google-ad-research/scripts/tests/test_lib_canon.py
autonomous: true
requirements:
  - SIGL-06
must_haves:
  truths:
    - "build_client() returns an httpx.Client that retries 429/500/503 up to 3 times before raising"
    - "build_client() does NOT retry 401 (auth failures are fatal, not transient)"
    - "canonicalise('grocery delivery') and canonicalise('groceries delivery') produce the same lemma_hash"
    - "canonicalise('how to find grocery delivery') preserves word order (question prefix)"
    - "canonicalise('') raises ValueError"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/lib/http.py"
      provides: "build_client() using httpx-retries RetryTransport"
      exports: ["build_client"]
    - path: ".claude/skills/google-ad-research/scripts/lib/canon.py"
      provides: "canonicalise(keyword) -> (canonical_form, lemma_hash)"
      exports: ["canonicalise"]
  key_links:
    - from: "scripts/serp_fetch.py"
      to: "lib/http.py"
      via: "from lib.http import build_client"
      pattern: "build_client"
    - from: "scripts/merge_signals.py"
      to: "lib/canon.py"
      via: "from lib.canon import canonicalise"
      pattern: "canonicalise"
---

<objective>
Implement lib/http.py (httpx RetryTransport factory) and lib/canon.py (inflect singularization + token-sort hash) turning their RED test stubs GREEN.

Purpose: Both modules are pure libraries consumed by Wave 2 scripts. Delivering them in Wave 1 unblocks serp_fetch.py and merge_signals.py from importing missing modules.
Output: lib/http.py, lib/canon.py, fully passing test_lib_http.py and test_lib_canon.py.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-collection/02-RESEARCH.md
@.planning/phases/02-signal-collection/02-00-SUMMARY.md

# Key contracts these modules must satisfy:
# lib/http.py — used by serp_fetch.py: build_client(*, timeout=30.0) -> httpx.Client context manager
#   RetryTransport(retry=Retry(total=3, backoff_factor=1.0, status_forcelist=[429,500,502,503,504]))
#   follow_redirects=False (Serper returns direct JSON)
#
# lib/canon.py — used by merge_signals.py: canonicalise(keyword: str) -> tuple[str, str]
#   Returns (canonical_form, lemma_hash) where lemma_hash = sha256[:16] of sorted lemmas
#   Question prefixes (how/what/why/is/are/can/who/where/when/do/does): preserve word order
#   inflect.engine().singular_noun(token) for singularisation (returns False if already singular)
</context>

<feature>
  <name>lib/http.py — httpx client with RetryTransport</name>
  <files>
    .claude/skills/google-ad-research/scripts/lib/http.py
    .claude/skills/google-ad-research/scripts/tests/test_lib_http.py
  </files>
  <behavior>
    - test_retry_on_429: Use respx to return 429 twice then 200; assert build_client() eventually succeeds and call count is 3.
    - test_no_retry_on_401: Use respx to return 401; assert HTTPStatusError raised on first attempt (not retried — 401 is not in status_forcelist).
    - test_success_path: Return 200 immediately; assert response.status_code == 200.
  </behavior>
  <implementation>
    Implement exactly the pattern from RESEARCH.md § "lib/http.py — httpx client factory":
    ```python
    from httpx_retries import Retry, RetryTransport
    def build_client(*, timeout: float = 30.0) -> httpx.Client:
        retry = Retry(total=3, backoff_factor=1.0, status_forcelist=[429, 500, 502, 503, 504])
        transport = RetryTransport(retry=retry)
        return httpx.Client(transport=transport, timeout=timeout, follow_redirects=False)
    ```
    No additional logic. No logging. The module is a pure factory.
    Tests use `respx.mock` context manager and `respx.route(...).mock(side_effect=[...])` for multi-response sequences.
  </implementation>
</feature>

<feature>
  <name>lib/canon.py — canonicalise(keyword) -> (canonical_form, lemma_hash)</name>
  <files>
    .claude/skills/google-ad-research/scripts/lib/canon.py
    .claude/skills/google-ad-research/scripts/tests/test_lib_canon.py
  </files>
  <behavior>
    - test_grocery_variants_merge: canonicalise("grocery delivery") and canonicalise("groceries delivery") and canonicalise("grocery deliveries") must all return the same lemma_hash (second element of tuple).
    - test_question_keywords_no_sort: canonicalise("how to find grocery delivery") — lemma_hash must differ from canonicalise("grocery delivery how find") (word order preserved for questions, so sorted hash differs).
    - test_empty_raises: canonicalise("") and canonicalise("   ") both raise ValueError.
    - test_token_sort_produces_stable_hash: calling canonicalise("delivery grocery") and canonicalise("grocery delivery") produces the same lemma_hash (non-question tokens are sorted before hashing).
  </behavior>
  <implementation>
    Implement exactly the pattern from RESEARCH.md § "lib/canon.py — canonicalisation":
    algorithm: lowercase → strip punctuation (keep hyphens) → tokenize → singularise via inflect → sort (non-question) or preserve order (question) → sha256[:16].
    canonical_form = lowercased + punctuation-stripped input (NOT the sorted form — the display form is the original normalised surface).
    _QUESTION_PREFIXES = {"how","what","why","is","are","can","who","where","when","do","does"}
    Use `re.compile(r"[^\w\s-]")` for punctuation strip.
    Module-level `_INF = inflect.engine()` (instantiate once, not per call).
  </implementation>
</feature>

<verification>
  <automated>uv run --with pytest --with respx --with httpx --with httpx-retries --with inflect pytest .claude/skills/google-ad-research/scripts/tests/test_lib_http.py .claude/skills/google-ad-research/scripts/tests/test_lib_canon.py -x -v 2>&1 | tail -20</automated>
</verification>

<success_criteria>
- All tests in test_lib_http.py and test_lib_canon.py pass (GREEN)
- RED → GREEN cycle followed: stubs fail first, then implementation makes them pass
- lib/http.py exports build_client; lib/canon.py exports canonicalise
- No third-party imports beyond httpx, httpx-retries, inflect (stdlib only otherwise)
- Full suite still passes: `uv run --with pytest ... pytest scripts/tests/ -x` GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-collection/02-01-SUMMARY.md`
</output>
