---
phase: 02-signal-collection
plan: "03"
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/skills/google-ad-research/scripts/tavily_extract.py
  - .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py
autonomous: true
requirements:
  - SIGL-02
must_haves:
  truths:
    - "tavily_extract.py calls TavilyClient.extract() with extract_depth='basic' (explicit, never omitted)"
    - "Competitor list is capped at max-competitors (default 5) and URLs capped at max-urls-per-competitor (default 5)"
    - "failed_results from Tavily response are written to raw/tavily-<domain>.json alongside results"
    - "One raw/tavily-<domain>.json file is written per competitor domain (slugified filename)"
    - "UsageLimitExceededError returns exit code 2; InvalidAPIKeyError/MissingAPIKeyError returns exit code 3"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tavily_extract.py"
      provides: "Tavily extract per competitor domain → raw/tavily-<domain>.json"
      exports: ["main", "main_with_args", "parse_competitor_arg"]
  key_links:
    - from: "tavily_extract.py"
      to: "TavilyClient.extract()"
      via: "client.extract(urls=urls, extract_depth='basic', format='markdown', include_usage=True)"
      pattern: "extract_depth.*basic"
    - from: "tavily_extract.py"
      to: "raw/tavily-<domain>.json"
      via: "out_path = raw_dir / f'tavily-{slugify(domain)}.json'"
      pattern: "tavily-.*\\.json"
---

<objective>
Implement tavily_extract.py — Tavily SDK extract caller that writes per-domain JSON files including both results and failed_results, with hard caps enforced at the CLI level.

Purpose: Second paid signal source. Parallel to 02-02 (no shared files). Pitfall 8 mitigation (crawl vs extract, caps, explicit basic depth) is enforced in code and verified by tests.
Output: tavily_extract.py with full CLI contract; test_tavily_extract.py fully GREEN.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-collection/02-RESEARCH.md
@.planning/phases/02-signal-collection/02-01-SUMMARY.md

# tavily_extract.py CLI contract (from RESEARCH.md § tavily_extract.py):
#   uv run tavily_extract.py --run-dir <abs> \
#     --competitor "tesco.com:https://tesco.com/a,https://tesco.com/b" \
#     --competitor "ocado.com:https://ocado.com/c"
#   Optional: --max-competitors 5  --max-urls-per-competitor 5
#   stdout: {"competitor_count", "urls_attempted", "urls_succeeded", "urls_failed", "credits_used"}
#   exit 0 ok / exit 2 UsageLimitExceededError / exit 3 auth / IO
#
# CRITICAL: Expose main_with_args(argv) for tests.
# CRITICAL: Never call client.crawl() — only client.extract().
# CRITICAL: annotated output shape must include "source": "tavily-extract" and "competitor_domain" on each result.
# Monkeypatching strategy: tests monkeypatch tavily.TavilyClient.extract to return fixture dict directly.
</context>

<feature>
  <name>tavily_extract.py — Tavily SDK extract with caps, failure persistence, source annotation</name>
  <files>
    .claude/skills/google-ad-research/scripts/tavily_extract.py
    .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py
  </files>
  <behavior>
    - test_caps_enforced: Pass 7 competitors in args; assert only 5 are processed (len(written files) == 5). Pass 1 competitor with 8 URLs; assert only 5 URLs appear in the extract call (monkeypatch captures call args).
    - test_uses_basic_depth: Monkeypatch TavilyClient.extract to record kwargs; assert call included extract_depth='basic'.
    - test_failed_results_persisted: Use tavily_fixture (1 result + 1 failed_result); assert raw/tavily-tesco-com.json exists and loaded JSON has both "results" (len 1) and "failed_results" (len 1).
    - test_exit_code_3_on_auth_error: Monkeypatch TavilyClient.extract to raise InvalidAPIKeyError; assert exit code 3.
  </behavior>
  <implementation>
    Implement using the full code example from RESEARCH.md § "tavily_extract.py — Tavily SDK with caps and failure persistence". Key points:
    - PEP 723 inline metadata: tavily-python>=0.7.24, python-dotenv>=1.0, python-slugify>=8.0
    - parse_competitor_arg("tesco.com:https://a,https://b") → ("tesco.com", ["https://a","https://b"])
    - Trim competitors list BEFORE iterating: competitors[:args.max_competitors]
    - Per competitor: urls = urls[:args.max_urls_per_competitor]
    - client.extract(urls=urls, extract_depth="basic", format="markdown", include_usage=True)
    - Annotated output: for each result in response.get("results", []), add "source": "tavily-extract" and "competitor_domain": domain
    - Persist response.get("failed_results", []) verbatim in the output JSON
    - Filename: raw_dir / f"tavily-{slugify(domain)}.json" using python-slugify
    - Exception handling: InvalidAPIKeyError/MissingAPIKeyError → exit 3; UsageLimitExceededError → exit 2; BadRequestError → log and continue (skip that competitor)
    - credits_used: -(-total_succeeded // 5) (ceiling divide — 1 credit per 5 successful URLs at basic depth)
    - Test monkeypatch pattern: monkeypatch.setattr("tavily.TavilyClient.extract", lambda self, **kwargs: fixture_dict)
  </implementation>
</feature>

<verification>
  <automated>uv run --with pytest --with python-dotenv --with python-slugify --with tavily-python pytest .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py -x -v 2>&1 | tail -20</automated>
</verification>

<success_criteria>
- All 4 tests in test_tavily_extract.py pass (GREEN)
- SIGL-02: extract_depth='basic' explicit in every client.extract() call (verified by test)
- Caps enforced: ≤5 competitors, ≤5 URLs per competitor
- failed_results written to disk alongside results (not silently dropped)
- Full test suite green: `uv run --with pytest ... pytest scripts/tests/ -x`
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-collection/02-03-SUMMARY.md`
</output>
