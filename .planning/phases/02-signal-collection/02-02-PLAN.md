---
phase: 02-signal-collection
plan: "02"
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/skills/google-ad-research/scripts/serp_fetch.py
  - .claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py
autonomous: true
requirements:
  - SIGL-01
  - SIGL-04
must_haves:
  truths:
    - "serp_fetch.py writes raw/serper.json containing organic, peopleAlsoAsk, relatedSearches, and ads blocks"
    - "The outgoing Serper POST request body contains gl and hl values derived from --gl and --hl CLI args"
    - "searchParameters is preserved verbatim in raw/serper.json for downstream locale lint"
    - "Empty ads block (no ads on query) produces no error and ads_count: 0 in stdout JSON"
    - "429 after exhausting retries returns exit code 2; 401 returns exit code 3"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/serp_fetch.py"
      provides: "Serper REST fetch → raw/serper.json with all four signal blocks"
      exports: ["main", "main_with_args", "fetch_seed", "normalise_response"]
  key_links:
    - from: "serp_fetch.py"
      to: "lib/http.py"
      via: "from lib.http import build_client"
      pattern: "build_client"
    - from: "serp_fetch.py"
      to: "raw/serper.json"
      via: "out_path.write_text(json.dumps(aggregated, ...))"
      pattern: "serper\\.json"
    - from: "test_serp_fetch.py"
      to: "serper_search_uk.json fixture"
      via: "respx.post(...).mock(return_value=Response(200, json=fixture))"
      pattern: "serper_fixture"
---

<objective>
Implement serp_fetch.py — Serper.dev REST caller that writes all four signal blocks (organic, PAA, related, ads) to raw/serper.json, with explicit gl/hl locale params and defensive .get() response parsing.

Purpose: First paid signal source. Turning test_serp_fetch.py GREEN confirms locale plumbing is correct (Pitfall 4) and the ads-absent case is safe (Pitfall: empty ads block).
Output: serp_fetch.py with full CLI contract; test_serp_fetch.py fully GREEN.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-collection/02-RESEARCH.md
@.planning/phases/02-signal-collection/02-01-SUMMARY.md

# serp_fetch.py CLI contract (from RESEARCH.md § serp_fetch.py — Serper.dev request shape):
#   uv run serp_fetch.py --run-dir <abs> --seeds "kw1" "kw2" --gl uk --hl en-GB [--num 20]
#   stdout: single JSON line {"raw_path", "seed_count", "organic_count", "paa_count", "related_count", "ads_count", "credits_used"}
#   stderr: human-readable progress per seed (via lib/log.configure_logger())
#   exit 0 ok / exit 2 retryable (429/5xx after retries) / exit 3 fatal (auth 401/403, IO, config)
#
# CRITICAL: Expose main_with_args(argv: list[str]) -> int in addition to main() so tests can call
# without subprocess. main() calls main_with_args(sys.argv[1:]).
#
# lib.http.build_client() is a context manager (with build_client() as client:)
# Locale: --gl and --hl are REQUIRED args (no defaults) — fail exit 3 if missing
</context>

<feature>
  <name>serp_fetch.py — Serper REST with locale plumbing and all signal blocks</name>
  <files>
    .claude/skills/google-ad-research/scripts/serp_fetch.py
    .claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py
  </files>
  <behavior>
    - test_writes_all_blocks: Given serper_search_uk fixture (has organic/paa/related/ads), after main_with_args([--run-dir, --seeds, --gl uk, --hl en-GB]), raw/serper.json exists; loaded JSON has by_seed[0] with non-empty organic, peopleAlsoAsk, relatedSearches, ads arrays.
    - test_locale_params_passed: respx captures the outgoing POST body; assert sent_body["gl"] == "uk" and sent_body["hl"] == "en-GB".
    - test_locale_persisted: raw/serper.json by_seed[0]["locale"] == {"gl": "uk", "hl": "en-GB"} and by_seed[0]["searchParameters"]["gl"] == "uk".
    - test_empty_ads_no_error: Given serper_empty_ads fixture, main_with_args exits 0; stdout JSON has "ads_count": 0; no exception.
    - test_retries_on_429: respx returns 429 three times then 200; assert exit code 2 (retries exhausted → HTTPStatusError → exit 2, since 4xx after retries is retryable upstream).
    - test_exit_code_3_on_401: respx returns 401 immediately (not retried by lib/http); assert exit code 3.
  </behavior>
  <implementation>
    Implement using the full code example from RESEARCH.md § "serp_fetch.py — Serper.dev request shape". Key points:
    - PEP 723 inline metadata header: httpx>=0.28, httpx-retries>=0.5, python-dotenv>=1.0
    - sys.path.insert(0, str(Path(__file__).resolve().parent)) before lib imports
    - fetch_seed(): POST to SERPER_URL with headers={"X-API-KEY": api_key}, json={"q", "gl", "hl", "num"}; response.raise_for_status()
    - normalise_response(): use .get(key, []) everywhere — never bracket access on potentially-absent blocks
    - Include "searchParameters": raw.get("searchParameters", {}) in normalised output
    - main() wraps main_with_args(sys.argv[1:]) for testability
    - Missing --run-dir / non-existent run-dir → exit 3
    - httpx.HTTPStatusError with status 401/403 → exit 3; all other HTTPStatusError → exit 2
    - SERPER_API_KEY never logged (load via load_env, read via os.environ)
  </implementation>
</feature>

<verification>
  <automated>uv run --with pytest --with python-dotenv --with respx --with httpx --with httpx-retries pytest .claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py -x -v 2>&1 | tail -20</automated>
</verification>

<success_criteria>
- All 6 tests in test_serp_fetch.py pass (GREEN)
- SIGL-01: raw/serper.json written with organic + PAA + related + ads blocks
- SIGL-04: gl and hl appear in outgoing request body AND in persisted raw/serper.json
- Exit codes correct: 0 success, 2 retryable, 3 fatal auth/IO
- Full test suite green: `uv run --with pytest ... pytest scripts/tests/ -x`
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-collection/02-02-SUMMARY.md`
</output>
