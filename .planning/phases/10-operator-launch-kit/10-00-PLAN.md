---
phase: 10-operator-launch-kit
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/tests/test_export_csv.py
  - .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_phase10.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_empty.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase10.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/forecast_phase10.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_with_match.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_two_verticals.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_empty.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/golden_positives.csv
  - .claude/skills/google-ad-research/scripts/tests/fixtures/golden_negatives.csv
  - .claude/skills/google-ad-research/scripts/tests/fixtures/golden_ad_groups.csv
  - .claude/skills/google-ad-research/scripts/export_csv.py
autonomous: true
requirements: [EXPT-01, EXPT-02, EXPT-03, EXPT-04, EXPT-05, STEP-01, STEP-02, STEP-03, STEP-04, CMPL-05]

must_haves:
  truths:
    - "RED test stubs for export_csv exist and are imported under MODULE_MISSING guard so pytest collection is clean"
    - "RED test stubs for render_report Next-Steps + Export Files extensions exist and skip cleanly"
    - "All eight Phase 10 fixture files exist with realistic shapes matching downstream contracts"
    - "Three golden CSV byte fixtures encode the exact bytes expected from Wave 1 export_csv.py (no BOM, CRLF, headers matching spec)"
    - "export_csv.py MODULE_MISSING stub exists so test_export_csv.py collects cleanly before Wave 1 implementation"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_export_csv.py"
      provides: "RED unit tests for EXPT-01..04 (positives, negatives, ad_groups CSVs + BOM/CRLF/round-trip byte contracts)"
      contains: "MODULE_MISSING"
    - path: ".claude/skills/google-ad-research/scripts/tests/test_render_report.py"
      provides: "Extended with RED tests for STEP-01..04 + CMPL-05 + EXPT-05 (Next Steps section, substitution, HTML checkboxes, JSON next_steps[]+exports[])"
      contains: "render_next_steps_section"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/negatives_phase10.json"
      provides: "Mixed-tier negatives (Strong + Considered + Investigate) for level-mapping tests"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/negatives_empty.json"
      provides: "Empty list for header-only CSV test (Pitfall 4)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase10.json"
      provides: "Clusters fixture with theme_intent names for ad-group iteration tests"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/forecast_phase10.json"
      provides: "Forecast with campaign_totals.daily_spend_mid_usd populated for STEP-02 substitution"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/compliance_with_match.json"
      provides: "Single matched vertical for CMPL-05 reorder test"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/compliance_two_verticals.json"
      provides: "Two matched verticals for combined-step assertion (CMPL-05 combine rule)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/compliance_empty.json"
      provides: "Empty matched_verticals[] for standard-order baseline"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/golden_positives.csv"
      provides: "Byte-exact expected positives.csv output (CRLF, no BOM)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/golden_negatives.csv"
      provides: "Byte-exact expected negatives.csv output"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/golden_ad_groups.csv"
      provides: "Byte-exact expected ad_groups.csv output"
    - path: ".claude/skills/google-ad-research/scripts/export_csv.py"
      provides: "MODULE_MISSING stub raising NotImplementedError so tests collect cleanly"
      contains: "NotImplementedError"
  key_links:
    - from: "tests/test_export_csv.py"
      to: "export_csv module"
      via: "try/except ImportError + pytestmark skipif (MODULE_MISSING guard pattern from Phase 2/3/9)"
      pattern: "MODULE_MISSING"
    - from: "tests/test_render_report.py"
      to: "render_report.render_next_steps_section"
      via: "try/except AttributeError + pytest.skip"
      pattern: "render_next_steps_section"
    - from: "tests/test_export_csv.py"
      to: "fixtures/golden_*.csv"
      via: "Path(__file__).parent / 'fixtures' / 'golden_positives.csv'"
      pattern: "golden_positives.csv"
---

<objective>
Establish RED test scaffolding for Phase 10 (Operator Launch Kit). This is the Wave 0 prerequisite that makes the Nyquist contract auditable: every Wave 1+ task ships with a failing-test-first verify command pointing at a file that already exists.

Purpose: Wave 1 implementation (`export_csv.py` + render_report Next-Steps extension) can run in parallel only if their tests already exist. RED stubs use the MODULE_MISSING / AttributeError skip-guard pattern established in Phases 2-9, so pytest collection stays clean while the production code is being written.

Output: Two test files (one new — `test_export_csv.py`; one extended — `test_render_report.py`), 8 JSON/MD fixtures, 3 byte-exact golden CSVs, and a MODULE_MISSING stub for `export_csv.py`. Suite-level invariant: pytest collects + skips cleanly with zero `ImportError` or `NameError`.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operator-launch-kit/10-RESEARCH.md
@.planning/phases/10-operator-launch-kit/10-VALIDATION.md
@.claude/skills/google-ad-research/scripts/tests/conftest.py
@.claude/skills/google-ad-research/scripts/tests/test_bid_suggest.py
@.claude/skills/google-ad-research/scripts/tests/test_compliance_check.py
@.claude/skills/google-ad-research/scripts/tests/test_render_report.py
@.claude/skills/google-ad-research/scripts/tests/fixtures/ranked_with_cpc.json
@.claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase9.json
@.claude/skills/google-ad-research/scripts/tests/fixtures/negatives_valid.json
@.claude/skills/google-ad-research/scripts/render_report.py

<interfaces>
<!-- Contracts the Wave 1 implementations must satisfy. Wave 0 tests assert these. -->

From scripts/render_report.py (existing — extended in Wave 1):
```python
# Existing helpers Wave 0 tests will import (already exist):
def _parse_brief_fields(brief_text: str) -> dict[str, str]: ...   # L676
def _derive_brief_slug(run_dir: Path) -> str: ...                  # L693
def _micros_to_usd(micros: int | None) -> str: ...                 # L142 — returns "—" for None
def _html_escape(s: str) -> str: ...                                # L846

# Existing signature Wave 1 will extend:
def build_report_json(
    ranked: list[dict], clusters_data: dict, competitor_intel: dict,
    negatives: list[dict], brief_text: str, run_dir: Path,
    *, niche_pulse: dict | None = None, account_perf: dict | None = None,
    negatives_sync: dict | None = None, forecast: dict | None = None,
    compliance: dict | None = None,
) -> dict: ...

# NEW helper Wave 1 must add (Wave 0 tests reference via getattr/skip):
def render_next_steps_section(
    brief_fields: dict[str, str],
    forecast: dict | None,
    compliance: dict | None,
    clusters_data: dict,
) -> tuple[str, list[dict]]:
    """Return (markdown_string, list_of_step_dicts).
    Each step dict: {"n": int, "text": str, "id": str (8-char SHA-1)}.
    """

# NEW helper Wave 2 must add:
def render_export_section(run_dir: Path) -> str:
    """Return ## Export Files markdown listing the three CSVs (or empty string)."""
```

From scripts/export_csv.py (NEW — Wave 1 implements; Wave 0 stubs):
```python
POSITIVES_HEADERS = ["Campaign", "Ad Group", "Keyword", "Match Type", "Max CPC", "Final URL"]
NEGATIVES_HEADERS = ["Campaign", "Ad Group", "Keyword", "Match Type", "Level"]
AD_GROUPS_HEADERS = ["Campaign", "Ad Group", "Status", "Default Max CPC"]

TIER_TO_LEVEL = {"Strong": "campaign", "Considered": "ad_group", "Investigate": "ad_group"}
MATCH_TYPE_TITLECASE = {"phrase": "Phrase", "exact": "Exact", "broad": "Broad"}

def write_positives(path: Path, rows: list[dict]) -> None: ...
def write_negatives(path: Path, rows: list[dict]) -> None: ...
def write_ad_groups(path: Path, rows: list[dict]) -> None: ...
def main(argv: list[str] | None = None) -> int: ...  # CLI: --run-dir; exits 0/2/3
```

From existing test_compliance_check.py (MODULE_MISSING pattern to mirror):
```python
try:
    import compliance_check  # noqa: F401
    MODULE_MISSING = False
except (ImportError, AttributeError):
    MODULE_MISSING = True

pytestmark = pytest.mark.skipif(MODULE_MISSING, reason="compliance_check.py not implemented yet")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 10 fixtures + export_csv.py MODULE_MISSING stub</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_phase10.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_empty.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase10.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/forecast_phase10.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_with_match.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_two_verticals.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/compliance_empty.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/golden_positives.csv,
    .claude/skills/google-ad-research/scripts/tests/fixtures/golden_negatives.csv,
    .claude/skills/google-ad-research/scripts/tests/fixtures/golden_ad_groups.csv,
    .claude/skills/google-ad-research/scripts/export_csv.py
  </files>
  <behavior>
    - negatives_phase10.json: list of 5+ dicts with mix of tiers — at least one Strong, one Considered, one Investigate. Each has keys `keyword`, `tier`, `category`, `justification`. Optional `match_type` (string lowercase) and `cluster` (string).
    - negatives_empty.json: literal `[]`.
    - clusters_phase10.json: dict with `metadata`, `clusters` (3 clusters with `name` in theme_intent snake_case, `intent`, `keywords` of {keyword, score}), and `orphans: []`. At least one cluster must have keywords whose `suggested_max_cpc_micros` would aggregate to a non-zero median; at least one cluster's keywords are all-null CPC.
    - forecast_phase10.json: dict with `clusters[]` (per-cluster bands), `campaign_totals.daily_spend_mid_usd: 12.50`, and `methodology` block. Realistic shape matching Phase 9 forecast_budget.py output.
    - compliance_with_match.json: dict with `matched_verticals: [{name: "medical", evidence_tokens: [...], evidence_sources: {...}, matched_keyword_count: 3, verification_url: "https://support.google.com/adspolicy/answer/176031", policy_note: "..."}]`.
    - compliance_two_verticals.json: dict with `matched_verticals: [{name: "medical", ...}, {name: "legal", ..., verification_url: "https://..."}]` (2 entries; used to assert CMPL-05 combined-step rule).
    - compliance_empty.json: `{"matched_verticals": [], "scanned_at": "..."}`.
    - golden_positives.csv: byte-exact expected file with CRLF endings, no BOM, header `Campaign,Ad Group,Keyword,Match Type,Max CPC,Final URL\r\n` then 3+ data rows derived from clusters_phase10 + a minimal ranked-enriched companion. Title-cased Match Type. Max CPC formatted `"{micros/1_000_000:.2f}"`. Empty Final URL.
    - golden_negatives.csv: header `Campaign,Ad Group,Keyword,Match Type,Level\r\n` + rows where Strong → Level=campaign + Ad Group empty; Considered/Investigate → Level=ad_group + Ad Group=cluster name.
    - golden_ad_groups.csv: header `Campaign,Ad Group,Status,Default Max CPC\r\n` + one row per cluster, Status=Enabled, Default Max CPC from cluster-median (`0.00` for all-null cluster).
    - Campaign column for all three: title-cased brief slug (use `"Phase 10 Test Brief"` as the slug for fixtures so it's deterministic).
    - export_csv.py: PEP 723 metadata + module docstring + raise `NotImplementedError("export_csv.py not yet implemented (Phase 10 Wave 0 stub)")` at the bottom of an empty `main()` so `import export_csv` succeeds (allowing MODULE_MISSING guard to NOT trip) but `main()` calls fail. Define the three HEADER constants and the TIER_TO_LEVEL + MATCH_TYPE_TITLECASE module-level dicts so Wave 1 has the contracts in place. Do NOT implement write_* helpers — Wave 1 owns those. Use `# /// script\n# requires-python = ">=3.11"\n# dependencies = []\n# ///` PEP 723 block.
  </behavior>
  <action>
    Create all 8 JSON fixtures with realistic shapes that match the downstream production contracts (Phase 6 negatives schema, Phase 4 clusters.json schema, Phase 9 forecast.json + compliance-flags.json schemas).

    Mirror existing fixture conventions: 2-space JSON indent, ISO-8601 timestamps in `Z` form, lowercase intent strings, snake_case cluster names ending in `_<intent>`.

    For golden CSV bytes: open files in binary write mode (`wb`) or use Python's `csv` module locally to generate them once and then check the bytes. CRITICAL: bytes must end every line with `b"\r\n"` and the file must NOT start with `b"\xef\xbb\xbf"`. The header row is included.

    For export_csv.py stub:
    - Write PEP 723 inline metadata block (mirror bid_suggest.py header pattern).
    - Module docstring stating "Write Google Ads Editor v2.x importable CSVs (EXPT-01..04). Stub — Wave 1 implements."
    - Define module-level constants: POSITIVES_HEADERS, NEGATIVES_HEADERS, AD_GROUPS_HEADERS, TIER_TO_LEVEL, MATCH_TYPE_TITLECASE.
    - Define `def main(argv=None) -> int: raise NotImplementedError(...)`.
    - Add `if __name__ == "__main__": sys.exit(main())` guard.

    Why a stub rather than complete absence: `try: import export_csv` in test_export_csv.py succeeds; the MODULE_MISSING guard then trips on a sentinel attribute check (e.g., `hasattr(export_csv, "write_positives")`). This is the Phase 9 pattern (`bid_suggest.py` shipped stub `main_with_args(NotImplementedError)` in plan 09-01 Task 1).

    Avoid: writing fixture rows that exceed 5 entries (keeps test diff readable); embedding non-ASCII characters in golden CSVs (delays Editor smoke debugging); hardcoding absolute Windows paths in fixtures.
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify pytest .claude/skills/google-ad-research/scripts/tests/ --collect-only -q 2>&amp;1 | grep -E "error|ImportError|SyntaxError" ; if [ $? -eq 0 ]; then echo "FAIL: collection errors"; exit 1; else echo "PASS: collection clean"; fi</automated>
  </verify>
  <done>
    All 11 files exist on disk. `python -c "import json; [json.load(open(p)) for p in ['<each fixture>.json']]"` succeeds. The 3 golden CSVs each pass: `bytes[:3] != b'\xef\xbb\xbf'` AND `b'\r\n' in bytes`. `python -c "import sys; sys.path.insert(0, '.claude/skills/google-ad-research/scripts'); import export_csv; assert export_csv.POSITIVES_HEADERS == ['Campaign', 'Ad Group', 'Keyword', 'Match Type', 'Max CPC', 'Final URL']"` succeeds. pytest collection produces zero errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test scaffolding — test_export_csv.py + test_render_report.py extension</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_export_csv.py,
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  </files>
  <behavior>
    test_export_csv.py — new file, ~14 test functions, all SKIP under MODULE_MISSING guard until Wave 1 lands:

    EXPT-01 (positives.csv):
    - test_positives_headers_exact: After Wave 1, write_positives produces fieldnames == POSITIVES_HEADERS.
    - test_positives_rows_per_keyword: One row per ranked-enriched keyword that maps to a cluster.
    - test_positives_max_cpc_format: f"${X.XX:.2f}" formatting from micros (1_500_000 → "1.50"); None → "0.00".
    - test_positives_match_type_titlecase: lowercase "phrase" → "Phrase"; unknown → "Phrase" fallback.
    - test_positives_final_url_empty: column is empty string per row.
    - test_positives_campaign_titlecase_brief_slug: Campaign column = title-cased brief slug.

    EXPT-02 (negatives.csv):
    - test_negatives_strong_to_campaign_level: Strong tier → Level="campaign", Ad Group="".
    - test_negatives_considered_to_ad_group: Considered → Level="ad_group", Ad Group=cluster name.
    - test_negatives_investigate_to_ad_group: Investigate → Level="ad_group".
    - test_negatives_empty_input: negatives_empty.json fixture → header-only CSV (1 line + CRLF).
    - test_negatives_match_type_default_phrase: default Match Type = "Phrase" when missing on the dict.

    EXPT-03 (ad_groups.csv):
    - test_ad_groups_one_row_per_cluster: row count == len(clusters).
    - test_ad_groups_status_enabled: every row has Status="Enabled".
    - test_ad_groups_default_max_cpc_from_cluster_median: Default Max CPC = USD-formatted cluster-median suggested_max_cpc_micros.
    - test_ad_groups_zero_cpc_for_all_null_cluster: cluster with all-null suggested_max_cpc_micros → "0.00".
    - test_ad_groups_ad_group_name_verbatim_from_cluster: Ad Group column == clusters_phase10.json[i]["name"] exactly (Pitfall 6 mitigation — no massaging).

    EXPT-04 (byte contract + round-trip + exit codes):
    - test_all_csvs_no_bom: bytes[:3] != b"\xef\xbb\xbf" for all 3 files.
    - test_all_csvs_crlf_only: b"\r\n" in bytes AND no bare b"\n" outside CRLF pairs (assert decoded text has no "\n" after replacing "\r\n" with "").
    - test_all_csvs_round_trip_dictreader: csv.DictReader fieldnames == expected; rows parse back without loss.
    - test_all_csvs_match_golden_bytes: parametrized — each CSV bytes == fixtures/golden_<name>.csv bytes (byte-exact).
    - test_exit_code_0_on_happy_path: main() with valid run_dir → returns 0.
    - test_exit_code_3_on_missing_inputs: main() with run_dir missing ranked-enriched.json → returns 3.

    test_render_report.py — append (do NOT recreate the file) ~10 new test functions, each skipping cleanly if `getattr(render_report, "render_next_steps_section", None) is None`:

    STEP-01 + STEP-02 + STEP-04:
    - test_next_steps_section_default_order: render_next_steps_section({location, language, audience}, forecast=..., compliance=None|compliance_empty, clusters_data=...) returns (markdown, list_of_8_steps).
    - test_next_steps_section_substitution: step text contains brief location/language, daily_spend_mid_usd formatted as "$X.XX", and all cluster names from clusters_phase10.json.
    - test_next_steps_section_step_ids_8_char_sha1: each step['id'] is 8 chars lowercase hex.
    - test_next_steps_section_n_from_position: step n == 1-indexed position in the final list.
    - test_report_json_next_steps_array: build_report_json(..., next_steps=...) returns dict with top-level "next_steps" list whose items have keys {n, text, id}. (REQUIRES build_report_json signature extension — Wave 1 plan 10-02 owns this.)

    STEP-03 (HTML):
    - test_next_steps_html_section_exists: render_html_report output contains 'id="next-steps"' AND '<input type="checkbox"'.
    - test_next_steps_html_localstorage_namespacing: HTML script block contains the literal substring "gar_" + run_slug pattern reference (assert regex `r"gar_\$\{[A-Za-z_.]+\}_step_"` or equivalent template literal).
    - test_next_steps_html_escapes_step_text: step text containing `<script>` is escaped via existing _html_escape.

    CMPL-05:
    - test_next_steps_compliance_reorder_single_vertical: with compliance_with_match.json, step 1 contains "medical" and the vertical's verification_url; step 2 begins with "Create campaign".
    - test_next_steps_compliance_combined_two_verticals: with compliance_two_verticals.json, step 1 mentions BOTH "Medical" and "Legal" (joined " + "), both URLs (joined "; ") — count of "Complete ... verification" steps == 1, not 2.
    - test_next_steps_no_compliance_standard_order: with compliance_empty.json (or None), step 1 == "Create campaign…" — no verification step prepended.

    EXPT-05 (Wave 2 ownership but test scaffolded here):
    - test_export_files_section_in_markdown (mark `xfail(reason="EXPT-05 lands in plan 10-03")` OR skip with `pytest.skip` if `getattr(render_report, "render_export_section", None) is None`).
    - test_report_json_exports_array (same skip pattern).

    Use existing tmp_run_dir fixture from conftest.py. Use Path(__file__).parent / "fixtures" / "<file>" for fixture loading. Mirror MODULE_MISSING guard from test_compliance_check.py:

    ```python
    try:
        sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
        import export_csv  # noqa: F401
        MODULE_INCOMPLETE = not hasattr(export_csv, "write_positives")
    except ImportError:
        MODULE_INCOMPLETE = True
    pytestmark = pytest.mark.skipif(MODULE_INCOMPLETE, reason="export_csv not yet implemented")
    ```

    For test_render_report.py extension: use a per-function guard (the existing tests are GREEN and must remain so), not file-level pytestmark. Pattern:

    ```python
    def test_next_steps_section_default_order(tmp_run_dir):
        if not hasattr(render_report, "render_next_steps_section"):
            pytest.skip("render_next_steps_section not yet implemented (Wave 1)")
        ...
    ```
  </behavior>
  <action>
    Create test_export_csv.py from scratch (~250 lines). Append ~10 test functions to test_render_report.py (DO NOT rewrite the file — read first, append; this preserves all existing GREEN tests).

    Each test must:
    1. Load the relevant fixture (negatives_phase10.json, clusters_phase10.json, etc.) via `json.loads(Path(__file__).parent.joinpath("fixtures", "<name>").read_text())`.
    2. Stage a tmp_run_dir with the inputs export_csv.main() expects (`ranked-enriched.json`, `clusters.json`, `negatives.json`, `brief.md`, optionally `forecast.json` + `compliance-flags.json`).
    3. Call the target helper or `export_csv.main(["--run-dir", str(tmp_run_dir)])`.
    4. Assert the byte / row / structural contract.

    For golden-bytes test: `assert (tmp_run_dir/"export"/"positives.csv").read_bytes() == (fixtures_dir/"golden_positives.csv").read_bytes()`. This is the strongest Nyquist signal — single-byte drift fails it.

    For build_report_json next_steps kwarg test: the Wave 1 implementer (plan 10-02) extends the signature. The test asserts the contract; collecting before Wave 1 implementation is allowed because the per-function `hasattr` guard skips.

    Avoid: leaving any test with no `<automated>` route (every test runs under one of the two quick commands from VALIDATION.md); copy-pasting Phase 9 test logic that asserts forecast shapes (re-derive the assertions for Phase 10); embedding magic numbers in assertions where the fixture already encodes them (read from fixture, don't hardcode).
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify --with respx --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_export_csv.py .claude/skills/google-ad-research/scripts/tests/test_render_report.py -x --tb=short 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    test_export_csv.py collects with 14+ functions, all in SKIPPED state with reason mentioning MODULE_INCOMPLETE (export_csv stub lacks write_positives). test_render_report.py preserves all existing GREEN tests AND adds 10+ new functions, the new ones SKIPPED with reason mentioning "not yet implemented (Wave 1)". Suite-wide test count increases by 24+. Zero ImportError, NameError, or SyntaxError during collection. Existing tests still pass: `pytest tests/test_render_report.py -x` excluding new tests shows 100% pass on the legacy suite.
  </done>
</task>

</tasks>

<verification>
After both tasks commit:
- Full suite collection produces zero errors: `uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ --collect-only -q`
- All Phase 10 fixtures parse as valid JSON / CSV.
- Golden CSV bytes have no BOM and use CRLF endings.
- export_csv.py is importable but `main()` raises NotImplementedError.
- test_export_csv.py and the new test_render_report.py functions all skip cleanly with informative reasons.
- All v1.0 + Phase 9 tests remain GREEN.
</verification>

<success_criteria>
- Wave 0 deliverable: every Wave 1+ task has an `<automated>` verify command that runs against a file that already exists on disk.
- pytest collection is clean (zero errors, zero warnings related to Phase 10).
- Tests are runnable but RED (skipped) — the GREEN transition happens in Wave 1.
- No production code modified outside the export_csv.py stub.
- Atomic commits: one per task.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-launch-kit/10-00-SUMMARY.md`
</output>
