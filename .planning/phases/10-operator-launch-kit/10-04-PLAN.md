---
phase: 10-operator-launch-kit
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
  - .claude/skills/google-ad-research/references/phase10-operator-launch-kit.md
autonomous: false
requirements: [EXPT-01, EXPT-02, EXPT-03, EXPT-04, EXPT-05, STEP-01, STEP-02, STEP-03, STEP-04, CMPL-05]

must_haves:
  truths:
    - "Operator entering Phase 10 in a fresh skill session loads references/phase10-operator-launch-kit.md and follows Steps 41-43 to produce the launch kit"
    - "SKILL.md stays at or under the 500-line cap (currently 497/500) — Phase 10 wiring is a 3-line pointer mirroring the Phase 5/7/8/9 pattern"
    - "Three CSVs import cleanly into Google Ads Editor v2.x without column-mapping errors on a real desktop import (manually verified)"
    - "HTML report's Next Steps checkboxes persist across browser reload AND do NOT bleed across two runs (cross-run isolation verified visually)"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Phase 10 pointer section with Read-tool instruction to load the references rubric"
      contains: "phase10-operator-launch-kit.md"
    - path: ".claude/skills/google-ad-research/references/phase10-operator-launch-kit.md"
      provides: "Operator-facing rubric for Steps 41-43: run export_csv.py + re-render report + verify import"
      min_lines: 80
  key_links:
    - from: "SKILL.md"
      to: "references/phase10-operator-launch-kit.md"
      via: "explicit 'Load it with the Read tool when entering Phase 10' instruction"
      pattern: "phase10-operator-launch-kit\\.md"
    - from: "references/phase10-operator-launch-kit.md"
      to: "scripts/export_csv.py"
      via: "uv run command + parse stdout JSON contract"
      pattern: "uv run .*export_csv\\.py"
    - from: "references/phase10-operator-launch-kit.md"
      to: "scripts/render_report.py"
      via: "Re-run render after Phase 10 to surface Export Files + Next Steps"
      pattern: "render_report\\.py"
---

<objective>
Close out Phase 10 by wiring the new scripts into the operator-facing skill prompt and verifying real-world Editor import works.

Purpose: The skill is operator-facing. If the operator finishes Phase 9 and sees no mention of Phase 10, the launch kit is unreachable. The pointer pattern (3-line stub in SKILL.md + full rubric in references/) is the established Phase 5/7/8/9 convention — it keeps SKILL.md under the 500-line cap while documenting the workflow exhaustively.

The human-verify task is the Editor import smoke that automated tests cannot replicate — Editor is a desktop app, and the manual smoke is the ONLY way to know the CSV bytes that look right on disk actually land in the right Editor columns.

Output: 3-line pointer in SKILL.md (after the Phase 9 pointer block); a new references/phase10-operator-launch-kit.md rubric (~120 lines, mirroring phase9-economics-compliance.md structure); a passed human-verify checkpoint after Editor import.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-operator-launch-kit/10-RESEARCH.md
@.planning/phases/10-operator-launch-kit/10-VALIDATION.md
@.planning/phases/10-operator-launch-kit/10-01-PLAN.md
@.planning/phases/10-operator-launch-kit/10-02-PLAN.md
@.planning/phases/10-operator-launch-kit/10-03-PLAN.md
@.claude/skills/google-ad-research/SKILL.md
@.claude/skills/google-ad-research/references/phase9-economics-compliance.md

<interfaces>
SKILL.md pointer pattern (mirror this EXACTLY for Phase 10):

```markdown
## Phase 9: Campaign Economics and Compliance (optional, launch-kit prep)

> See `.claude/skills/google-ad-research/references/phase9-economics-compliance.md` for full step instructions (Steps 36-40). Load it with the Read tool when entering Phase 9. Phase 9 is optional in the v1.0 workflow but **mandatory for Phase 10 (Operator Launch Kit)** — it adds Suggested Max CPC per keyword, per-cluster + campaign-level budget forecast bands, and a regulated-vertical compliance scan. Pure-compute phase — no API costs. Requires Phase 8 (`ranked-enriched.json` with `cpc_micros`) to have run. Produces an additive mutation of `ranked-enriched.json` plus two new sidecars (`forecast.json`, `compliance-flags.json`).
```

Phase 10 pointer to add after Phase 9 block in SKILL.md (3-4 content lines):

```markdown
---

## Phase 10: Operator Launch Kit (Editor CSVs + Next Steps checklist)

> See `.claude/skills/google-ad-research/references/phase10-operator-launch-kit.md` for full step instructions (Steps 41-43). Load it with the Read tool when entering Phase 10. Phase 10 is the v1.1 capstone — turns the report into a campaign launch kit. Reads Phase 9 outputs (`suggested_max_cpc_micros`, `forecast.campaign_totals.daily_spend_mid_usd`, `compliance-flags.json.matched_verticals[]`) and emits three Google Ads Editor v2.x-importable CSVs under `{run_dir}/export/` plus a bespoke Next-Steps ops checklist in `report.md`. Pure-compute, zero API costs. Requires Phase 9 to have run.
```

Line budget: SKILL.md is currently 497/500 lines. Append must keep total <= 500 (CLAUDE.md NON-NEGOTIABLE).

references/phase10-operator-launch-kit.md structure (mirror phase9-economics-compliance.md):
- Title + 3-line summary
- When to run
- Prerequisites
- Step 41: Confirm operator wants Phase 10 (ask before running)
- Step 42: Run export_csv.py + parse stdout JSON
- Step 43: Re-render report (render_report.py) + surface Export Files + Next Steps in operator confirmation message
- Anti-patterns (5-7 items, drawn from Pitfalls 1-10 in 10-RESEARCH.md)
- Failure modes
- Downstream contract (none — Phase 10 is leaf)

Step numbering note: Phases 1-9 occupy Steps 1-40 (Step 36-40 are Phase 9). Phase 10 starts at Step 41 and is 3 steps long. No further phases — Step 43 is the workflow terminus.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author references/phase10-operator-launch-kit.md operator rubric</name>
  <files>.claude/skills/google-ad-research/references/phase10-operator-launch-kit.md</files>
  <action>
    Create the file at `.claude/skills/google-ad-research/references/phase10-operator-launch-kit.md`. Mirror the structure of `references/phase9-economics-compliance.md` (209 lines, well-tested operator surface from STATE.md).

    Content:

    1. **Title + 3-line summary**: "Phase 10 — Operator Launch Kit" — purpose: Editor CSVs + Next Steps checklist; pure-compute; reads Phase 9 outputs; v1.1 capstone.

    2. **When to run**: After Phase 9 (ranked-enriched.json has `suggested_max_cpc_micros`; forecast.json + compliance-flags.json exist). Recommended cadence: once per campaign brief. Re-run safe: export_csv.py overwrites the export/ folder; render_report.py overwrites report.md / report.json / report.html. Mention the "junior PPC manager moves to live campaign in <30 min instead of 30 min hand-copy" value prop.

    3. **Prerequisites**:
       - `{run_dir}/ranked-enriched.json` with `suggested_max_cpc_micros` field (Phase 9 BIDS-01).
       - `{run_dir}/clusters.json` (Phase 4 validation passed).
       - `{run_dir}/negatives.json` (Phase 6 generate_negatives).
       - `{run_dir}/forecast.json` (Phase 9 FRCS-01).
       - `{run_dir}/compliance-flags.json` (Phase 9 CMPL-01 — written even on empty match).
       - `{run_dir}/brief.md` (Phase 1).
       - No new env keys. Pure stdlib (`csv`, `pathlib`, `hashlib`) + existing `tabulate`.

    4. **Step 41: Confirm operator wants Phase 10**:
       - Ask: "Run Phase 10 (Operator Launch Kit)? Produces three Editor CSVs under `{run_dir}/export/` plus a Next-Steps checklist in `report.md`. No API costs — pure compute. ~5 seconds runtime."
       - If no -> stop. If yes -> continue.

    5. **Step 42: Run `export_csv.py`**:

       ```bash
       uv run "${CLAUDE_SKILL_DIR}/scripts/export_csv.py" --run-dir "{run_dir}"
       ```

       Parse stdout JSON. Surface to operator: `positives_rows`, `negatives_rows`, `ad_groups_rows`, `exports_dir`.

       Healthy run: `positives_rows` approximates `len(clustered keywords)`, `ad_groups_rows == total_clusters`, `negatives_rows == len(negatives.json)`.

       Exit codes:
       - 0 -> continue to Step 43.
       - 3 -> fatal: missing required input (ranked-enriched.json / clusters.json / negatives.json / brief.md). Surface stderr; do NOT proceed. Likely cause: Phase 9 was skipped.
       - 2 -> disk write error (permissions, full disk). Surface stderr; one retry permitted after operator confirms disk state.

       Produces: `{run_dir}/export/positives.csv`, `{run_dir}/export/negatives.csv`, `{run_dir}/export/ad_groups.csv` — UTF-8 no BOM, CRLF endings, RFC 4180 quoting.

    6. **Step 43: Re-render report + confirm launch kit complete**:

       ```bash
       uv run --with python-slugify --with python-dotenv --with tabulate \
         "${CLAUDE_SKILL_DIR}/scripts/render_report.py" --run-dir "{run_dir}"
       ```

       The re-render picks up the new Export Files section (EXPT-05) and the Next Steps section (STEP-01..04 + CMPL-05). On compliance hits, step 1 of the checklist becomes "Complete <verticals> verification at <URLs>" — the rest of the 8 standard steps renumber.

       Surface to operator (final confirmation):

       > "Phase 10 complete — launch kit ready.
       > - CSVs: `{run_dir}/export/positives.csv`, `negatives.csv`, `ad_groups.csv`
       > - Next Steps checklist: `{run_dir}/report.md` (## Next Steps section, {N} steps)
       > - Compliance: {if non-empty: 'reordered — verification is Step 1'; else: 'no flags — standard 8-step order'}
       > - Daily budget for Step 2: ${daily_spend_mid_usd} (Phase 9 mid forecast)
       > - HTML view with checkboxes: open `{run_dir}/report.html` in any browser. Progress saves locally per run.
       >
       > Workflow complete. Import the CSVs into Google Ads Editor via File -> Import -> CSV."

    7. **Anti-patterns**:
       - Don't run Phase 10 before Phase 9. Without `suggested_max_cpc_micros`, the Max CPC column is all "0.00" — operator has to set every bid manually in Editor, defeating the launch-kit value prop.
       - Don't edit the export/ CSVs by hand. The Wave 1 byte contract (no BOM, CRLF, RFC 4180 quoting) is fragile — Excel re-saves break it silently. Re-run export_csv.py instead.
       - Don't paste positives.csv at ad-group level by hand. The CSV's first two columns (Campaign, Ad Group) tell Editor where each row lands; pasting outside the column mapping defeats the contract.
       - Don't ignore the Default Max CPC = $0.00 cells in ad_groups.csv. Those are clusters with no Ahrefs CPC data. Set manually in Editor before enabling, or accept the Editor default.
       - Don't expect the Final URL column to be populated. brief.md has no website-URL field in v1.1; operator pastes URLs in Editor (or deferred to v1.2 brief schema).
       - Don't run Phase 10 if `compliance-flags.json` has not been re-checked since the last brief edit. Stale compliance match -> wrong step-1 reorder.
       - Don't hand-edit the Next Steps text in `report.md`. localStorage checkbox IDs are SHA-1(text) — edits invalidate saved progress.

    8. **Failure modes**:
       - **Empty negatives.csv**: brief produced no negatives. CSV is header-only — valid Editor input, just nothing to paste. Not an error.
       - **Cluster shows Default Max CPC = $0.00**: cluster's keywords all lack Ahrefs CPC data. Editor accepts; operator sets manually.
       - **Match Type rejected by Editor**: Phase 10 ships title-case `Phrase`/`Exact`/`Broad`. If Editor rejects, drop to lowercase by editing `MATCH_TYPE_TITLECASE` in `export_csv.py` and re-running. Wave 3 manual smoke validates the case.
       - **localStorage checkboxes don't persist**: operator opened report.html via `file://` and browser denied localStorage (some Firefox/Safari configs). Open via local web server: `python -m http.server 8000 -d {run_dir}` then navigate to `http://localhost:8000/report.html`.

    9. **Downstream contract**: None — Phase 10 is the workflow leaf. The CSVs feed Google Ads Editor (operator's manual action); next_steps[] + exports[] in report.json feed future v2 tooling (audit logs, dashboards) but no in-skill phase consumes them.

    Aim for ~120-150 lines. Mirror phase9-economics-compliance.md's prose style — direct, second-person operator voice, bash code blocks for commands, explicit exit code handling.

    Avoid: re-deriving content from REQUIREMENTS.md or ROADMAP.md (the rubric is operator-facing, not requirements-facing); embedding test commands (those belong in CLAUDE.md or VALIDATION.md); duplicating the Step 36-40 content from phase9-economics-compliance.md verbatim (cross-link only when needed); using emoji except the existing warning glyph that's part of Phase 9 CMPL-03 surface.
  </action>
  <verify>
    <automated>wc -l .claude/skills/google-ad-research/references/phase10-operator-launch-kit.md | awk '{ if ($1 >= 80 && $1 <= 200) print "PASS: "$1" lines"; else { print "FAIL: "$1" lines outside 80-200"; exit 1 } }' &amp;&amp; grep -c "Step 4" .claude/skills/google-ad-research/references/phase10-operator-launch-kit.md | awk '{ if ($1 >= 3) print "PASS: covers Step 41/42/43"; else { print "FAIL: missing step coverage"; exit 1 } }'</automated>
  </verify>
  <done>
    File exists at the documented path. Line count 80-200. Contains the three step headings (Step 41, Step 42, Step 43). Bash code blocks have `uv run` invocations with `--run-dir` flag. Anti-patterns section has 5+ items. Failure modes section has 3+ items. Operator second-person voice consistent with phase9-economics-compliance.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 10 pointer to SKILL.md (500-line cap enforced)</name>
  <files>.claude/skills/google-ad-research/SKILL.md</files>
  <action>
    Read SKILL.md (currently 497/500 lines). Append the Phase 10 pointer block AFTER the Phase 9 pointer (which ends near line 497).

    Add exactly this block (mirror Phase 9 style, target 3 added content lines + section break):

    ```markdown

    ---

    ## Phase 10: Operator Launch Kit (Editor CSVs + Next Steps checklist)

    > See `.claude/skills/google-ad-research/references/phase10-operator-launch-kit.md` for full step instructions (Steps 41-43). Load it with the Read tool when entering Phase 10. Phase 10 is the v1.1 capstone — turns the report into a campaign launch kit. Reads Phase 9 outputs (`suggested_max_cpc_micros`, `forecast.campaign_totals.daily_spend_mid_usd`, `compliance-flags.json.matched_verticals[]`) and emits three Google Ads Editor v2.x-importable CSVs under `{run_dir}/export/` plus a bespoke Next-Steps ops checklist in `report.md`. Pure-compute, zero API costs. Requires Phase 9 to have run.
    ```

    Verify total line count stays at or under 500 after the edit. If the append pushes past 500, condense the prose (drop "v1.1 capstone" or one parenthetical) until at-or-under 500.

    Avoid: adding new prose anywhere outside the Phase 10 pointer block (other sections are stable); using h1/h2 levels that conflict with the existing pattern (Phase 9 uses `## Phase 9: ...`, so Phase 10 uses `## Phase 10: ...`); embedding step instructions inline (defeats the pointer pattern — the whole point is the rubric lives in references/); adding allowed-tools to the frontmatter (Phase 10 uses only Bash + Read; both already allowed).
  </action>
  <verify>
    <automated>wc -l .claude/skills/google-ad-research/SKILL.md | awk '{ if ($1 &lt;= 500) print "PASS: "$1" lines (under 500 cap)"; else { print "FAIL: "$1" lines exceeds 500 cap"; exit 1 } }' &amp;&amp; grep -c "phase10-operator-launch-kit.md" .claude/skills/google-ad-research/SKILL.md | awk '{ if ($1 &gt;= 1) print "PASS: Phase 10 pointer present"; else { print "FAIL: pointer missing"; exit 1 } }'</automated>
  </verify>
  <done>
    SKILL.md at or under 500 lines (CLAUDE.md NON-NEGOTIABLE). Contains the literal substring "phase10-operator-launch-kit.md" exactly once. The new pointer block follows the Phase 9 block with `---` section break. The pointer prose names the three Phase 9 inputs ("suggested_max_cpc_micros", "forecast.campaign_totals.daily_spend_mid_usd", "compliance-flags.json.matched_verticals[]") so a future skill executor reading top-down sees the dependency chain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Editor import + HTML checkbox cross-run human smoke</name>
  <files>.runs/&lt;chosen-run-dir&gt;/export/positives.csv, .runs/&lt;chosen-run-dir&gt;/export/negatives.csv, .runs/&lt;chosen-run-dir&gt;/export/ad_groups.csv, .runs/&lt;chosen-run-dir&gt;/report.md, .runs/&lt;chosen-run-dir&gt;/report.html, .runs/&lt;chosen-run-dir&gt;/report.json</files>
  <what-built>
    The complete Phase 10 launch kit:
    - `export_csv.py` (Wave 1, plan 10-01) — emits three Editor-importable CSVs.
    - `render_report.py` extension (Waves 1+2, plans 10-02 + 10-03) — emits Export Files + Next Steps sections + report.json `next_steps[]` + `exports[]`.
    - SKILL.md pointer + references/phase10-operator-launch-kit.md (this plan).
    - Full suite green: all Phase 10 unit + integration tests + zero v1.0 / Phase 9 regressions.
  </what-built>
  <action>
    After Tasks 1-2 land and the automated suite is green, the operator (or Claude with operator approval) runs the Phase 10 scripts against a real run-folder and walks through the five visual smokes below. No source files are created by this task — it inspects files written by plans 10-01 through 10-03 and the script invocations.

    Pick an existing run-folder that has all Phase 9 outputs. Suggested: `.runs/2026-05-08T081041Z-primary-urgent-care-car-accident-lake-worth/` (used for Phase 9 closeout smoke per STATE.md; contains the medical+legal compliance match needed to exercise CMPL-05). If unavailable, build a minimal one by copying Phase 10 test fixtures into a fresh `.runs/<timestamp>-test/` folder.

    From the project root, run:

    ```
    uv run .claude/skills/google-ad-research/scripts/export_csv.py --run-dir <run-dir>
    uv run --with python-slugify --with python-dotenv --with tabulate .claude/skills/google-ad-research/scripts/render_report.py --run-dir <run-dir>
    ```

    Each must exit 0 and print a one-line JSON summary on stdout. Then perform the five smokes in `<how-to-verify>`.
  </action>
  <how-to-verify>
    **A. Editor import smoke (CRITICAL — validates EXPT-04 byte contract):**

    1. Confirm `{run_dir}/export/` has all three CSVs after running export_csv.py.
    2. Open Google Ads Editor v2.x on your desktop.
    3. File -> Import -> CSV. Select `positives.csv`. Confirm:
       - Zero column-mapping warnings (Editor pre-fills the mapping correctly).
       - Match Type column accepts Phrase / Exact / Broad (title-case) without error.
       - All rows parse — no "Required column missing" errors.
    4. Repeat for `negatives.csv`. Confirm Strong-tier negatives land at Campaign level; Considered/Investigate land at Ad Group level.
    5. Repeat for `ad_groups.csv`. Confirm Ad group names match cluster names (no massaging); Status = Enabled per row; Default Max CPC pre-fills from the cluster median.

    If ANY warning appears, capture the message and report — do NOT approve.

    **B. HTML checkbox persistence smoke (validates STEP-03):**

    1. Open `{run_dir}/report.html` in a browser (Chrome or Firefox).
    2. Locate the Next Steps section.
    3. Check boxes for steps 1, 3, 5.
    4. Hard refresh (Ctrl+F5 / Cmd+Shift+R).
    5. Confirm boxes 1, 3, 5 are still checked.

    **C. HTML checkbox cross-run isolation smoke (validates Pitfall 7):**

    1. Open a DIFFERENT run folder's `report.html` (pick any other `.runs/.../report.html`).
    2. Confirm ALL checkboxes are unchecked (no bleed from run A).
    3. Open browser DevTools -> Application -> localStorage. Confirm keys are prefixed `gar_<slug_A>_step_<id>` for run A and `gar_<slug_B>_step_<id>` for run B (different prefixes).

    **D. CMPL-05 visual smoke (validates compliance reorder):**

    1. Find a run folder with non-empty `compliance-flags.json["matched_verticals"]` (the suggested primary-urgent-care run will hit medical + legal). If none exists, create a synthetic brief containing "personal injury lawyer" or "ozempic dosage", run Phase 9 + Phase 10.
    2. Open `report.md`. Confirm Step 1 begins with "Complete" and names the matched vertical(s) + verification URL.
    3. Open a NON-compliance run's `report.md`. Confirm Step 1 begins with "Create campaign in".

    **E. Section ordering invariant (validates Pitfall 3):**

    1. Open a compliance-positive run's `report.md`.
    2. Confirm visual order top-to-bottom: Compliance Required block (above Ranked Keywords) -> ... -> Export Files -> Next Steps. The compliance block stays at the top; Next Steps stays at the bottom.

    Approve when all 5 smokes pass. Report any failure with: which smoke, what the failure was, screenshot or log excerpt.
  </how-to-verify>
  <verify>
    Manual smoke — operator confirms via "approved" resume signal. No automated command (this checkpoint exists because Google Ads Editor is a desktop GUI with no programmatic import API; localStorage cross-run isolation requires a real browser to validate).
  </verify>
  <done>
    Operator typed "approved". All 5 smokes pass: (A) three CSVs import to Editor with zero column-mapping warnings; (B) checkboxes persist across browser hard-refresh; (C) two runs' checkboxes are independent in localStorage; (D) compliance-positive run shows verification as Step 1, non-compliance run shows "Create campaign" as Step 1; (E) section order is Compliance-top, Next-Steps-bottom in report.md.
  </done>
  <resume-signal>Type "approved" once all 5 smokes pass. If any smoke fails, describe the failure and which artifact needs adjustment — the next iteration plans a fix.</resume-signal>
</task>

</tasks>

<verification>
- SKILL.md at or under 500 lines AND contains the phase10-operator-launch-kit.md pointer.
- references/phase10-operator-launch-kit.md exists with Steps 41-43.
- 5 manual smokes (A: Editor import, B: HTML persistence, C: cross-run isolation, D: CMPL-05 reorder, E: section order) all pass.
- Full automated suite still green.
</verification>

<success_criteria>
- Phase 10 wired into the operator-facing skill (SKILL.md pointer + references rubric).
- Three CSVs import into Google Ads Editor v2.x without warnings on a real run-folder.
- HTML checkboxes persist across browser reload and isolate cross-run via slug-namespaced localStorage keys.
- CMPL-05 reorder verified visually on a regulated-vertical run AND not-triggered on a non-compliance run.
- SKILL.md stays at or under the 500-line cap (CLAUDE.md NON-NEGOTIABLE).
- Operator has a single-document workflow: SKILL.md pointer -> references rubric -> two `uv run` commands -> launch kit complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-launch-kit/10-04-SUMMARY.md`.

After this plan ships, Phase 10 is complete. Run `/gsd:verify-phase 10` to close out the phase against ROADMAP success criteria, then the v1.1 milestone is fully delivered (Phase 9 data layer + Phase 10 output layer = 23/23 v1.1 requirements complete).
</output>
