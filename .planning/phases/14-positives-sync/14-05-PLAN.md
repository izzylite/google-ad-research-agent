---
phase: 14-positives-sync
plan: 05
type: execute
wave: 5
depends_on: ["14-02", "14-03", "14-04"]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
  - .claude/skills/google-ad-research/references/phase8-account-data.md
autonomous: false
requirements: [POS-06]
must_haves:
  truths:
    - "SKILL.md gains a Phase 14 LLM re-tag step in the Phase 8 sub-flow pointer (or inline if line budget allows)"
    - "SKILL.md final line count <= 500 (CLAUDE.md hard rule)"
    - "references/phase8-account-data.md gains a Step 34a (or equivalent sub-step) describing the LLM re-tag prompt + downstream contract"
    - "LLM re-tag prompt anchored with 5 borderline cases from CONTEXT.md: token reorder, match-type drift, semantic synonym, narrowing opportunity, locale variant"
    - "LLM writes refined buckets back to {run_dir}/positives-sync.json (overwriting script-only output)"
    - "Anti-pattern documented: LLM does NOT invent new bucket names; only re-tags existing entries between the 4 fixed buckets"
    - "Operator-checkpoint at end of Phase 14: human-verify the rendered Positives Sync section + new_to_add positives.csv on a real run"
    - "All POS-01..07 tests pass end-to-end (full suite GREEN after this plan ships)"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Phase 14 LLM re-tag pointer + line-count compliance"
      contains: "Positives Sync"
    - path: ".claude/skills/google-ad-research/references/phase8-account-data.md"
      provides: "Step 34a LLM re-tag prompt rubric + downstream contract for Phase 14"
      contains: "cross_ref_positives"
  key_links:
    - from: "SKILL.md Phase 8 sub-flow"
      to: "references/phase8-account-data.md Step 34a"
      via: "pointer line referencing the LLM re-tag step"
      pattern: "Step 34a|positives.*re.?tag|Positives Sync"
    - from: "LLM re-tag prompt"
      to: "{run_dir}/positives-sync.json (overwrite)"
      via: "Write tool invocation by SKILL.md"
      pattern: "positives-sync.json"
---

<objective>
Plan 14-05 (Wave 4, sequential close-out): document the LLM re-tag step in SKILL.md + references/phase8-account-data.md. Add a final human-verify checkpoint to confirm the full Phase 14 chain on a real account.

Purpose: POS-06 is the operator-visible polish that catches the ~20% of borderline cases plain string-norm dedup misses (token reorder, match-type drift, semantic synonyms). Without it, operators still hand-scrub `positives.csv` for these cases — defeating the milestone goal. This is the FINAL plan; it integrates the prior 4 waves into the operator-facing workflow.

Output: 2 modified docs (SKILL.md + references/phase8-account-data.md); 1 human-verify checkpoint pass.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-positives-sync/14-CONTEXT.md
@.planning/phases/14-positives-sync/14-02-PLAN.md
@.planning/phases/14-positives-sync/14-03-PLAN.md
@.planning/phases/14-positives-sync/14-04-PLAN.md
@CLAUDE.md
@.claude/skills/google-ad-research/SKILL.md
@.claude/skills/google-ad-research/references/phase8-account-data.md

<interfaces>
SKILL.md Phase 8 pointer (lines 472-479):
```markdown
## Phase 8: Account Data + Volume Enrichment (auto-run when creds present)

> See `.claude/skills/google-ad-research/references/phase8-account-data.md` for full step instructions (Steps 31-35). ...
```

references/phase8-account-data.md Step 34 currently covers `perf_synth.py` (negatives sync). Phase 14 adds parallel positives-sync output to the SAME script invocation — no new bash command needed; only the LLM re-tag step is operator-facing.

Borderline cases from CONTEXT.md (use verbatim as anchor examples in the prompt):
1. Token reorder: `urgent care lake worth` ranked vs `lake worth urgent care` active → already_active
2. Match-type drift: ranked exact `pip insurance clinic` vs account broad `pip clinic` → covered_by_broad
3. Semantic synonym: ranked `open 24 hours` vs account `24 hour clinic` (both transactional) → already_active
4. Narrowing opportunity: ranked exact `car accident chiropractor` vs account broad `car accident` → covered_by_broad (flag for operator review)
5. Locale variant: ranked Spanish `clinica de accidente` vs account English `accident clinic` → stays new_to_add

Current SKILL.md line count (per planning context): 499 lines. Adding more than ~1 line will breach the 500-line cap → MUST update references/phase8-account-data.md inline and add only a single sentence to the SKILL.md Phase 8 pointer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 34a LLM re-tag rubric to references/phase8-account-data.md</name>
  <files>.claude/skills/google-ad-research/references/phase8-account-data.md</files>
  <action>
Insert a new "## Step 34a: LLM re-tag for positives-sync (POS-06)" section between the existing Step 34 (perf_synth invocation) and Step 35 (re-render). The new step must:

1. Trigger condition: runs ONLY when `{run_dir}/positives-sync.json` exists AND Phase 14 is enabled. Skips silently otherwise.

2. Instruction body for Claude: read positives-sync.json + ranked-enriched.json + (if present) competitor-intel.json, then re-classify borderline cases using the 5 anchor examples (verbatim from CONTEXT.md borderline list). Rewrite positives-sync.json with refined buckets.

3. Prompt anchor block — embed the 5 borderline cases as a verbatim list:

```markdown
**Borderline cases LLM must catch (anchor examples):**

1. **Token reorder** — `urgent care lake worth` (ranked) vs `lake worth urgent care` (active) → re-tag to `already_active`.
2. **Match-type drift** — ranked exact `pip insurance clinic` vs account broad `pip clinic` → re-tag to `covered_by_broad`.
3. **Semantic synonym** — ranked `open 24 hours` vs account `24 hour clinic` (both transactional) → re-tag to `already_active`.
4. **Narrowing opportunity** — ranked exact `car accident chiropractor` vs account broad `car accident` → re-tag to `covered_by_broad` AND flag in justification: "Exact narrows targeting — operator review".
5. **Locale variant** — ranked Spanish `clinica de accidente` vs account English `accident clinic` → STAY `new_to_add` (locale gap is a legitimate net-new opportunity).
```

4. Output contract section — be explicit:

```markdown
**LLM output contract (Write tool to {run_dir}/positives-sync.json):**

- Preserve all top-level keys from the input file (synthesized_at, our_total, existing_total, stats).
- Only re-distribute entries BETWEEN the 4 existing bucket arrays: `already_active`, `paused_in_account`, `covered_by_broad`, `new_to_add`. Do NOT introduce new bucket names.
- For each re-tagged entry, append a `"retag_reason"` field with one of the 5 anchor case labels (e.g., "token_reorder", "match_type_drift", "semantic_synonym", "narrowing_opportunity").
- Recompute the `stats` block to reflect the new bucket counts. Total entries must remain unchanged (no kw added or dropped).
- Untouched entries: leave verbatim.
```

5. Anti-patterns block (mirror the existing anti-patterns section style):

```markdown
**Anti-patterns:**

- **Don't invent new bucket names.** The 4 buckets are locked — re-tag entries between them; do NOT create `maybe_active` or similar.
- **Don't drop entries.** Re-tagging must preserve total kw count. operator-side audit relies on the count staying constant.
- **Don't re-tag without evidence.** Each re-tagged entry must have a `retag_reason` matching one of the 5 anchor cases. If a case doesn't fit any, leave the entry alone.
- **Don't run on briefs without `positives-sync.json`.** Phase 14 graceful-skip (POS-05) means absent file = nothing to re-tag. Halt the step silently.
```

6. Downstream contract pointer — note that after this step, `render_report.py` should be re-invoked (Step 35) so report.md picks up the refined buckets. The CSV (Plan 14-04) consumes the same refined positives-sync.json automatically on the next export_csv.py invocation.
  </action>
  <verify>
    <automated>grep -c "Step 34a" .claude/skills/google-ad-research/references/phase8-account-data.md</automated>
  </verify>
  <done>Step 34a section exists with all 6 components (trigger / instruction body / 5 anchor cases / output contract / anti-patterns / downstream pointer); existing Steps 31-35 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md Phase 8 pointer — line-budget-aware</name>
  <files>.claude/skills/google-ad-research/SKILL.md</files>
  <action>
1. Run `wc -l .claude/skills/google-ad-research/SKILL.md` to confirm current line count.

2. If at 499 lines (or close): add ONE line ONLY to the Phase 8 pointer mentioning the new Step 34a. Pattern:

```markdown
## Phase 8: Account Data + Volume Enrichment (auto-run when creds present)

> See `.claude/skills/google-ad-research/references/phase8-account-data.md` for full step instructions (Steps 31-35 + 34a LLM re-tag for POS-06 positives sync). ...
```

Single-character-style change — append ` + 34a LLM re-tag for POS-06 positives sync` to the existing parenthetical. No new blank lines, no new bullet points.

3. After the edit, run `wc -l .claude/skills/google-ad-research/SKILL.md` again. The line count MUST be <= 500. If it ends up at 500 or 501, compact a blank line elsewhere (e.g., between Phase 10 and Phase 11 pointers, where CLAUDE.md decision history shows compaction has been done before — Phase 11 04-04 commit).

4. Confirm no other Phase pointer is altered. Phase 14 LLM re-tag is intentionally documented as a sub-step of Phase 8 (not a new top-level Phase 14 section in SKILL.md) because the perf_synth invocation that produces positives-sync.json already runs in Step 34.
  </action>
  <verify>
    <automated>test $(wc -l < .claude/skills/google-ad-research/SKILL.md) -le 500 && grep -c "34a" .claude/skills/google-ad-research/SKILL.md</automated>
  </verify>
  <done>SKILL.md still <= 500 lines; Phase 8 pointer mentions Step 34a / POS-06 / positives sync; no other Phase sections altered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 14 end-to-end on a real run</name>
  <files>(no files modified — verification only)</files>
  <action>Operator runs the skill on a real client account to verify all 7 POS checkpoints (see how-to-verify block below). This is a manual smoke; no code changes here.</action>
  <verify><automated>uv run --with pytest --with python-dotenv --with python-slugify --with respx --with httpx --with httpx-retries --with tabulate --with inflect pytest .claude/skills/google-ad-research/scripts/tests/ -x</automated></verify>
  <done>All 7 POS checkpoints (POS-01..07) confirmed passing on a real run; full test suite GREEN.</done>
  <what-built>
Full Phase 14 chain across 5 plans:
- perf_fetch.py pulls keyword_view (POS-01)
- perf_synth.py writes positives-sync.json with 4 buckets (POS-02, POS-05)
- render_report.py renders ## Positives Sync section in report.md + HTML (POS-03, POS-05)
- export_csv.py filters positives.csv to new_to_add by default + --include-existing override (POS-04, POS-05)
- SKILL.md Step 34a documents the LLM re-tag step (POS-06)
- Test coverage: unit tests + golden fixture + respx mocks (POS-07)
  </what-built>
  <how-to-verify>
Operator runs the skill on a real client account where Phase 8 OAuth is already wired (e.g., the Lake Worth FL urgent-care brief used for Phase 11 + 12 closeout smokes). Verify all 7 checkpoints:

1. **POS-01:** After Phase 8 perf_fetch.py runs, `{run_dir}/raw/google-ads-keywords.json` exists with items[] containing keyword/match_type/status/ad_group/campaign fields. Inspect first 2-3 items.

2. **POS-02:** After Phase 8 perf_synth.py runs, `{run_dir}/positives-sync.json` exists with 4 bucket keys + stats block. Sanity check: stats counts sum to our_total.

3. **POS-03:** `{run_dir}/report.md` contains a `## Positives Sync` section with stats line + enumerated new_to_add list + count-only audit buckets. Visual check.

4. **POS-04:** `{run_dir}/export/positives.csv` row count equals stats.new_to_add count from positives-sync.json. Open in a text editor or `wc -l`.

5. **POS-04 (--include-existing):** Re-run `uv run export_csv.py --run-dir {run_dir} --include-existing`. positives.csv now has all ranked rows + a `Status` column. Spot-check a row from each bucket.

6. **POS-05 (graceful skip):** On a different brief without Phase 8 OAuth (or with raw/google-ads-keywords.json manually deleted), re-run the skill from Phase 8 perf_synth onward. positives-sync.json should NOT be produced; report.md should NOT contain ## Positives Sync; positives.csv should match pre-Phase-14 behaviour (full ranked list, no Status column).

7. **POS-06:** Manually invoke the SKILL.md Step 34a re-tag prompt against the real positives-sync.json. Confirm at least one borderline kw flips bucket; check `retag_reason` field appears on re-tagged entries; check stats block recomputed and counts still sum to our_total.

8. **POS-07:** Full test suite green:
```
uv run --with pytest --with python-dotenv --with python-slugify --with respx --with httpx --with httpx-retries --with tabulate --with inflect pytest .claude/skills/google-ad-research/scripts/tests/ -x
```
Expected: 250+ passed, 0 failed, all Phase 14 tests in PASSED state (no remaining SKIPs from Wave 0).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checkpoints pass on a real run, or describe any failure mode with the exact bucket / file / row that misbehaved.</resume-signal>
</task>

</tasks>

<verification>
1. SKILL.md line count <= 500
2. references/phase8-account-data.md grep for "Step 34a" returns 1+ match
3. Operator approval on the human-verify checkpoint above
4. Full test suite green: `uv run pytest .claude/skills/google-ad-research/scripts/tests/ -x` → 0 failures, 0 SKIPs for Phase 14
</verification>

<success_criteria>
- POS-06 satisfied: SKILL.md + references doc carry the LLM re-tag prompt + downstream contract
- Phase 14 ships end-to-end (POS-01..07 all complete)
- Real-account smoke confirms operator UX of new_to_add-by-default positives.csv
</success_criteria>

<output>
After completion, create `.planning/phases/14-positives-sync/14-05-SUMMARY.md` and update STATE.md + REQUIREMENTS.md to mark POS-01..07 Complete + v1.4 milestone shipped.
</output>
