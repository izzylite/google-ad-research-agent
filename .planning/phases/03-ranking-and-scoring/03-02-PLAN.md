---
phase: 03-ranking-and-scoring
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
autonomous: true
requirements: [RANK-01]
must_haves:
  truths:
    - "SKILL.md Step 11 contains the locked 4-class intent rubric with anchor examples and temperature=0 directive"
    - "SKILL.md Step 11 instructs writing intent-labels.json with the correct schema before advancing"
    - "SKILL.md Step 12 invokes rank_keywords.py via uv run with --run-dir and gates on exit code"
    - "SKILL.md Step 13 confirms Phase 3 complete and stops"
    - "SKILL.md remains under 500 lines"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Steps 11-13: intent labeling rubric, intent-labels.json write, rank_keywords.py invocation"
      contains: "Step 11"
  key_links:
    - from: "SKILL.md Step 11"
      to: "{run_dir}/intent-labels.json"
      via: "Write tool instruction with schema"
      pattern: "intent-labels\\.json"
    - from: "SKILL.md Step 12"
      to: "scripts/rank_keywords.py"
      via: "uv run invocation"
      pattern: "rank_keywords\\.py"
---

<objective>
Extend SKILL.md with Steps 11-13: the intent labeling rubric (RANK-01), the intent-labels.json write instruction, and the rank_keywords.py invocation.

Purpose: Connect the locked categorical rubric (non-negotiable from STATE.md decisions) to the operator-facing workflow so Phase 3 runs end-to-end in a skill session.
Output: SKILL.md with Steps 11-13 appended; stays under 500 lines.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ranking-and-scoring/03-RESEARCH.md
@.planning/phases/03-ranking-and-scoring/03-01-SUMMARY.md

# Current SKILL.md (must stay ≤ 500 lines after update)
@.claude/skills/google-ad-research/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 11-13 to SKILL.md</name>
  <files>.claude/skills/google-ad-research/SKILL.md</files>
  <action>
Read SKILL.md. Locate the Phase 2 stop line (Step 10 ending: "STOP. Do not proceed to ranking..."). Replace that stop line with a forward gate: "Phase 3 (ranking and scoring) begins at Step 11 below." Then append the following three steps.

**Step 11 — Intent labeling (RANK-01)**

Use the template from 03-RESEARCH.md § Pattern 1 (SKILL.md Step 11 structure) and § SKILL.md Step 11 Batch Prompt Template. Key requirements:
- Read {run_dir}/keywords.json
- Process in batches of ≤ 30 keywords
- Include the 4 anchor examples in EVERY batch (verbatim from research: order grocery delivery → transactional; best grocery delivery uk → commercial; how does grocery delivery work → informational; ocado website → navigational)
- Assign intent (exactly one of: informational | commercial | transactional | navigational) using the categorical rubric table from 03-RESEARCH.md § Categorical Rubric (embed the full 4-class table inline — this is the locked rubric)
- Assign match_type using the rules from 03-RESEARCH.md § Match-Type Heuristic (exact for transactional/navigational with source_diversity ≥ 3; phrase otherwise; broad do not assign in v1)
- Write intent-labels.json via the Write tool to {run_dir}/intent-labels.json with schema: [{"canonical": "...", "lemma_hash": "...", "intent": "...", "match_type": "..."}]
- Gate: verify len(intent-labels.json) == len(keywords.json) before advancing; if short, re-run the missing batch only
- Write intent-meta.json: {"model": "<model name>", "rubric_version": "v1.0", "batches": N, "keywords_labeled": N, "scored_at": "<ISO timestamp>"}
- Temperature: 0 directive (verbatim: "Temperature: 0. Use the categorical rubric — do NOT score 0-1 or use any scale.")
- Do not advance to Step 12 until intent-labels.json exists and every keyword has a matching entry

**Step 12 — Run rank_keywords.py (RANK-02, RANK-03, RANK-04)**

```bash
uv run "${CLAUDE_SKILL_DIR}/scripts/rank_keywords.py" --run-dir "{run_dir}"
```

Parse stdout JSON. Surface ranked_count, avg_score, intent_distribution to operator.

Exit code handling:
- Exit 0: continue
- Exit 3: surface error message from stderr; do NOT proceed; tell operator which file is missing or which keyword has no label

Do not advance to Step 13 until ranked.json exists in run_dir.

**Step 13 — Confirm Phase 3 complete and stop**

Tell the operator:
> "Phase 3 complete. Ranking summary:
> - {ranked_count} keywords ranked
> - Avg score: {avg_score:.1f}
> - Intent distribution: {intent_distribution}
>
> Ranked keyword file: `{run_dir}/ranked.json`
>
> Phase 4 (clustering) is not yet available in this skill."

STOP. Do not proceed to any Phase 4+ activity.

**Line count check:** After writing, count lines in SKILL.md. If > 500, extract the 4-class rubric table into `.claude/skills/google-ad-research/references/intent-rubric.md` and replace it with `@references/intent-rubric.md` inline load instruction — but attempt inline first.
  </action>
  <verify>
    <automated>python -c "lines = open('.claude/skills/google-ad-research/SKILL.md').readlines(); count = len(lines); print(f'Lines: {count}'); assert count <= 500, f'SKILL.md exceeds 500 lines: {count}'"</automated>
  </verify>
  <done>
    SKILL.md contains Steps 11, 12, and 13. Step 11 has the 4-class rubric table, anchor examples, temperature=0 directive, intent-labels.json write instruction, and the len-check gate. Step 12 has the uv run invocation and exit-code handling. Step 13 has the summary message and STOP directive. Line count ≤ 500.
  </done>
</task>

</tasks>

<verification>
Line count check:
```bash
python -c "lines = open('.claude/skills/google-ad-research/SKILL.md').readlines(); print(len(lines))"
```

Content checks:
```bash
python -c "
text = open('.claude/skills/google-ad-research/SKILL.md').read()
assert 'Step 11' in text
assert 'Step 12' in text
assert 'Step 13' in text
assert 'intent-labels.json' in text
assert 'rank_keywords.py' in text
assert 'Temperature: 0' in text
assert 'transactional' in text
assert 'commercial' in text
assert 'informational' in text
assert 'navigational' in text
print('All checks passed')
"
```
</verification>

<success_criteria>
- SKILL.md Steps 11-13 exist with correct content
- Step 11 contains the locked 4-class rubric table with all anchor examples
- Step 11 has temperature=0 directive and len-check gate
- Step 12 has the uv run invocation with --run-dir and exit-code 3 handling
- Step 13 has the STOP directive
- SKILL.md is ≤ 500 lines
- Full test suite still passes (SKILL.md change is text-only; no Python regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ranking-and-scoring/03-02-SUMMARY.md`
</output>
