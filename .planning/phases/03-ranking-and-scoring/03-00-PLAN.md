---
phase: 03-ranking-and-scoring
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/keywords_phase2.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/intent_labels.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "pytest collects all test_rank_keywords.py test functions without ImportError"
    - "Every test fails (RED) — rank_keywords.py does not exist yet"
    - "Fixtures cover the full diversity range (source_diversity 1, 2, 3, 4) and all four intent classes"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py"
      provides: "RED test stubs for RANK-01 through RANK-04"
      exports: []
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/keywords_phase2.json"
      provides: "Realistic keywords.json sample — 5 rows from merge_signals.py schema"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/intent_labels.json"
      provides: "Matching intent-labels.json — one entry per fixture keyword covering all 4 intent classes"
  key_links:
    - from: "test_rank_keywords.py"
      to: "scripts/rank_keywords.py"
      via: "import rank_keywords (will fail RED until Wave 1)"
      pattern: "import rank_keywords"
---

<objective>
Write RED test stubs and fixtures for rank_keywords.py before the implementation exists.

Purpose: Establish the Nyquist-compliant test contract so Wave 1 has explicit pass criteria and cannot ship without green tests.
Output: test_rank_keywords.py (all tests failing), keywords_phase2.json fixture, intent_labels.json fixture.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ranking-and-scoring/03-RESEARCH.md

# Existing test infrastructure to follow
@.claude/skills/google-ad-research/scripts/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write keywords_phase2.json and intent_labels.json fixtures</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/fixtures/keywords_phase2.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/intent_labels.json
  </files>
  <action>
Create two fixture files that together exercise the full scoring matrix.

**keywords_phase2.json** — 5 rows matching merge_signals.py output schema exactly. Use these rows (all canonical, lemma_hash is 16-char hex, sources is array of objects with a "source" string key):

```json
[
  {
    "canonical": "order groceries uk",
    "lemma_hash": "a1b2c3d4e5f6a7b8",
    "signal_count": 5,
    "source_diversity": 4,
    "sources": [
      {"source": "serper-organic"}, {"source": "serper-paa"},
      {"source": "tavily-extract"}, {"source": "websearch-baseline"}
    ]
  },
  {
    "canonical": "best grocery delivery uk",
    "lemma_hash": "b2c3d4e5f6a7b8c9",
    "signal_count": 8,
    "source_diversity": 3,
    "sources": [
      {"source": "serper-organic"}, {"source": "serper-related"},
      {"source": "websearch-baseline"}
    ]
  },
  {
    "canonical": "how does grocery delivery work",
    "lemma_hash": "c3d4e5f6a7b8c9d0",
    "signal_count": 3,
    "source_diversity": 3,
    "sources": [
      {"source": "serper-paa"}, {"source": "serper-related"},
      {"source": "websearch-baseline"}
    ]
  },
  {
    "canonical": "ocado website",
    "lemma_hash": "d4e5f6a7b8c9d0e1",
    "signal_count": 1,
    "source_diversity": 1,
    "sources": [
      {"source": "serper-organic"}
    ]
  },
  {
    "canonical": "grocery delivery service",
    "lemma_hash": "e5f6a7b8c9d0e1f2",
    "signal_count": 6,
    "source_diversity": 2,
    "sources": [
      {"source": "serper-organic"}, {"source": "tavily-extract"}
    ]
  }
]
```

**intent_labels.json** — 5 rows matching intent-labels.json schema (canonical, lemma_hash, intent, match_type). Assign:
- "order groceries uk" → transactional, exact (diversity ≥ 3)
- "best grocery delivery uk" → commercial, phrase
- "how does grocery delivery work" → informational, phrase
- "ocado website" → navigational, phrase (diversity < 3)
- "grocery delivery service" → commercial, phrase

All lemma_hash values must match keywords_phase2.json exactly.
  </action>
  <verify>
    <automated>uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <done>Both JSON files exist and are valid JSON. Each intent_labels.json entry has canonical, lemma_hash, intent (one of 4 classes), match_type (phrase or exact).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for rank_keywords.py</name>
  <files>.claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py</files>
  <behavior>
    All tests import from rank_keywords (will raise ImportError / ModuleNotFoundError → RED).
    - test_compute_score_formula: compute_score(4, "transactional", 5) == 435
    - test_source_diversity_dominates_signal_count: score(4, "informational", 1) > score(1, "transactional", 99)
    - test_intent_weight_ordering: transactional > commercial > informational at same diversity
    - test_sort_tiebreak: tie on score → higher signal_count wins; tie on both → alphabetical
    - test_validate_labels_rejects_invalid_intent: ValueError on intent="unknown"
    - test_validate_labels_rejects_invalid_match_type: ValueError on match_type="unknown"
    - test_missing_label_exits_3: build_ranked raises ValueError when a keyword has no matching label
    - test_match_type_exact_transactional: transactional + diversity ≥ 3 → exact (from intent_labels fixture)
    - test_match_type_exact_navigational: navigational + diversity ≥ 3 → exact
    - test_match_type_phrase_default: commercial and informational → phrase
    - test_no_broad_in_output: no row in build_ranked output has match_type == "broad"
    - test_output_schema_columns: every row has exactly the 8 canonical keys
    - test_no_volume_field_name: "volume" not in any row key
    - test_theme_empty_string: theme == "" for all rows
    - test_sources_compact_form: sources field is list of strings (not dicts)
    - test_deterministic_output: same inputs → identical ranked list on two calls
  </behavior>
  <action>
Create test_rank_keywords.py following the existing conftest/test patterns (see conftest.py). Key points:

1. Import at top of file: `import rank_keywords` — this is the RED import that will fail until Wave 1.
2. Load fixtures via pathlib relative to conftest FIXTURES_DIR pattern:
   ```python
   FIXTURES_DIR = Path(__file__).parent / "fixtures"
   ```
3. Use `keywords_phase2.json` and `intent_labels.json` fixtures loaded with `json.loads(...)`.
4. Tests for `compute_score` and `validate_labels` call the functions directly.
5. Tests for `build_ranked` pass the loaded fixture dicts; for the label dict, build it by calling `validate_labels(intent_labels_list)` first.
6. `test_missing_label_exits_3`: pass a keywords list with a lemma_hash that has NO matching entry in labels → expect `ValueError`.
7. `test_match_type_exact_navigational`: need a navigational keyword with source_diversity ≥ 3. Since the fixture has ocado (navigational, diversity 1), fabricate an inline row: `{"canonical": "ocado login", "lemma_hash": "nav3_hash", "signal_count": 2, "source_diversity": 3, "sources": [{"source": "s1"}, {"source": "s2"}, {"source": "s3"}]}` with matching label `{"canonical": "ocado login", "lemma_hash": "nav3_hash", "intent": "navigational", "match_type": "exact"}`.
8. Do NOT mark tests as `@pytest.mark.xfail` — they must be genuine RED failures (ImportError).

Research reference: 03-RESEARCH.md § Validation Architecture → Phase Requirements → Test Map for full test names and assertions.
  </action>
  <verify>
    <automated>uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py --collect-only -q</automated>
  </verify>
  <done>pytest --collect-only shows all 16 test functions collected. Running the tests (without --collect-only) produces errors/failures (not passes) because rank_keywords.py does not exist yet.</done>
</task>

</tasks>

<verification>
```bash
uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py --collect-only -q
```
Must show 16 test items collected with zero errors at collection time (the import error surfaces at run time, not collection time — use `importorskip` or wrap import in try/except at module level with a skip marker only if needed to allow collection).

Actually: structure the import so collection works even when rank_keywords is missing. Pattern:
```python
try:
    import rank_keywords
    RK_MISSING = False
except ImportError:
    rank_keywords = None  # type: ignore
    RK_MISSING = True
```
Then each test body: `if RK_MISSING: pytest.skip("rank_keywords not yet implemented")` — this makes tests SKIP (not ERROR) at Wave 0, which is still RED from a coverage perspective, and avoids conftest import-error noise.
</verification>

<success_criteria>
- keywords_phase2.json: 5 rows, valid JSON, merge_signals.py schema
- intent_labels.json: 5 rows with matching lemma_hashes, all 4 intent classes represented
- test_rank_keywords.py: 16 test functions collected by pytest
- All 16 tests skip or fail (not pass) — rank_keywords.py does not exist
- Full suite still passes: `uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/03-ranking-and-scoring/03-00-SUMMARY.md`
</output>
