---
phase: 03-ranking-and-scoring
plan: "01"
type: tdd
wave: 1
depends_on: ["03-00"]
files_modified:
  - .claude/skills/google-ad-research/scripts/rank_keywords.py
autonomous: true
requirements: [RANK-02, RANK-03, RANK-04]
must_haves:
  truths:
    - "Running rank_keywords.py --run-dir <dir> on valid inputs writes ranked.json and exits 0"
    - "Ranked.json rows contain exactly the 8 canonical columns — no 'volume', no null theme"
    - "source_diversity is the dominant ranking signal: a 4-source keyword outranks any 1-source keyword regardless of signal_count or intent"
    - "match_type is exact only for transactional or navigational keywords with source_diversity >= 3; phrase otherwise; broad never appears"
    - "Identical inputs produce identical ranked.json on repeated runs"
    - "Missing intent label causes exit code 3"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/rank_keywords.py"
      provides: "Deterministic scoring: keywords.json + intent-labels.json → ranked.json"
      exports: ["compute_score", "build_ranked", "validate_labels"]
      min_lines: 80
  key_links:
    - from: "rank_keywords.py main()"
      to: "{run_dir}/intent-labels.json"
      via: "json.loads + validate_labels()"
      pattern: "validate_labels"
    - from: "rank_keywords.py build_ranked()"
      to: "{run_dir}/ranked.json"
      via: "json.dumps + Path.write_text"
      pattern: "ranked\\.json"
---

<objective>
Implement rank_keywords.py — pure deterministic math that joins intent-labels.json with keywords.json, computes composite scores, applies match-type heuristics, and writes ranked.json.

Purpose: Turn all Wave 0 tests from SKIP/RED to GREEN. This is the only Phase 3 Python script; no LLM calls, no new dependencies.
Output: .claude/skills/google-ad-research/scripts/rank_keywords.py
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ranking-and-scoring/03-RESEARCH.md
@.planning/phases/03-ranking-and-scoring/03-00-SUMMARY.md

# Existing script conventions
@.claude/skills/google-ad-research/scripts/merge_signals.py

# Test contract (already RED — must turn GREEN)
@.claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py
@.claude/skills/google-ad-research/scripts/tests/fixtures/keywords_phase2.json
@.claude/skills/google-ad-research/scripts/tests/fixtures/intent_labels.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement rank_keywords.py (RED → GREEN)</name>
  <files>.claude/skills/google-ad-research/scripts/rank_keywords.py</files>
  <behavior>
    All 16 tests in test_rank_keywords.py must pass after this task.

    Core contracts (from 03-RESEARCH.md § Code Examples and § Composite Scoring Formula):
    - compute_score(source_diversity, intent, signal_count) → source_diversity*100 + INTENT_WEIGHTS[intent] + signal_count
      INTENT_WEIGHTS = {transactional:30, commercial:20, navigational:10, informational:5}
    - validate_labels(list[dict]) → dict[lemma_hash → row]; raises ValueError on invalid intent or match_type
    - build_ranked(keywords, labels_dict) → list sorted (-score, -signal_count, keyword asc); raises ValueError on missing label
    - Output row keys exactly: keyword, intent, match_type, theme, signal_count, source_diversity, sources, score
      - sources: sorted list of distinct source *strings* (not dicts) from kw["sources"][*]["source"]
      - theme: "" always
    - CLI: reads {run_dir}/keywords.json + {run_dir}/intent-labels.json; writes {run_dir}/ranked.json
    - stdout: single JSON line {"ranked_count": N, "avg_score": float, "intent_distribution": {...}}
    - exit 0 ok, exit 3 on any fatal (missing file, join failure, validation error)
  </behavior>
  <action>
Create rank_keywords.py following PEP 723 inline metadata pattern used by merge_signals.py (# /// script block, requires-python = ">=3.13", dependencies = []).

Structure:
1. Module docstring with CLI contract (see 03-RESEARCH.md § Pattern 2 — CLI contract)
2. INTENT_WEIGHTS dict constant
3. compute_score(source_diversity, intent, signal_count) → int
4. validate_labels(labels_list) → dict[str, dict] — raises ValueError on bad intent/match_type
5. build_ranked(keywords, labels) → list[dict] — raises ValueError on missing label; sorts (-score, -signal_count, keyword)
6. main() — argparse --run-dir, loads files, calls build_ranked, writes ranked.json, prints stdout summary, sys.exit(3) on ValueError/FileNotFoundError
7. if __name__ == "__main__": main()

Do NOT implement match-type logic in rank_keywords.py — match_type comes from intent-labels.json (written by the skill prompt in Step 11). rank_keywords.py only reads it from the label and passes it through to ranked.json. This is the locked architecture from 03-RESEARCH.md § Pattern 1.

Exception: validate_labels() does validate that match_type is one of {phrase, exact, broad} — it rejects unknown values.

Reference the full scoring core from 03-RESEARCH.md § Code Examples → rank_keywords.py — Scoring Core. Use it as the implementation blueprint; the test_rank_keywords.py stubs were written against those exact function signatures.
  </action>
  <verify>
    <automated>uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_rank_keywords.py -x -q</automated>
  </verify>
  <done>All 16 tests in test_rank_keywords.py pass. Full suite still green: uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ -x -q</done>
</task>

</tasks>

<verification>
Full test suite green:
```bash
uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ -x -q
```

Smoke test via CLI (create minimal fixture files in a temp dir):
```bash
uv run .claude/skills/google-ad-research/scripts/rank_keywords.py --run-dir /tmp/smoke-rank-test
```
Should exit 3 with a clear message (missing keywords.json) — not an unhandled traceback.
</verification>

<success_criteria>
- rank_keywords.py exists with PEP 723 metadata, stdlib-only, no new pip deps
- All 16 test_rank_keywords.py tests pass
- Full test suite passes (no regressions in Phase 1/2 tests)
- compute_score, build_ranked, validate_labels importable as module-level functions
- ranked.json written correctly for the fixture keywords (verifiable via test_output_schema_columns)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ranking-and-scoring/03-01-SUMMARY.md`
</output>
