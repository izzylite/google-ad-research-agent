---
phase: 11-account-structure-mapping
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/tests/test_geo_filter.py
  - .claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py
  - .claude/skills/google-ad-research/scripts/tests/test_run_init.py
  - .claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py
  - .claude/skills/google-ad-research/scripts/tests/test_merge_signals.py
  - .claude/skills/google-ad-research/scripts/tests/test_export_csv.py
  - .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/us-cities-subset.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-perf-phase11.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-search-terms-phase11.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-50pct.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-60pct.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-20pct.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/brief-with-geo-focus.md
  - .claude/skills/google-ad-research/scripts/tests/fixtures/brief-no-geo-focus.md
  - .claude/skills/google-ad-research/scripts/ad_group_match.py
autonomous: true
requirements: [GEO-01, GEO-02, GEO-03, GEO-04, GEO-05, ADGM-01, ADGM-02, ADGM-03, ADGM-04, ADGM-05, ADGM-06]
must_haves:
  truths:
    - "Two new RED test files (test_geo_filter.py + test_ad_group_match.py) exist and collect cleanly under pytest"
    - "All seven Phase 11 fixture files exist and load as valid JSON / markdown"
    - "ad_group_match.py exists at scripts/ root with PEP 723 header, _THRESHOLDS frozen, and main() raises NotImplementedError"
    - "Test imports succeed; absent feature-functions skip via MODULE_INCOMPLETE / hasattr guards (Phase 10 10-00 pattern)"
    - "Full pytest suite still green for legacy tests; new RED stubs SKIPPED — zero regressions"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_geo_filter.py"
      provides: "RED stubs for GEO-03 + GEO-04 (city-county-state filter, hierarchy)"
      contains: "def test_us_cities_loadable"
    - path: ".claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py"
      provides: "RED stubs for ADGM-01..04 (graceful skip, jaccard math, tiers, mapping shape)"
      contains: "def test_phase8_absent_graceful_skip"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/us-cities-subset.json"
      provides: "FL + TX + CA city→county subset for filter tests"
      contains: "boca raton"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-perf-phase11.json"
      provides: "Phase 8 perf shape with 3-4 ENABLED ad groups"
      contains: "ad_groups"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-search-terms-phase11.json"
      provides: "Search terms keyed by ad_group_name (NOT ad_group_id — Pitfall 1)"
      contains: "ad_group_name"
    - path: ".claude/skills/google-ad-research/scripts/ad_group_match.py"
      provides: "MODULE_INCOMPLETE stub — _THRESHOLDS locked + main() NotImplementedError"
      contains: "_THRESHOLDS"
  key_links:
    - from: ".claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py"
      to: ".claude/skills/google-ad-research/scripts/ad_group_match.py"
      via: "try: import ad_group_match; MODULE_INCOMPLETE = not hasattr(ad_group_match, 'build_mapping')"
      pattern: "MODULE_INCOMPLETE"
    - from: ".claude/skills/google-ad-research/scripts/tests/test_geo_filter.py"
      to: ".claude/skills/google-ad-research/scripts/tests/fixtures/us-cities-subset.json"
      via: "Path(__file__).parent / 'fixtures' / 'us-cities-subset.json'"
      pattern: "us-cities-subset.json"
---

<objective>
Wave 0: RED-stub scaffolding for Phase 11. Lands two new test files (test_geo_filter.py, test_ad_group_match.py), extends five existing test files with hasattr-guarded RED stubs, ships seven fixtures (including a small us-cities subset and two ad-group-mapping coverage fixtures at the 50% / 60% / 20% boundary), and the ad_group_match.py MODULE_INCOMPLETE stub with the _THRESHOLDS config block already locked.

Purpose: Mirror the Phase 10 10-00 RED-scaffold pattern. Tests collect cleanly today; flip GREEN as Wave 1 (11-01 geo, 11-02 ad_group_match core) and Wave 2 (11-03 integrations) ship.

Output: 16 files (8 new + 8 modified/new tests). Zero legacy regressions. All new tests SKIPPED via MODULE_INCOMPLETE / per-function hasattr guards. ad_group_match.py imports cleanly but raises NotImplementedError on `main()`.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-account-structure-mapping/11-RESEARCH.md
@.planning/phases/11-account-structure-mapping/11-VALIDATION.md
@CLAUDE.md

# Pattern source: Phase 10 RED scaffold
@.planning/phases/10-operator-launch-kit/10-00-PLAN.md
@.claude/skills/google-ad-research/scripts/tests/test_export_csv.py
@.claude/skills/google-ad-research/scripts/tests/test_render_report.py

# Files this plan EXTENDS (read first to understand existing structure)
@.claude/skills/google-ad-research/scripts/tests/test_run_init.py
@.claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py
@.claude/skills/google-ad-research/scripts/tests/test_merge_signals.py

# Pattern source for ad_group_match.py stub
@.claude/skills/google-ad-research/scripts/perf_synth.py
@.claude/skills/google-ad-research/scripts/export_csv.py

# Real-shape source for fixture authoring
@.runs/2026-05-08T081041Z-primary-urgent-care-car-accident-lake-worth/raw/google-ads-search-terms.json
@.runs/2026-05-08T081041Z-primary-urgent-care-car-accident-lake-worth/raw/google-ads-perf.json

<interfaces>
<!-- Wave 0 LOCKS these interfaces. Wave 1+2 implement against them. -->

ad_group_match.py module surface (Wave 0 stub — Wave 1 fills in):
```python
# /// script
# requires-python = ">=3.11"
# dependencies = []
# ///
_THRESHOLDS: dict[str, float] = {"high": 0.7, "medium": 0.4}
assert frozenset(_THRESHOLDS) == frozenset({"high", "medium"}), (
    "_THRESHOLDS drift — ADGM-03 taxonomy changed?"
)
_DEFAULT_INTENT_MISMATCH_MULTIPLIER: float = 0.5
_STOPWORDS: frozenset[str] = frozenset({
    "near", "me", "the", "a", "an", "of", "in", "on", "at",
    "to", "for", "and", "or", "with", "by", "from", "is", "are",
    "best", "top",
})

def main_with_args(argv: list[str]) -> int:
    raise NotImplementedError("ad_group_match.py — Wave 1 (plan 11-02) fills in")

def main() -> int:
    import sys
    return main_with_args(sys.argv[1:])
```

`ad-group-mapping.json` schema (locked at fixture authoring time — Wave 1 emits):
```json
{
  "matches": [
    {"keyword": "lake worth accident doctor", "existing_ad_group": "Accident Exams – Lake Worth",
     "confidence": "high", "score": 0.78, "reason": "jaccard=0.78 intent_match=true"}
  ],
  "unmapped_count": 4,
  "mapping_coverage_pct": 60.0,
  "computed_at": "2026-05-14T22:00:00Z",
  "skipped_reason": null
}
```

us-cities.json schema (Wave 0 ships subset fixture; Wave 1 plan 11-01 ships full reference data file):
```json
{"fl": {"lake worth": "palm beach", "boca raton": "palm beach",
        "west palm beach": "palm beach", "tampa": "hillsborough",
        "miami": "miami-dade", "jacksonville": "duval", "hollywood": "broward"},
 "tx": {"lake worth": "tarrant", "dallas": "dallas", "houston": "harris"},
 "ca": {"los angeles": "los angeles", "hollywood": "los angeles"}}
```

google-ads-search-terms-phase11.json shape (matches real run-folder Pitfall 1 verification — uses `ad_group_name` NOT `ad_group_id`):
```json
{"items": [
  {"search_term": "car accident doctor lake worth",
   "ad_group_name": "Accident Exams – Lake Worth",
   "campaign_name": "Primary Urgent Care", "clicks": 12, "impressions": 340,
   "cost_usd": 18.20},
  {"search_term": "sports injury clinic", "ad_group_name": "Sports Injury",
   "campaign_name": "Primary Urgent Care", "clicks": 4, "impressions": 95,
   "cost_usd": 6.40}
]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the two new RED test files (test_geo_filter.py + test_ad_group_match.py) and seven fixtures</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_geo_filter.py,
    .claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py,
    .claude/skills/google-ad-research/scripts/tests/fixtures/us-cities-subset.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-perf-phase11.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/google-ads-search-terms-phase11.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-50pct.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-60pct.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/ad-group-mapping-20pct.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/brief-with-geo-focus.md,
    .claude/skills/google-ad-research/scripts/tests/fixtures/brief-no-geo-focus.md
  </files>
  <behavior>
    test_geo_filter.py covers (all SKIPPED until Wave 1):
    - test_us_cities_loadable: us-cities-subset.json loads as JSON; us_cities["fl"]["lake worth"] == "palm beach"; us_cities["fl"]["boca raton"] == "palm beach"; us_cities["tx"]["lake worth"] == "tarrant"
    - test_keyword_kept_when_city_in_geo_focus: keyword "lake worth chiropractor" with state=fl, geo_focus=["Palm Beach County"] → NOT dropped (Lake Worth's county IS in focus; Pitfall 5)
    - test_keyword_kept_when_city_county_in_geo_focus: keyword "boca raton dentist" with state=fl, geo_focus=["Palm Beach County"] → NOT dropped (Boca Raton's county IS Palm Beach; Pitfall 5 hierarchy)
    - test_keyword_dropped_when_other_state_city: keyword "tampa pain clinic" with state=fl, geo_focus=["Palm Beach County"] → DROPPED (Tampa is in Hillsborough, not Palm Beach)
    - test_keyword_kept_when_no_geo_focus: keyword "tampa pain clinic" with geo_focus=[] → NOT dropped (backward compat — empty geo_focus disables city filter)
    - test_state_disambiguation: keyword containing "lake worth" with state=fl and geo_focus=["Palm Beach County"] → KEPT; same keyword with state=tx and geo_focus=["Tarrant County"] → also KEPT; Tampa stays dropped only in FL run (Pitfall 4 homonym)
    - test_stopword_safety: Geographic filter MUST NOT use stopword list (Geographic filter is independent of similarity tokenization)

    test_ad_group_match.py covers (all SKIPPED until Wave 1):
    - test_module_imports: `import ad_group_match` succeeds (stub exists from Task 3)
    - test_phase8_absent_graceful_skip: missing raw/google-ads-perf.json → exit 0 with `ad-group-mapping.json` containing matches=[], unmapped_count=N (total ranked rows), mapping_coverage_pct=0.0, skipped_reason="phase8_artifacts_absent"
    - test_similarity_math_exact_intent: jaccard({"lake","worth","accident","doctor"}, {"lake","worth","accident","doctor"}) * 1.0 = 1.0 (same intent match)
    - test_similarity_math_intent_mismatch: jaccard(...)=0.6 * 0.5 = 0.3 (intent mismatch halves)
    - test_stopword_filter_active: token set for "doctor near me lake worth" excludes "near"/"me" — only {"doctor","lake","worth"}
    - test_confidence_tier_high: score=0.75 → "high"
    - test_confidence_tier_medium_boundary: score=0.4 exactly → "medium"; score=0.39999 → "low"
    - test_confidence_tier_high_boundary: score=0.7 exactly → "high"; score=0.69999 → "medium"
    - test_confidence_tier_low: score=0.2 → "low"
    - test_mapping_shape_keys: ad-group-mapping.json has keys {matches, unmapped_count, mapping_coverage_pct, computed_at}; matches[0] has {keyword, existing_ad_group, confidence, score, reason}
    - test_coverage_pct_high_plus_medium_only: 6 high + 2 medium + 2 low out of 10 ranked → coverage_pct = 80.0 (not 100.0; low excluded per Pitfall 7)
    - test_disabled_ad_groups_skipped: ad_groups[] with status="REMOVED" → not surfaced as a match candidate
    - test_token_bag_keyed_by_ad_group_name: search_terms.json missing ad_group_id is fine; bucketing key is ad_group_name (Pitfall 1)
    - test_unicode_dashes_preserved: existing_ad_group "Accident Exams – Lake Worth" (U+2013) round-trips byte-for-byte through mapping (Pitfall 2)

    Both test files MUST use the Phase 10 hasattr-guard pattern:
    ```python
    try:
        import ad_group_match
        MODULE_INCOMPLETE = not hasattr(ad_group_match, "build_mapping")
    except ImportError:
        MODULE_INCOMPLETE = True
    pytestmark = pytest.mark.skipif(
        MODULE_INCOMPLETE, reason="ad_group_match.py incomplete — Wave 1 plan 11-02"
    )
    ```
    For test_geo_filter.py, guard on `merge_signals` having a new `_keyword_drifts_city` function or the constant `_US_CITIES_DATA_PATH`:
    ```python
    try:
        import merge_signals
        GEO_FILTER_INCOMPLETE = not hasattr(merge_signals, "_keyword_drifts_city")
    except ImportError:
        GEO_FILTER_INCOMPLETE = True
    pytestmark = pytest.mark.skipif(
        GEO_FILTER_INCOMPLETE, reason="merge_signals city filter — Wave 1 plan 11-01"
    )
    ```
  </behavior>
  <action>
    1. Author `test_geo_filter.py` with all 7 tests above. Use `pytestmark = pytest.mark.skipif(GEO_FILTER_INCOMPLETE, ...)` at module top. Test functions reference `merge_signals._keyword_drifts_city(text, city_filter)` and `merge_signals._build_city_filter(state, geo_focus, us_cities)` — both Wave 1 deliverables. Use `Path(__file__).parent / "fixtures" / "us-cities-subset.json"` to load the fixture inside each test (not at module level, so collection succeeds even if fixture missing — though Task 1 SHOULD create it).

    2. Author `test_ad_group_match.py` with all 14 tests above. Use the hasattr guard on `ad_group_match.build_mapping`. Reference these Wave 1 functions: `build_mapping(ranked, perf, search_terms)`, `_jaccard(a, b)`, `_tokens(text)`, `_classify(score)`, `_intent_match_multiplier(intent_a, intent_b)`. Use `tmp_path` from pytest for run_dir; write fixture JSONs into `run_dir/raw/` for the graceful-skip + happy-path cases.

    3. Author `fixtures/us-cities-subset.json`:
       ```json
       {"fl": {"lake worth": "palm beach", "boca raton": "palm beach",
               "west palm beach": "palm beach", "tampa": "hillsborough",
               "jacksonville": "duval", "miami": "miami-dade",
               "hollywood": "broward"},
        "tx": {"lake worth": "tarrant", "dallas": "dallas", "houston": "harris"},
        "ca": {"los angeles": "los angeles", "hollywood": "los angeles"}}
       ```
       (counties stored as VALUES — lowercase, no "county" suffix. Filter logic in Wave 1 strips "county" suffix from geo_focus before lookup.)

    4. Author `fixtures/google-ads-perf-phase11.json` matching real Phase 8 shape (read the .runs/2026-05-08T081041Z-... file first to verify keys). MUST contain `ad_groups[]` with 4 entries: 3 ENABLED ("Accident Exams – Lake Worth" with en-dash U+2013, "Sports Injury", "Car Injury Care"), 1 REMOVED ("Old Legacy Ad Group"). Each ENABLED entry: `{ad_group_id, name, status, campaign_name, cost_usd, clicks, impressions}`. Use Unicode en-dash literally in the name (not escaped).

    5. Author `fixtures/google-ads-search-terms-phase11.json` matching real shape: `{"items": [...]}` where each item has `{search_term, ad_group_name, campaign_name, clicks, impressions, cost_usd}` — NO ad_group_id (Pitfall 1). Provide 8-10 items distributed across the 3 ENABLED ad groups (e.g., 4 items for "Accident Exams – Lake Worth", 3 for "Sports Injury", 2 for "Car Injury Care"). Search terms should overlap with ranked-enriched fixture vocabulary so similarity math has meaningful inputs.

    6. Author three `ad-group-mapping-*pct.json` fixtures for ADGM-06 boundary testing:
       - `ad-group-mapping-50pct.json` — 10 total ranked rows; 5 with confidence ∈ {high, medium} → coverage_pct = 50.0 EXACTLY (boundary — no rewrite per Pitfall 7 / open-question 4)
       - `ad-group-mapping-60pct.json` — 10 total; 6 high+medium → coverage_pct = 60.0 (triggers rewrite). Include at least 2 distinct existing_ad_group names so the Counter-grouped rewrite has something to display.
       - `ad-group-mapping-20pct.json` — 10 total; 2 high+medium, 8 low → coverage_pct = 20.0 (no rewrite — confirms negative path)
       All three: schema = {matches, unmapped_count, mapping_coverage_pct, computed_at, skipped_reason=null}.

    7. Author `fixtures/brief-with-geo-focus.md`:
       ```markdown
       # Campaign Brief
       ## Required
       - **Industry:** Urgent care
       - **Product:** Accident & injury care
       - **Location:** Florida
       - **Language:** en-US
       - **Audience:** Adults 25-65 post-accident
       ## Optional
       - **Geo focus:** Palm Beach County, Lake Worth
       ```

    8. Author `fixtures/brief-no-geo-focus.md` — identical structure minus the Geo focus line and minus the entire Optional section (backward compat baseline).

    Constraints:
    - DO NOT implement merge_signals._keyword_drifts_city or ad_group_match.build_mapping — those are Wave 1.
    - All test functions MUST be either skipped (via pytestmark) or use per-function hasattr guards if the test exercises a function that may land in a different Wave 1 plan than the module-level guard suggests.
    - Fixture JSONs MUST be valid JSON (no trailing commas) and use UTF-8 (en-dash literal preserved).
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify --with respx --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_geo_filter.py .claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py --collect-only -q</automated>
    Expected: 0 errors. 7 tests collected from test_geo_filter.py + 14 from test_ad_group_match.py = 21 tests total, all marked SKIP (modulewise skipif) once Task 3 ships ad_group_match.py stub.
  </verify>
  <done>
    Two new test files exist and pytest collects them without error. Seven new fixture files exist on disk and are valid JSON/markdown. All 21 new tests collected — all skipped (because Wave 1 functions absent). Zero legacy test regressions: full suite still passes existing GREEN tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend five existing test files with per-function hasattr-guarded Phase 11 RED stubs</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_run_init.py,
    .claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py,
    .claude/skills/google-ad-research/scripts/tests/test_merge_signals.py,
    .claude/skills/google-ad-research/scripts/tests/test_export_csv.py,
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  </files>
  <behavior>
    test_run_init.py adds (per-function hasattr guard — file already has GREEN Phase 1-8 tests):
    - test_geo_focus_persisted: stdin brief with `**Geo focus:** Palm Beach County, Lake Worth` → resulting brief.md contains the exact same line verbatim (GEO-01). Skip if `run_init` lacks a new `_parse_optional_geo_focus` helper OR if brief-with-geo-focus fixture absent. Guard: `_skip_unless_geo_focus_supported()`.
    - test_geo_focus_absent_backward_compat: brief without Geo focus line → brief.md does NOT contain `**Geo focus:**` (backward compat).

    test_serp_fetch.py adds:
    - test_geo_focus_appended_to_query: when invoked with `--geo-focus "Palm Beach County" "Lake Worth"`, the Serper POST body's `q` field contains both tokens appended ONCE per query (respx side_effect captures body). Skip if argparse `--geo-focus` arg absent. Guard: `_skip_unless_geo_focus_arg()`.
    - test_geo_focus_dedup_on_existing_token: when seed already textually contains "lake worth", token is NOT re-appended (Pitfall 8 — case-insensitive `if token.lower() not in q.lower()` guard).

    test_merge_signals.py adds:
    - test_city_filter_active: integration — given brief.md with `**Geo focus:** Palm Beach County`, state derived as fl, keyword "tampa pain clinic" is dropped from keywords.json output. Skip if merge_signals lacks `_keyword_drifts_city`. Guard: `_skip_unless_city_filter()`.
    - test_city_filter_inactive_when_geo_focus_empty: same brief minus Geo focus line → "tampa pain clinic" NOT dropped (backward compat).
    - test_city_filter_preserves_county_hierarchy: keyword "boca raton dentist" with geo_focus=["Palm Beach County"] → NOT dropped (Pitfall 5).

    test_export_csv.py adds:
    - test_existing_ad_group_in_positives: when ad-group-mapping-60pct.json present, positives.csv `Ad Group` column = "Accident Exams – Lake Worth" for matched-high rows; cluster slug for low/unmapped (ADGM-05). Skip if export_csv lacks a new helper `_resolve_ad_group_from_mapping` OR equivalent in `_build_positives_rows` signature change. Guard: `_skip_unless_mapping_aware()`.
    - test_ad_groups_csv_skips_existing: ad_groups.csv `Ad Group` column does NOT contain any existing-ad-group name from the mapping — only NEW cluster slugs (ADGM-05). Critical: prevents Editor duplicate-name errors.
    - test_no_mapping_file_backward_compat: when ad-group-mapping.json absent, positives.csv Ad Group = cluster slug (existing behavior preserved).
    - test_unicode_dash_preserved_in_csv: "Accident Exams – Lake Worth" round-trips byte-for-byte through CSV (Pitfall 2 verification at integration level).

    test_render_report.py adds:
    - test_geo_focus_section_rendered: brief with geo_focus → report.md contains `## Geographic Focus` section with "Florida → Palm Beach County, Lake Worth" line (GEO-05). Skip if render_report lacks `render_geographic_focus_section`. Guard: `_skip_unless_geo_section()`.
    - test_geo_focus_section_omitted_when_empty: brief without geo_focus → report.md does NOT contain `## Geographic Focus` heading.
    - test_next_steps_rewrite_high_coverage: ad-group-mapping-60pct.json present → step 3 in Next Steps reads "Add keywords to existing ad groups: ..." with each existing_ad_group name + keyword count (ADGM-06). Skip if render_report's Next Steps lacks mapping-awareness. Guard: `_skip_unless_next_steps_mapping_aware()`.
    - test_next_steps_no_rewrite_at_exactly_50pct: ad-group-mapping-50pct.json → step 3 reads ORIGINAL "Create ad groups: ..." (boundary case per Pitfall 7 / open-question 4 — strict `> 50.0`).
    - test_next_steps_no_rewrite_low_coverage: ad-group-mapping-20pct.json → step 3 reads ORIGINAL "Create ad groups: ...".
  </behavior>
  <action>
    1. Read existing test_run_init.py / test_serp_fetch.py / test_merge_signals.py / test_export_csv.py / test_render_report.py to understand the per-file conventions (helper-skip patterns, fixture loading, conftest usage).

    2. For each file, ADD a helper at the top (after imports) of the form:
       ```python
       def _skip_unless_geo_focus_supported():
           try:
               import run_init
               if not hasattr(run_init, "_parse_optional_geo_focus"):
                   pytest.skip("run_init geo focus support — Wave 1 plan 11-01")
           except ImportError:
               pytest.skip("run_init module incomplete")
       ```
       Each new test calls the helper at its start (mirrors test_render_report.py Phase 10 `_skip_unless_next_steps()` precedent).

    3. ADD the test functions described in <behavior>. Use existing fixtures from Task 1 (`fixtures/brief-with-geo-focus.md`, `fixtures/brief-no-geo-focus.md`, `fixtures/ad-group-mapping-60pct.json`, etc.).

    4. For test_serp_fetch.py, use the existing respx side_effect pattern (see Phase 2-04 decision in STATE.md — "respx side_effect pattern used to capture outgoing POST body").

    5. For test_merge_signals.py integration tests: write `keywords.json` (or whatever input merge_signals reads) into `tmp_path`, write `brief.md`, copy `us-cities-subset.json` into a location merge_signals can read (the new constant `_US_CITIES_DATA_PATH` in Wave 1 plan 11-01 will reference `references/us-cities.json` by default; tests must override via fixture monkeypatch or CLI flag — see decision-block below).

    6. For test_export_csv.py, drop ad-group-mapping fixtures into `tmp_run_dir/` (the existing tmp_run_dir fixture from conftest.py).

    Pre-implementation decision to document in this plan's task action:
    > Wave 1 (plan 11-01) MUST add a `--us-cities-path` CLI flag to merge_signals.py defaulting to `references/us-cities.json` so tests can monkeypatch a fixture path. Document this contract in Task 4 of plan 11-01.

    Pre-implementation decision to document:
    > Wave 1 (plan 11-02) reads `ad-group-mapping.json` from run_dir root. Wave 2 (plan 11-03) integrations: export_csv.py + render_report.py must look up `{run_dir}/ad-group-mapping.json` and tolerate absence (return None → fall back to existing behavior).

    Constraints:
    - DO NOT remove or modify any existing test function. ALL existing tests must remain GREEN.
    - All new tests use per-function hasattr guards (NOT module-level pytestmark) because these files host GREEN legacy tests (Phase 10 10-00 decision).
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify --with respx --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ -x --tb=short 2>&1 | tail -40</automated>
    Expected: All previously-GREEN tests still GREEN. New Phase 11 tests all SKIPPED. Total pass count = previous pass count. Total skip count increases by the new test counts (run_init: +2, serp_fetch: +2, merge_signals: +3, export_csv: +4, render_report: +5 = +16 skips).
  </verify>
  <done>
    Five existing test files extended with the new Phase 11 RED stubs. Per-function hasattr guards skip them cleanly. Zero legacy GREEN tests broken. New tests collected and ready to flip GREEN as Wave 1 and Wave 2 implementations land.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship ad_group_match.py MODULE_INCOMPLETE stub with locked _THRESHOLDS / _STOPWORDS constants</name>
  <files>.claude/skills/google-ad-research/scripts/ad_group_match.py</files>
  <behavior>
    Module imports cleanly. Constants frozen at import. Public API absent → tests skip.
    - `import ad_group_match` succeeds (no ImportError)
    - `ad_group_match._THRESHOLDS == {"high": 0.7, "medium": 0.4}` and frozenset assertion passes at import
    - `ad_group_match._STOPWORDS` is a frozenset containing "near", "me", "the", "best", "top" at minimum
    - `ad_group_match._DEFAULT_INTENT_MISMATCH_MULTIPLIER == 0.5`
    - `not hasattr(ad_group_match, "build_mapping")` is TRUE (Wave 1 fills in)
    - `not hasattr(ad_group_match, "_jaccard")` is TRUE
    - `not hasattr(ad_group_match, "_tokens")` is TRUE
    - `not hasattr(ad_group_match, "_classify")` is TRUE (the helper is renamed to a public-ish name in Wave 1 to ease test imports, OR remains private with the test importing it via `from ad_group_match import _classify` — Wave 1 plan 11-02 decides)
    - Running `python ad_group_match.py --run-dir /tmp` raises NotImplementedError with a message naming "Wave 1" and "plan 11-02"
  </behavior>
  <action>
    Create `.claude/skills/google-ad-research/scripts/ad_group_match.py` with this verbatim header (mirrors perf_synth.py and Phase 10 export_csv.py stub):

    ```python
    # /// script
    # requires-python = ">=3.11"
    # dependencies = []
    # ///
    """ad_group_match.py — Map ranked keywords to existing ad groups.

    Reads:
        {run_dir}/ranked-enriched.json (or ranked.json fallback)
        {run_dir}/raw/google-ads-perf.json
        {run_dir}/raw/google-ads-search-terms.json

    Writes:
        {run_dir}/ad-group-mapping.json — {matches[], unmapped_count, mapping_coverage_pct}

    CLI:
        uv run ad_group_match.py --run-dir <abs>

    Stdout (one JSON line on success):
        {"mapping_path": "...", "total_ranked": N, "matched_high": N,
         "matched_medium": N, "unmapped": N, "coverage_pct": float}

    Exit codes:
        0  ok (including silent skip — no perf.json present, ADGM-01)
        2  retryable (disk PermissionError / OSError)
        3  fatal (--run-dir missing / ranked.json unparseable)

    --- STUB (Wave 0 / Phase 11 plan 11-00) ---
    Wave 1 (plan 11-02) ships build_mapping(), _jaccard(), _tokens(), _classify().
    MODULE_INCOMPLETE = not hasattr(ad_group_match, "build_mapping")
    """
    from __future__ import annotations

    import sys

    # --- Locked taxonomy (ADGM-03; frozenset assertion fails fast at import) ---
    _THRESHOLDS: dict[str, float] = {
        "high": 0.7,
        "medium": 0.4,
    }
    assert frozenset(_THRESHOLDS) == frozenset({"high", "medium"}), (
        "_THRESHOLDS drift — ADGM-03 taxonomy changed?"
    )

    # --- Intent-mismatch multiplier (ADGM-02; configurable single source) ---
    _DEFAULT_INTENT_MISMATCH_MULTIPLIER: float = 0.5

    # --- Stopword filter (Pitfall 3; identical to Wave 1 implementation) ---
    _STOPWORDS: frozenset[str] = frozenset({
        "near", "me", "the", "a", "an", "of", "in", "on", "at",
        "to", "for", "and", "or", "with", "by", "from", "is", "are",
        "best", "top",
    })


    def main_with_args(argv: list[str]) -> int:  # pragma: no cover — Wave 1 fills in
        raise NotImplementedError(
            "ad_group_match.py is a Wave 0 stub. Wave 1 (plan 11-02) ships build_mapping(). "
            "See .planning/phases/11-account-structure-mapping/11-02-PLAN.md"
        )


    def main() -> int:  # pragma: no cover
        return main_with_args(sys.argv[1:])


    if __name__ == "__main__":  # pragma: no cover
        sys.exit(main())
    ```

    Do NOT add `build_mapping`, `_jaccard`, `_tokens`, `_classify`, or any helper that touches Phase 8 artifacts. Those are Wave 1 (plan 11-02).
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify pytest .claude/skills/google-ad-research/scripts/tests/test_ad_group_match.py::test_module_imports -x -v</automated>
    Expected: 1 test PASSES (test_module_imports runs without skip — module imports cleanly). All other tests in the file SKIP via MODULE_INCOMPLETE pytestmark.

    Secondary check (Python REPL via uv run):
    `uv run python -c "import sys; sys.path.insert(0, '.claude/skills/google-ad-research/scripts'); import ad_group_match; assert ad_group_match._THRESHOLDS == {'high': 0.7, 'medium': 0.4}; assert 'near' in ad_group_match._STOPWORDS; print('stub OK')"`
    Expected stdout: `stub OK`
  </verify>
  <done>
    ad_group_match.py exists. PEP 723 header present. _THRESHOLDS frozenset assertion fires at import. main_with_args raises NotImplementedError with a Wave-1-pointer message. test_module_imports PASSES; all other tests in test_ad_group_match.py SKIP.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the full suite:

```bash
uv run --with pytest --with python-dotenv --with python-slugify --with respx --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ -x
```

Expected outcome:
- All previously-GREEN tests REMAIN GREEN (zero legacy regressions)
- 1 new test PASSES (test_ad_group_match.py::test_module_imports)
- ~36 new tests SKIPPED (21 in two new files + 16 added to existing files - 1 module-imports passes = ~36 skipped)
- Zero test collection errors
- Total test count = previous total + (21 + 16) = previous + 37

Then verify the SKILL.md cap is still at 500 (this plan does NOT touch SKILL.md):

```bash
wc -l .claude/skills/google-ad-research/SKILL.md
```

Expected: `500`. If anything other than 500, an unintended SKILL.md edit happened — abort.
</verification>

<success_criteria>
- [ ] `scripts/tests/test_geo_filter.py` exists with 7 RED stubs, all SKIP-or-collect
- [ ] `scripts/tests/test_ad_group_match.py` exists with 14 RED stubs, all but `test_module_imports` SKIP
- [ ] 5 existing test files extended with 16 new per-function-guarded Phase 11 RED stubs
- [ ] 7 fixture files created and load as valid JSON/markdown
- [ ] `scripts/ad_group_match.py` exists with locked _THRESHOLDS + _STOPWORDS + NotImplementedError main
- [ ] Full suite runs clean: legacy tests GREEN, Phase 11 tests SKIPPED, 0 errors
- [ ] SKILL.md untouched (still 500 lines)
- [ ] Per-task commits land with `feat(phase-11):` / `test(phase-11):` prefixes
</success_criteria>

<output>
After completion, create `.planning/phases/11-account-structure-mapping/11-00-SUMMARY.md` describing:
- Files created (count + names)
- Tests added (count + module-level pass/skip rates)
- Locked interfaces (the _THRESHOLDS dict, the ad-group-mapping.json schema, the us-cities.json schema)
- Decision deltas (any deviation from RESEARCH/VALIDATION discovered during implementation)
- Wave 1 unblocked: plans 11-01 (geo plumbing) and 11-02 (ad_group_match core) ready to run in parallel
</output>
