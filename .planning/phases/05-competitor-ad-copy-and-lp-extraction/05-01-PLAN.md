---
phase: 05-competitor-ad-copy-and-lp-extraction
plan: "01"
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - .claude/skills/google-ad-research/scripts/competitor_intel.py
  - .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py
autonomous: true
requirements: [COMP-01, COMP-02]

must_haves:
  truths:
    - "competitor_intel.py loops over clusters.json, calls Serper once per cluster with the top-scored keyword"
    - "Affiliate ads (URL-param and domain-blocklist) are filtered out before output"
    - "Multiple ads from the same display-URL domain are deduplicated; highest position wins"
    - "Top 5 advertiser link URLs are passed to Tavily extract; raw_content written per URL"
    - "Empty ads block for a cluster writes ads=[] advertisers=[] without crashing"
    - "All 10 tests in test_competitor_intel.py pass GREEN"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/competitor_intel.py"
      provides: "Full Phase 5 orchestrator: Serper requery + affiliate filter + dedupe + Tavily LP extract"
      exports: ["main_with_args", "is_affiliate_url", "is_affiliate_domain", "dedupe_by_domain", "filter_ads"]
      min_lines: 120
    - path: ".claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py"
      provides: "10 GREEN tests (stubs filled with real assertions)"
  key_links:
    - from: "competitor_intel.py"
      to: "serp_fetch.fetch_seed + normalise_response"
      via: "sys.path.insert(0, ...) import pattern"
      pattern: "from serp_fetch import fetch_seed, normalise_response"
    - from: "competitor_intel.py"
      to: "tavily-python TavilyClient.extract()"
      via: "TavilyClient(api_key=...).extract(urls=[...])"
      pattern: "client\\.extract"
    - from: "competitor_intel.py"
      to: "lib/http.build_client()"
      via: "reuse RetryTransport for Serper httpx calls"
      pattern: "build_client"
    - from: "competitor_intel.py"
      to: ".runs/<ts>-<slug>/raw/competitor-intel.json"
      via: "lib/io.write_json pattern"
      pattern: "competitor-intel\\.json"
---

<objective>
Implement competitor_intel.py: per-cluster Serper requery, affiliate filtering, domain deduplication, and Tavily LP extraction. Fill in the 10 test stubs with real assertions and turn them GREEN.

Purpose: Satisfies COMP-01 (per-cluster Serper ads fetch) and COMP-02 (affiliate filter + domain dedup + cap).
Output: scripts/competitor_intel.py (full implementation), test_competitor_intel.py (GREEN).
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-competitor-ad-copy-and-lp-extraction/05-00-SUMMARY.md
@.claude/skills/google-ad-research/scripts/serp_fetch.py
@.claude/skills/google-ad-research/scripts/tavily_extract.py
@.claude/skills/google-ad-research/scripts/lib/http.py
@.claude/skills/google-ad-research/scripts/lib/config.py
@.claude/skills/google-ad-research/scripts/lib/io.py
@.claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py
@.claude/skills/google-ad-research/scripts/tests/conftest.py

<interfaces>
<!-- PEP 723 inline script metadata header (required — matches serp_fetch.py pattern):  -->
<!-- # /// script                                                                          -->
<!-- # requires-python = ">=3.11"                                                          -->
<!-- # dependencies = ["httpx>=0.28", "tavily-python>=0.7.24", "python-dotenv>=1.0",      -->
<!-- #                  "python-slugify>=8.0"]                                             -->
<!-- # ///                                                                                 -->

<!-- lib/http.build_client() signature (from http.py):                                    -->
<!--   def build_client(timeout: float = 30.0, retries: int = 3) -> httpx.Client          -->

<!-- lib/config.load_env() (from config.py):                                              -->
<!--   def load_env() -> None  — loads .env with override=False                           -->
<!--   def require(key: str) -> str  — raises ConfigError if key absent                   -->

<!-- serp_fetch exports:                                                                   -->
<!--   def fetch_seed(client, query, gl, hl, num, api_key) -> dict                        -->
<!--   def normalise_response(raw, seed, gl, hl) -> dict  — returns {"ads": [...], ...}   -->
<!--   ads entry shape: {title, link, snippet, displayUrl, position}                      -->

<!-- TavilyClient.extract() (tavily-python SDK):                                          -->
<!--   client = TavilyClient(api_key=str)                                                 -->
<!--   response = client.extract(urls=[str,...], extract_depth="basic",                   -->
<!--                              format="markdown", include_usage=True)                  -->
<!--   response shape: {"results":[{url,raw_content}],"failed_results":[{url,error}],     -->
<!--                    "usage":{extract_credits:N}}                                       -->

<!-- Output path: run_dir / "raw" / "competitor-intel.json"                               -->
<!-- CLI: --run-dir, --gl (default "uk"), --hl (default "en-GB")                          -->
<!-- Exit codes: 0=ok, 2=retryable/quota, 3=hard error (missing input file, bad key)      -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement competitor_intel.py</name>
  <files>.claude/skills/google-ad-research/scripts/competitor_intel.py</files>
  <behavior>
    - is_affiliate_url("https://quidco.com/grocery?ref=abc123") == True
    - is_affiliate_url("https://ocado.com/landing/same-day") == False
    - is_affiliate_url("https://example.com/aff/track") == True  (path fragment /aff/)
    - is_affiliate_domain("https://www.awin1.com/cread.php") == True
    - is_affiliate_domain("https://sub.awin.com/x") == True  (subdomain match)
    - is_affiliate_domain("https://www.tesco.com/delivery") == False
    - dedupe_by_domain([{displayUrl:"tesco.com",position:4,...},{displayUrl:"tesco.com",position:1,...}]) -> 1 entry with position=1
    - dedupe_by_domain([{displayUrl:null,link:"https://tesco.com/x",position:1}]) -> 1 entry (fallback to link netloc)
    - filter_ads(6-ad list with 2 affiliate) -> 4 entries (2 dropped)
    - main_with_args with empty ads block for a cluster -> that cluster has ads=[], advertisers=[] in output, exit 0
    - main_with_args with Tavily failed_result -> advertiser entry with extract_status="failed", raw_content=""
    - output JSON has top-level "metadata" and "clusters" keys
    - each cluster entry has: representative_keyword, serper_fetched_at, ads_raw_count, ads_filtered_count, ads, advertisers
  </behavior>
  <action>
Replace the ImportError stub with the full implementation. Follow these conventions EXACTLY:

1. **PEP 723 header** — copy the dependencies block from tavily_extract.py; add tavily-python if not already present.

2. **Module-level constants:**
   ```python
   MAX_ADVERTISERS = 5
   AFFILIATE_PARAMS = frozenset({"ref","aff_id","affiliate_id","partner_id","affid","aff_sub"})
   AFFILIATE_PATH_FRAGMENTS = ("/aff/", "/affiliate/", "/out/", "/track/")
   AFFILIATE_DOMAINS = frozenset({
       "awin.com","awin1.com","skimlinks.com","partnerize.com","viglink.com",
       "sovrn.com","rakuten.com","rakutenadvertising.com","tradedoubler.com",
       "cj.com","shareasale.com","impactradius.com","impact.com","pepperjam.com",
       "webgains.com","zanox.com","affiliatefuture.com","vouchercloud.com",
       "vouchercodes.co.uk","myvouchercodes.co.uk","hotukdeals.com",
       "topcashback.co.uk","quidco.com","cashbackkings.com",
   })
   ```

3. **Helper functions (all importable for tests):**
   - `extract_domain(url: str) -> str` — urlparse netloc, lowercase, removeprefix("www.")
   - `is_affiliate_url(url: str) -> bool` — check AFFILIATE_PARAMS in parse_qs keys + AFFILIATE_PATH_FRAGMENTS in path.lower()
   - `is_affiliate_domain(url: str) -> bool` — extract_domain check against AFFILIATE_DOMAINS exact + endswith("." + d)
   - `is_affiliate(ad: dict) -> bool` — is_affiliate_url(ad["link"]) OR is_affiliate_domain(ad.get("displayUrl") or ad["link"])
   - `dedupe_by_domain(ads: list[dict]) -> list[dict]` — sort by position ascending (missing pos = 99), keep first per domain key; fallback to link netloc when displayUrl absent/unparseable
   - `filter_ads(ads: list[dict]) -> tuple[list[dict], int]` — returns (clean_ads, filtered_count)

4. **main_with_args(argv):**
   - argparse: --run-dir (required), --gl (default "uk"), --hl (default "en-GB")
   - load_env(); serper_key = require("SERPER_API_KEY"); tavily_key = require("TAVILY_API_KEY")
   - Read run_dir / "clusters.json"; exit 3 if missing
   - Build httpx client via lib/http.build_client(); build TavilyClient
   - Loop over clusters, pick clusters["clusters"][i]["keywords"][0]["keyword"] as rep_kw
   - Call fetch_seed + normalise_response; record serper_fetched_at = datetime.utcnow().isoformat() + "Z"
   - filter_ads -> ads_clean; dedupe_by_domain -> ads_deduped; top_ads = ads_deduped[:MAX_ADVERTISERS]
   - Log per cluster: "{name}: {raw} ads raw, {filtered} filtered, {len(ads_deduped)} remaining" to stderr
   - Tavily extract on [a["link"] for a in top_ads if a.get("link")]; handle empty list (skip Tavily call)
   - Build cluster output dict matching the output schema from RESEARCH.md
   - Write output to run_dir / "raw" / "competitor-intel.json" via lib/io (or json.dumps + Path.write_text)
   - Print one JSON line to stdout: {"run_dir":..., "clusters_processed":N, "serper_credits_used":N, "tavily_credits_used":N}
   - Exit 0 on success; exit 2 on Tavily quota error; exit 3 on missing input or bad API key

5. **if __name__ == "__main__":** sys.exit(main_with_args(sys.argv[1:]))

Do NOT do LLM value-prop extraction in this script. raw_content is written as-is. LLM extraction is in SKILL.md Step 18 (Plan 05-02).
  </action>
  <verify>
    <automated>uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py -x -q</automated>
  </verify>
  <done>competitor_intel.py importable (no ImportError). is_affiliate_url, is_affiliate_domain, dedupe_by_domain, filter_ads all importable and unit-testable independently of Serper/Tavily APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Fill test stubs with real assertions — turn all 10 GREEN</name>
  <files>.claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py</files>
  <action>
Replace the `assert False, "RED — implement in Plan 05-01"` bodies with real test logic. Use respx to mock Serper and monkeypatch to mock TavilyClient.extract. Load fixtures from FIXTURES_DIR.

For each of the 10 test functions, implement assertions matching the behavior block in Task 1:

- **test_affiliate_url_param_filter**: import is_affiliate_url; assert True/False for the two fixture URLs.
- **test_affiliate_domain_blocklist**: import is_affiliate_domain; assert awin1.com True, tesco.com False.
- **test_affiliate_subdomain_blocked**: import is_affiliate_domain; assert sub.awin.com True.
- **test_dedupe_by_domain**: import dedupe_by_domain; build a 2-ad list with same domain at positions 4 and 1; assert len==1 and position==1.
- **test_advertiser_cap_enforcement**: build 7 unique-domain clean ads; run through filter_ads + dedupe_by_domain; slice [:MAX_ADVERTISERS]; assert len <= 5.
- **test_ads_fetched_per_cluster**: use respx to mock POST https://google.serper.dev/search returning serper_ads_raw fixture; monkeypatch TavilyClient.extract; call main_with_args with tmp_run_dir that has clusters_phase5.json copied into it; assert output competitor-intel.json exists and has 3 clusters.
- **test_empty_ads_block_ok**: mock Serper to return serper_ads_empty fixture; call main_with_args; assert the informational cluster in output has ads==[] and advertisers==[] and exit code is 0.
- **test_tavily_urls_built_from_top_ads**: from the serper_ads_raw fixture, run filter_ads + dedupe_by_domain + [:5]; assert the resulting URL list contains only "link" field values from non-affiliate ads.
- **test_tavily_failed_result_persisted**: monkeypatch TavilyClient.extract to return the tavily_lp_response fixture (which has 1 failed_result); assert the cluster's advertisers list contains an entry with extract_status=="failed" and raw_content=="".
- **test_output_schema_valid**: full integration via respx + monkeypatch; assert output JSON has "metadata" with "generated_at" key, and "clusters" dict where each value has "representative_keyword", "ads", "advertisers".

Use conftest.py's `tmp_run_dir` and `mock_env` fixtures. Copy fixture files into tmp_run_dir as needed using `shutil.copy` or writing the JSON directly.
  </action>
  <verify>
    <automated>uv run --with pytest --with respx pytest .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py -v</automated>
  </verify>
  <done>All 10 tests PASS. Full suite `uv run --with pytest --with respx pytest .claude/skills/google-ad-research/scripts/tests/ -q` is green.</done>
</task>

</tasks>

<verification>
uv run --with pytest --with respx pytest .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py -v
# 10 passed.

uv run --with pytest --with respx pytest .claude/skills/google-ad-research/scripts/tests/ -q
# All prior-phase tests + 10 new tests pass. 0 failures.
</verification>

<success_criteria>
- competitor_intel.py fully implements COMP-01 and COMP-02 behaviors
- is_affiliate_url, is_affiliate_domain, dedupe_by_domain, filter_ads importable without mocking
- All 10 tests GREEN; prior-phase suite unaffected
- Output competitor-intel.json written to run_dir/raw/ with correct schema
</success_criteria>

<output>
After completion, create `.planning/phases/05-competitor-ad-copy-and-lp-extraction/05-01-SUMMARY.md`
</output>
