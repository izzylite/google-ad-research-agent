---
phase: 05-competitor-ad-copy-and-lp-extraction
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase5.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_raw.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_empty.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/tavily_lp_response.json
  - .claude/skills/google-ad-research/scripts/competitor_intel.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "pytest --collect-only finds 10 test functions in test_competitor_intel.py"
    - "All 10 tests fail (RED) — competitor_intel.py is a MODULE_MISSING stub"
    - "All 4 fixture files are valid JSON and loadable by pytest"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py"
      provides: "10 RED test stubs covering COMP-01, COMP-02, COMP-03"
      min_lines: 80
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase5.json"
      provides: "3-cluster input (1 transactional, 1 commercial, 1 informational/empty-ads)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_raw.json"
      provides: "6-ad Serper response: 2 affiliate, 2 same-domain dupes, 2 clean"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_empty.json"
      provides: "Serper response with ads: []"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/tavily_lp_response.json"
      provides: "Tavily extract response: 3 ok results + 1 failed_result"
    - path: ".claude/skills/google-ad-research/scripts/competitor_intel.py"
      provides: "MODULE_MISSING stub — causes tests to be skipped until Wave 1"
  key_links:
    - from: "test_competitor_intel.py"
      to: "competitor_intel.py"
      via: "try/except ImportError MODULE_MISSING guard (matches serp_fetch.py pattern)"
      pattern: "MODULE_MISSING"
    - from: "test_competitor_intel.py"
      to: "tests/fixtures/*.json"
      via: "FIXTURES_DIR / filename"
      pattern: "FIXTURES_DIR"
---

<objective>
Create test infrastructure for Phase 5: 10 RED pytest stubs and 4 fixture files.
No production logic yet — competitor_intel.py is a MODULE_MISSING stub.

Purpose: Nyquist compliance requires tests exist (and fail) before implementation. Wave 1 turns them GREEN.
Output: test_competitor_intel.py (10 stubs), 4 fixture JSON files, competitor_intel.py stub.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/google-ad-research/scripts/tests/conftest.py
@.claude/skills/google-ad-research/scripts/tests/test_serp_fetch.py
@.claude/skills/google-ad-research/scripts/tests/fixtures/serper_search_uk.json
@.claude/skills/google-ad-research/scripts/tests/fixtures/tavily_extract_2urls.json

<interfaces>
<!-- Existing conftest.py fixtures available to all tests -->
<!-- SCRIPTS_DIR = Path(__file__).resolve().parent.parent injected into sys.path -->
<!-- tmp_run_dir(tmp_path) -> Path with raw/ subdir pre-created -->
<!-- mock_env(monkeypatch) -> sets SERPER_API_KEY + TAVILY_API_KEY -->

<!-- MODULE_MISSING guard pattern (from test_serp_fetch.py): -->
<!--   try: sys.path.insert(0, str(SCRIPTS_DIR)); import competitor_intel; MODULE_MISSING = False -->
<!--   except ImportError: MODULE_MISSING = True -->
<!--   pytestmark = pytest.mark.skipif(MODULE_MISSING, reason="competitor_intel.py not yet implemented") -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 4 fixture JSON files</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase5.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_raw.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/serper_ads_empty.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/tavily_lp_response.json
  </files>
  <action>
Create four fixture files:

**clusters_phase5.json** — 3-cluster clusters.json. First cluster: name="same_day_delivery_transactional", intent="transactional", keywords=[{"keyword":"order same day grocery delivery","score":95},{"keyword":"same day grocery delivery uk","score":88},{"keyword":"get groceries delivered today","score":82}]. Second cluster: name="grocery_brand_comparison_commercial", intent="commercial", keywords=[{"keyword":"best grocery delivery uk","score":75},{"keyword":"ocado vs tesco delivery","score":70},{"keyword":"grocery delivery comparison","score":65}]. Third cluster: name="delivery_how_informational", intent="informational", keywords=[{"keyword":"how does grocery delivery work","score":40},{"keyword":"what is same day delivery","score":35}]. Wrap in {"metadata":{"clustered_at":"2026-05-08T10:00:00Z","total_clusters":3,"total_keywords":8},"clusters":[...],"orphans":[]}.

**serper_ads_raw.json** — Serper /search response containing an "ads" array with exactly 6 entries:
- position 1: title="Same Day Grocery Delivery", link="https://www.ocado.com/landing/same-day", displayUrl="ocado.com/same-day" (clean)
- position 2: title="Grocery Deals & Cashback", link="https://www.quidco.com/grocery?ref=abc123", displayUrl="quidco.com/grocery" (affiliate — URL param ref=)
- position 3: title="Compare Grocery Delivery", link="https://www.awin1.com/cread.php?awinmid=123", displayUrl="awin1.com/grocery" (affiliate — domain blocklist)
- position 4: title="Tesco Grocery Delivery", link="https://www.tesco.com/groceries/delivery-promo", displayUrl="tesco.com/groceries" (clean)
- position 5: title="Tesco Same Day Offer", link="https://www.tesco.com/same-day-offer", displayUrl="tesco.com/same-day" (duplicate domain — tesco.com appears again at position 5; should be deduplicated, keeping position 4)
- position 6: title="Sainsbury's Delivery", link="https://www.sainsburys.co.uk/shop/gb/groceries/delivery", displayUrl="sainsburys.co.uk/delivery" (clean)
Wrap in standard Serper shape: {"searchParameters":{...},"ads":[...]} — no organic/PAA needed for this fixture.

**serper_ads_empty.json** — Minimal Serper response with ads: []. Shape: {"searchParameters":{"q":"how does grocery delivery work","gl":"uk","hl":"en-GB","num":10,"type":"search"},"ads":[]}.

**tavily_lp_response.json** — Tavily extract() response with "results" (3 ok) and "failed_results" (1 failed). Results: [{url:"https://www.ocado.com/landing/same-day", raw_content:"# Same Day Grocery Delivery\nOrder before 11am for delivery today. **Free delivery** on orders over £40.\n\n[Order Now](#order)"}, {url:"https://www.tesco.com/groceries/delivery-promo", raw_content:"# Groceries Delivered Today\nTesco same day delivery slots available now.\n\n[Book a Slot](#slot)"}, {url:"https://www.sainsburys.co.uk/shop/gb/groceries/delivery", raw_content:"# Sainsbury's Delivery\nFresh groceries to your door.\n\n[Shop Now](#shop)"}]. failed_results: [{url:"https://www.waitrose.com/landing/delivery", error:"extraction_failed"}]. Wrap in {"results":[...],"failed_results":[...],"usage":{"extract_credits":1}}.
  </action>
  <verify>
    <automated>uv run --with pytest python -c "import json; from pathlib import Path; [json.loads((Path('.claude/skills/google-ad-research/scripts/tests/fixtures') / f).read_text()) for f in ['clusters_phase5.json','serper_ads_raw.json','serper_ads_empty.json','tavily_lp_response.json']]; print('all fixtures valid JSON')"</automated>
  </verify>
  <done>4 fixture files exist and parse as valid JSON. serper_ads_raw.json has 6 ads including 2 affiliate entries and 1 domain duplicate. tavily_lp_response.json has 3 results + 1 failed_result.</done>
</task>

<task type="auto">
  <name>Task 2: Write test stubs and competitor_intel.py MODULE_MISSING stub</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py
    .claude/skills/google-ad-research/scripts/competitor_intel.py
  </files>
  <action>
**competitor_intel.py** — MODULE_MISSING stub, identical pattern to serp_fetch.py and tavily_extract.py. File contains only:
```python
# competitor_intel.py — Phase 5 orchestrator
# Implementation lands in Phase 5 Plan 01.
raise ImportError("competitor_intel not yet implemented")
```

**test_competitor_intel.py** — 10 RED stubs. Follow the exact MODULE_MISSING guard pattern from test_serp_fetch.py. File structure:

```
"""Tests for competitor_intel.py — Phase 5 competitor ad copy + LP extraction."""
import json, sys
from pathlib import Path
import pytest

try:
    sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
    import competitor_intel
    MODULE_MISSING = False
except ImportError:
    MODULE_MISSING = True

pytestmark = pytest.mark.skipif(MODULE_MISSING, reason="competitor_intel.py not yet implemented")

FIXTURES_DIR = Path(__file__).parent / "fixtures"
```

Then 10 test functions (all bodies are `pytest.fail("RED — not yet implemented")` or `assert False, "RED"`):

1. `test_ads_fetched_per_cluster` — COMP-01: loads clusters_phase5.json, asserts that for each cluster a Serper call would be made with the first keyword as query. Stub body only.

2. `test_empty_ads_block_ok` — COMP-01: loads serper_ads_empty.json, asserts that an empty ads block produces "ads":[] and "advertisers":[] in output without raising SystemExit. Stub body only.

3. `test_affiliate_url_param_filter` — COMP-02: import is_affiliate_url from competitor_intel. Assert is_affiliate_url("https://quidco.com/grocery?ref=abc123") is True. Assert is_affiliate_url("https://www.ocado.com/landing/same-day") is False. Stub body only.

4. `test_affiliate_domain_blocklist` — COMP-02: import is_affiliate_domain. Assert is_affiliate_domain("https://www.awin1.com/cread.php") is True. Assert is_affiliate_domain("https://www.tesco.com/delivery") is False. Stub body only.

5. `test_affiliate_subdomain_blocked` — COMP-02: import is_affiliate_domain. Assert is_affiliate_domain("https://sub.awin.com/track") is True. Stub body only.

6. `test_dedupe_by_domain` — COMP-02: import dedupe_by_domain. Pass list of ads where tesco.com appears at position 1 and position 4. Assert result has exactly 1 tesco.com entry and it has position=1. Stub body only.

7. `test_advertiser_cap_enforcement` — COMP-02: import dedupe_by_domain and filter helpers. Pass 7 clean, unique-domain ads. Assert output has at most 5 entries. Stub body only.

8. `test_tavily_urls_built_from_top_ads` — COMP-03: import build_advertiser_urls (or equivalent). Given top 4 deduped ads, assert result is a list of 4 URL strings taken from the "link" field. Stub body only.

9. `test_tavily_failed_result_persisted` — COMP-03: loads tavily_lp_response.json. Assert that processing the failed_results list produces an advertiser entry with extract_status="failed" and raw_content="". Stub body only.

10. `test_output_schema_valid` — COMP-03 integration: loads all fixtures, mocks Serper + Tavily, runs competitor_intel main logic, reads output competitor-intel.json. Assert output has "metadata" key and "clusters" key. Each cluster entry has "representative_keyword", "ads", "advertisers" keys. Stub body only.

All 10 test bodies: replace with `assert False, "RED — implement in Plan 05-01"` (do NOT write actual assertions — those go in Wave 1 when the module exists).
  </action>
  <verify>
    <automated>uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py --collect-only -q</automated>
  </verify>
  <done>--collect-only reports exactly 10 test items in test_competitor_intel.py. Running the suite shows all 10 skipped (MODULE_MISSING=True because competitor_intel.py raises ImportError). Full suite `uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ -q` still passes all prior-phase tests.</done>
</task>

</tasks>

<verification>
uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py --collect-only -q
# Must show 10 collected items.

uv run --with pytest pytest .claude/skills/google-ad-research/scripts/tests/ -q
# Must show prior-phase tests still passing; new tests skipped (not erroring).
</verification>

<success_criteria>
- 4 fixture files exist and are valid JSON
- test_competitor_intel.py has 10 test functions collected by pytest
- All 10 tests skip cleanly (MODULE_MISSING guard active) — no import errors, no syntax errors
- Prior-phase test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/05-competitor-ad-copy-and-lp-extraction/05-00-SUMMARY.md`
</output>
