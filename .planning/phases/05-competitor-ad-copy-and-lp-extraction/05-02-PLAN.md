---
phase: 05-competitor-ad-copy-and-lp-extraction
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
autonomous: true
requirements: [COMP-03]

must_haves:
  truths:
    - "SKILL.md Step 17 no longer tells the operator to stop — it chains into Phase 5"
    - "SKILL.md Step 18 invokes competitor_intel.py and surfaces credits used to operator"
    - "SKILL.md Step 19 instructs the LLM to extract headline/CTA/offer from raw_content per advertiser"
    - "SKILL.md remains under 500 lines after the addition"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Steps 18-19 for Phase 5 competitor intel + value-prop extraction"
      contains: "competitor_intel.py"
  key_links:
    - from: "SKILL.md Step 18"
      to: "scripts/competitor_intel.py"
      via: "uv run ${CLAUDE_SKILL_DIR}/scripts/competitor_intel.py --run-dir {run_dir}"
      pattern: "competitor_intel\\.py"
    - from: "SKILL.md Step 19"
      to: "raw/competitor-intel.json"
      via: "Read tool + LLM extraction loop over clusters[*].advertisers[*].raw_content"
      pattern: "raw_content"
---

<objective>
Wire Phase 5 into SKILL.md: update Step 17 stop gate, add Step 18 (invoke competitor_intel.py), add Step 19 (LLM extracts headline/CTA/offer from raw_content). Satisfies COMP-03.

Purpose: The skill currently stops at Step 17. Steps 18-19 unlock Phase 5 execution for operators.
Output: Updated SKILL.md with Steps 18-19 and a Phase 5 section header, line count verified under 500.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-competitor-ad-copy-and-lp-extraction/05-01-SUMMARY.md
@.claude/skills/google-ad-research/SKILL.md

<interfaces>
<!-- competitor_intel.py stdout (from Plan 05-01):                                        -->
<!-- {"run_dir":str,"clusters_processed":N,"serper_credits_used":N,"tavily_credits_used":N} -->

<!-- competitor-intel.json schema (from RESEARCH.md):                                     -->
<!-- {                                                                                     -->
<!--   "metadata": {"generated_at":str,"serper_credits_used":N,"tavily_credits_used":N},  -->
<!--   "clusters": {                                                                       -->
<!--     "<cluster_name>": {                                                               -->
<!--       "representative_keyword": str,                                                  -->
<!--       "ads_raw_count": N, "ads_filtered_count": N,                                   -->
<!--       "ads": [{title,description,domain,displayed_url,link,position}],               -->
<!--       "advertisers": [{domain,url,raw_content,tavily_fetched_at,extract_status}]     -->
<!--     }                                                                                 -->
<!--   }                                                                                   -->
<!-- }                                                                                     -->

<!-- LLM extraction target fields (per advertiser where extract_status=="ok"):             -->
<!-- headline: most prominent H1 or first bold phrase, <=10 words                         -->
<!-- cta: primary call-to-action button text or imperative phrase                         -->
<!-- offer: discount/free trial/delivery offer/price claim, or null if none               -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Step 17 and add Phase 5 Steps 18-19 to SKILL.md</name>
  <files>.claude/skills/google-ad-research/SKILL.md</files>
  <action>
Read SKILL.md. Make the following targeted edits:

**Edit 1 — Step 17 stop gate:** Find the paragraph that says "Phase 5 (competitor intel) is not yet available in this skill." and "**STOP. Do not proceed to any Phase 5+ activity.**". Replace with:

> "Phase 5 (competitor intel) begins at Step 18 below."

Remove the STOP line entirely.

**Edit 2 — Append Phase 5 section** after the closing line of Phase 4 (after the old STOP). Add the following verbatim block:

```
---

## Phase 5: Competitor Ad Copy and Landing Page Extraction

> Prerequisites: Phase 4 complete — `{run_dir}/clusters.json` exists and validator exited 0.

### Step 18: Run competitor_intel.py (COMP-01, COMP-02)

Derive locale parameters from brief (same as Step 8):
- `gl` = lowercased country code from Location field
- `hl` = language tag from Language field

Run the competitor intel script:
```bash
uv run "${CLAUDE_SKILL_DIR}/scripts/competitor_intel.py" \
  --run-dir "{run_dir}" \
  --gl {gl} \
  --hl {hl}
```

Parse stdout JSON. Surface to operator:
> "Phase 5 signal collection:
> - Clusters processed: {clusters_processed}
> - Serper credits used: {serper_credits_used} (~${serper_credits_used * 0.001:.3f})
> - Tavily credits used: {tavily_credits_used} (~${tavily_credits_used * 0.005:.3f})
> - Competitor intel: `{run_dir}/raw/competitor-intel.json`"

Exit code handling:
- **Exit 0:** continue to Step 19.
- **Exit 2 (Tavily quota):** Warn operator "Tavily quota reached — partial LP data available. Proceed to Step 19 with available data? (y/n)". Continue only if yes.
- **Exit 3:** Surface error from stderr. Do NOT proceed.

**Do not advance to Step 19 until competitor-intel.json exists in `{run_dir}/raw/`.**

### Step 19: Extract headline, CTA, and offer from landing pages (COMP-03)

Read `{run_dir}/raw/competitor-intel.json` using the Read tool.

For each cluster in `competitor-intel.json["clusters"]`:
  For each advertiser in the cluster's `advertisers` list where `extract_status == "ok"` and `raw_content` is non-empty:

    Read the advertiser's `raw_content`. Extract:
    - **headline**: the most prominent heading — the first H1 line (line starting with `# `) or, if absent, the first bold phrase (`**...**`). Truncate to ≤ 10 words. Do NOT invent; if no heading or bold phrase found, use `null`.
    - **cta**: the primary call-to-action — the first imperative verb phrase or button text found (e.g., in a Markdown link like `[Order Now](#order)` extract "Order Now"). If none found, use `null`.
    - **offer**: any discount, free trial, free delivery, or price claim found verbatim in the text (e.g., "Free delivery on orders over £40", "3 months free"). If none, use `null`.

Compile results per cluster into a `competitor_summary` list:

```json
[
  {
    "cluster": "<cluster_name>",
    "representative_keyword": "<keyword>",
    "advertisers": [
      {
        "domain": "<domain>",
        "headline": "<extracted or null>",
        "cta": "<extracted or null>",
        "offer": "<extracted or null>"
      }
    ]
  }
]
```

Skip clusters with no ok-status advertisers (log internally, do not surface).

**Do not advance to Step 20 until you have processed every cluster with ≥ 1 ok advertiser.**

### Step 20: Confirm Phase 5 complete and stop

Tell the operator:

> "Phase 5 complete. Competitor intel summary:
> - {N_clusters_with_ads} clusters had paid ads
> - {N_advertisers_extracted} advertiser landing pages successfully extracted
> - {N_advertisers_failed} advertiser pages failed extraction (JS-heavy or geo-blocked)
>
> Competitor-intel raw data: `{run_dir}/raw/competitor-intel.json`
>
> Phase 6 (report assembly) is not yet available in this skill."

**STOP. Do not proceed to any Phase 6+ activity.**
```

(End of appended block.)

After writing, count the lines. If SKILL.md exceeds 500 lines, extract the Phase 5 section to `.claude/skills/google-ad-research/references/phase5-competitor-intel.md` and replace the section body with:
```
> See `.claude/skills/google-ad-research/references/phase5-competitor-intel.md` for full step instructions. Load it with the Read tool when entering Phase 5.
```
This extraction is only needed if the 500-line limit is breached — check before deciding.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; lines = Path('.claude/skills/google-ad-research/SKILL.md').read_text().splitlines(); assert len(lines) <= 500, f'SKILL.md too long: {len(lines)} lines'; content = Path('.claude/skills/google-ad-research/SKILL.md').read_text(); assert 'competitor_intel.py' in content; assert 'Step 18' in content; assert 'Step 19' in content; assert 'raw_content' in content; print(f'OK: {len(lines)} lines, Steps 18-19 present')"</automated>
  </verify>
  <done>SKILL.md contains Step 18 (competitor_intel.py invocation) and Step 19 (LLM value-prop extraction from raw_content). Line count is under 500. Step 17 no longer terminates the workflow. Step 20 is the new stop gate.</done>
</task>

</tasks>

<verification>
python -c "
from pathlib import Path
sk = Path('.claude/skills/google-ad-research/SKILL.md').read_text()
lines = sk.splitlines()
assert len(lines) <= 500, f'{len(lines)} lines — too long'
assert 'Step 18' in sk and 'Step 19' in sk and 'Step 20' in sk
assert 'competitor_intel.py' in sk
assert 'raw_content' in sk
assert 'headline' in sk and 'cta' in sk and 'offer' in sk
assert 'Phase 6' in sk
print(f'SKILL.md OK: {len(lines)} lines')
"
</verification>

<success_criteria>
- SKILL.md has Steps 18, 19, 20 covering Phase 5
- Step 17 no longer contains a hard stop — it chains to Step 18
- competitor_intel.py invocation documented with correct CLI shape
- LLM extraction instructions for headline/cta/offer are explicit and unambiguous
- SKILL.md line count <= 500
</success_criteria>

<output>
After completion, create `.planning/phases/05-competitor-ad-copy-and-lp-extraction/05-02-SUMMARY.md`
</output>
