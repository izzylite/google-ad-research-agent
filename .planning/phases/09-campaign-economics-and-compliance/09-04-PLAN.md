---
phase: 09-campaign-economics-and-compliance
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - .claude/skills/google-ad-research/scripts/render_report.py
  - .claude/skills/google-ad-research/scripts/tests/test_render_report.py
autonomous: true
requirements:
  - BIDS-03
  - FRCS-04
  - FRCS-05
  - CMPL-03
  - CMPL-04
must_haves:
  truths:
    - "Enriched keyword table in report.md and report.html shows a 'Suggested CPC' column formatted as USD with cents ($X.XX); rows with null show '—' and inherit the no_cpc_data flag."
    - "report.md contains a 'Budget Forecast' section listing per-cluster and campaign-level daily click + spend bands plus a 'How this is calculated' subsection (FRCS-05)."
    - "When compliance-flags.json has matched_verticals[], a '⚠ Compliance Required' block renders ABOVE the Ranked Keywords table in report.md (blockquote with ⚠ prefix) and report.html (yellow-background section)."
    - "When matched_verticals is empty / file absent, the compliance block does NOT render and the report is otherwise unchanged."
    - "report.json gains a 'compliance' array (matched_verticals contents) and a 'forecast' object (forecast.json contents or {})."
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/render_report.py"
      provides: "Report renderer extended with Suggested CPC column + Budget Forecast section + Compliance warning + report.json forecast/compliance keys"
    - path: ".claude/skills/google-ad-research/scripts/tests/test_render_report.py"
      provides: "New assertions for column, section ordering, compliance block presence/absence, report.json keys"
  key_links:
    - from: "render_full_report"
      to: "forecast.json + compliance-flags.json"
      via: "Path.exists() auto-detect (mirroring Phase 7 niche-pulse + Phase 8 account-perf pattern)"
      pattern: "(forecast|compliance-flags)\\.json"
    - from: "sections list"
      to: "Compliance block index"
      via: "inserted before the Ranked Keywords section, before Niche Pulse if present"
      pattern: "Compliance Required.*Ranked Keywords"
    - from: "build_report_json"
      to: "report.json['compliance'] + report.json['forecast']"
      via: "kwarg pass-through"
      pattern: "compliance.*forecast"
---

<objective>
Extend `render_report.py` to surface Phase 9 outputs in `report.md`, `report.html`, and `report.json`. Three new render concerns:
1. Add a `Suggested CPC` column to the Volume-Enriched keyword table (BIDS-03; USD formatted).
2. Render a `Budget Forecast` section between Ad Group Clusters and Negatives (FRCS-04) with a `How this is calculated` subsection (FRCS-05).
3. Render a `⚠ Compliance Required` block immediately above the Ranked Keywords table when matched_verticals is non-empty (CMPL-03). Render nothing when clean.

Also extend `build_report_json` with `compliance` (array) and `forecast` (object) keys (CMPL-04). All Phase 9 sidecars are auto-detected via `Path.exists()` — render gracefully degrades when files absent. New tests are added to test_render_report.py.

Purpose: All Phase 9 data files (`suggested_max_cpc_micros` on ranked rows, `forecast.json`, `compliance-flags.json`) become operator-visible in the rendered report. Phase 10 reads the same data programmatically from report.json or the sidecar files.

Output: render_report.py grows by ~150-200 lines (helper functions for forecast + compliance sections, column addition to enriched table). test_render_report.py gains ~80-120 lines of new assertions.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-campaign-economics-and-compliance/09-RESEARCH.md
@.claude/skills/google-ad-research/scripts/render_report.py
@.claude/skills/google-ad-research/scripts/tests/test_render_report.py
@.claude/skills/google-ad-research/scripts/lib/io.py

<!-- Phase 9 sidecar artifacts produced by 09-01, 09-02, 09-03 -->
@.planning/phases/09-campaign-economics-and-compliance/09-01-SUMMARY.md
@.planning/phases/09-campaign-economics-and-compliance/09-02-SUMMARY.md
@.planning/phases/09-campaign-economics-and-compliance/09-03-SUMMARY.md

<interfaces>
<!-- New signatures and sidecar auto-detect — extend the existing render_report.py exports. -->

# Signature changes (additive kwargs only)
def render_full_report(
    ranked, clusters_data, competitor_intel, negatives, brief_text, run_dir,
    *,
    top_n=100,
    niche_pulse=None,
    account_perf=None,
    negatives_sync=None,
    forecast=None,        # NEW (FRCS-04, FRCS-05)
    compliance=None,      # NEW (CMPL-03)
) -> str: ...

def build_report_json(
    ranked, clusters_data, competitor_intel, negatives, brief_text, run_dir,
    *,
    niche_pulse=None, account_perf=None, negatives_sync=None,
    forecast=None,        # NEW
    compliance=None,      # NEW (CMPL-04)
) -> dict: ...

# New private helpers
def render_forecast_section(forecast: dict | None) -> str:
    """Return markdown for Budget Forecast section. Empty string when forecast is None / empty."""

def render_compliance_warning(compliance: dict | None) -> str:
    """Return markdown for ⚠ Compliance Required block. Empty string when matched_verticals is empty / compliance is None."""

# In main_with_args / CLI runner: add the two sidecar auto-detects
# (mirror lines ~1273-1297 — niche-pulse + account-perf + negatives-sync auto-detect)
</interfaces>

<!-- Render order (RESEARCH.md §"render_report.py extension points" — sections list) -->

Final section order in report.md (Phase 9 changes marked NEW):
1. Header + HOW_TO_READ
2. ⚠ Compliance Required (NEW — CMPL-03; only when matched_verticals non-empty)
3. Niche Pulse (existing)
4. Account Performance (existing)
5. Negatives Sync (existing)
6. Ad Group Clusters (existing)
7. Budget Forecast (NEW — FRCS-04, FRCS-05)
8. Negatives (existing)
9. Competitor Ad Copy (existing)
10. Ranked Keywords / Volume-Enriched Keywords (existing — NOW with Suggested CPC column, BIDS-03)

CRITICAL — CMPL-03 contract: compliance block MUST appear before the Ranked Keywords block. Verify with a markdown index lookup in test (`report_md.index("⚠ Compliance Required") < report_md.index("## Ranked Keywords")`).

<!-- USD conversion (Pitfall 8) — display-boundary helper -->

Add a small helper near the top of render_report.py (or extend the existing CPC display helper):

```python
def _micros_to_usd(micros: int | None) -> str:
    if micros is None:
        return "—"
    return f"${micros / 1_000_000:.2f}"
```

Use _micros_to_usd for both the existing `cpc_micros` column (verify it does this already — see render_report.py line ~323) AND the new `suggested_max_cpc_micros` column. Single function = consistent USD formatting across both columns.

<!-- Compliance block format — markdown body -->

When matched_verticals non-empty, build a blockquote section:

```markdown
> ## ⚠ Compliance Required
>
> This campaign matches **{count}** regulated vertical(s). Verify compliance before launching.
>
> **{vertical_name}**
> - Evidence tokens: `{token1}`, `{token2}`, ...
> - Matched keywords: {matched_keyword_count}
> - Verification: <{verification_url}>
> - Policy note: {policy_note}
>
> {repeat per vertical}
```

Use `lib.io.escape_md_cell` on policy_note and tokens to guard against pipe / quote injection.

<!-- HTML rendering -->

Mirror the existing `renderAccountPerf` / `renderNichePulse` JS pattern in the `<script>` block:
- Add `renderForecast(forecast)` JS function — appends an HTML section with a table of clusters and a "How this is calculated" panel. Background: `#ecfdf5` (light green; matches account-perf totals).
- Add `renderCompliance(compliance)` JS function — appends a `<section>` block at the top of the report body (before the keywords section). Background: `#fef3c7` (warning yellow; already used for pulse highlights).
- Both functions are no-ops when their data is empty.

Inject the two new JSON blobs into the HTML template:
```html
<script id="forecast-data" type="application/json">{forecast_json}</script>
<script id="compliance-data" type="application/json">{compliance_json}</script>
```
Mirror the existing `<script id="account-perf-data">` injection pattern (line ~880 in current render_report.py).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Suggested CPC column to enriched keyword table + report.json keys</name>
  <files>
    .claude/skills/google-ad-research/scripts/render_report.py
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  </files>
  <behavior>
    - `render_enriched_keyword_table(ranked)` produces a markdown table whose header row contains "Suggested CPC" as a new column (after "CPC" / before "Difficulty" — or at end; whichever ordering keeps the existing column block intact).
    - Rows with `suggested_max_cpc_micros` set render as `$0.30` (USD with cents).
    - Rows with `suggested_max_cpc_micros` null render as `—` (em-dash), regardless of `no_cpc_data` flag.
    - Existing columns (Keyword, Intent, Match Type, Score, Volume, CPC, KD/Difficulty, etc.) and existing tests for the enriched table remain green.
    - `build_report_json` now accepts `forecast=None, compliance=None` kwargs; returned dict has `forecast` key (object: forecast dict OR {}) and `compliance` key (array: matched_verticals OR []).
    - When forecast=None: report.json["forecast"] == {}.
    - When compliance=None: report.json["compliance"] == [].
    - When compliance={"matched_verticals": [{...}]}: report.json["compliance"] == [{...}] (the array, not the wrapping object).
  </behavior>
  <action>
1. Open render_report.py. Locate `render_enriched_keyword_table` (~line 317).
2. Add "Suggested CPC" to the headers list. Add formatted cell to each row via `_micros_to_usd(row.get("suggested_max_cpc_micros"))`. If `_micros_to_usd` doesn't exist yet, add it module-level near the other format helpers (search for `cpc_micros` to find where existing CPC USD formatting happens; extract the formula into a single helper).
3. Update the column-count / colspec if any markdown/HTML headers downstream count columns — search for hardcoded column counts and update.
4. Locate `build_report_json` (search for its def). Extend signature with `forecast=None, compliance=None` (keyword-only, after the existing sidecar kwargs).
5. In the returned dict, add:
   ```python
   "forecast": forecast or {},
   "compliance": (compliance or {}).get("matched_verticals", []) if isinstance(compliance, dict) else [],
   ```
   (CMPL-04: array, not object — extract matched_verticals out of the wrapper.)
6. Open test_render_report.py. Add tests:
   - `test_enriched_table_has_suggested_cpc_column` — load ranked with suggested_max_cpc_micros set; render; assert `"Suggested CPC"` substring in returned markdown.
   - `test_enriched_table_renders_dash_for_null_suggested_cpc` — row with `suggested_max_cpc_micros: null` renders `—`.
   - `test_enriched_table_renders_usd_format` — row with `suggested_max_cpc_micros: 300000` renders `$0.30` (regex match).
   - `test_report_json_forecast_empty_default` — call build_report_json without forecast kwarg → assert returned["forecast"] == {}.
   - `test_report_json_compliance_array` — call with compliance={"matched_verticals":[{"name":"medical","verification_url":"X","policy_note":"Y","evidence_tokens":[],"evidence_sources":{"brief":[],"keywords":[]},"matched_keyword_count":0}]} → assert returned["compliance"] is list, len==1, returned["compliance"][0]["name"]=="medical".
   - `test_report_json_compliance_empty_when_none` — call without compliance kwarg → assert returned["compliance"] == [].
   - `test_report_json_compliance_empty_when_matched_verticals_empty` — call with compliance={"matched_verticals":[]} → returned["compliance"] == [].
  </action>
  <verify>
    <automated>cd .claude/skills/google-ad-research/scripts && uv run --with pytest --with tabulate --with python-dotenv --with python-slugify pytest tests/test_render_report.py -x -v -k "suggested_cpc or compliance or forecast_empty" 2>&1 | tail -20</automated>
  </verify>
  <done>
    Enriched table renders Suggested CPC column with correct USD formatting; null renders as em-dash. report.json has `forecast` object key and `compliance` array key, both defaulting empty. Existing tests (e.g., Ahrefs columns, ranking, clusters) remain green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Budget Forecast section (FRCS-04, FRCS-05) to report.md + HTML</name>
  <files>
    .claude/skills/google-ad-research/scripts/render_report.py
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  </files>
  <behavior>
    - `render_forecast_section(forecast_dict)` returns a non-empty markdown string when forecast has at least one cluster, empty string otherwise.
    - The section heading is `## Budget Forecast`.
    - A per-cluster table renders columns: Cluster | Intent | Keywords (with vol) | Daily Clicks (low/mid/high) | Daily Spend USD (low/mid/high) | Monthly Spend Mid USD.
    - A "Campaign Totals" sub-line shows daily_spend_low/mid/high_usd + monthly_spend_mid_usd from forecast["campaign_totals"].
    - A `### How this is calculated` subsection lists:
      • the four intent CTR anchors (pulled from forecast["methodology"]["intent_ctrs"]),
      • the avg-CPC ratio (forecast["methodology"]["avg_cpc_ratio"]),
      • the band multipliers,
      • the methodology.notes disclaimer string verbatim.
    - render_full_report places the Budget Forecast section AFTER "Ad Group Clusters" and BEFORE "Negatives" (verified by index ordering in test).
    - render_report.py's CLI / main_with_args auto-loads `{run_dir}/forecast.json` via Path.exists() (mirroring niche-pulse / account-perf pattern) and passes the parsed dict to render_full_report.
  </behavior>
  <action>
1. Add `render_forecast_section(forecast: dict | None) -> str` near the other section helpers (e.g., near `render_account_perf_section`, line ~344). Body:
   - If forecast is None or not forecast.get("clusters"): return "".
   - Build header `## Budget Forecast\n\n`.
   - Build per-cluster table via `tabulate(rows, headers, tablefmt="github")`. Rows = `[name, intent, f"{kw_count} ({with_vol} with vol)", f"{low}/{mid}/{high}", f"${spend_low}/${spend_mid}/${spend_high}", f"${monthly_mid}"]` per cluster. Apply `lib.io.escape_md_cell` to cluster name.
   - Campaign totals line: `**Campaign Totals:** Daily {clicks_low}/{clicks_mid}/{clicks_high} clicks · ${spend_low}/${spend_mid}/${spend_high} daily spend · ${monthly_mid} monthly (mid)`.
   - Add `### How this is calculated\n\n` subsection. Format:
     ```
     These are directional estimates — not Google's official forecast.
     
     - **Clicks** = monthly search volume × intent-class CTR ÷ 30 days  
       CTR anchors: transactional {T}%, commercial {C}%, informational {I}%, navigational {N}% (pulled from forecast.methodology.intent_ctrs × 100).
     - **Spend** = clicks × (suggested max CPC × {avg_cpc_ratio}).
     - **Bands** = mid × {low_mult} (low) / × {mid_mult} (mid) / × {high_mult} (high).
     - {forecast.methodology.notes}
     ```
   - Return the assembled markdown string.

2. In `render_full_report`, insert `render_forecast_section(forecast)` into the sections list between the Ad Group Clusters section and the Negatives section. If the forecast helper returns "", nothing is appended (graceful degrade).

3. Locate render_report.py's CLI runner / main (the auto-detect block around lines 1273-1297). Add a `forecast` sidecar auto-detect block mirroring niche-pulse / account-perf:
   ```python
   forecast = None
   forecast_path = run_dir / "forecast.json"
   if forecast_path.exists():
       try:
           forecast = json.loads(forecast_path.read_text(encoding="utf-8"))
       except (json.JSONDecodeError, OSError):
           forecast = None
   ```
   Pass `forecast=forecast` to render_full_report and build_report_json.

4. HTML extension: inject `<script id="forecast-data" type="application/json">{forecast_json}</script>` near the existing niche/account-perf script blocks. Add a `renderForecast(forecastObj)` JS function in the existing script block (after `renderAccountPerf`):
   - Reads `#forecast-data` JSON.
   - Renders a `<section class="forecast-section">` with `background:#ecfdf5; padding:16px; border-radius:6px;` (inline style — mirror existing pulse-highlights styling).
   - Appends a per-cluster `<table>` and the methodology block.
   - No-op when forecast is empty/null.
   - Calls itself on DOMContentLoaded (mirror `renderAccountPerf()` invocation site).

5. Tests in test_render_report.py:
   - `test_forecast_section_in_report` — load fixture forecast.json (build a minimal one inline in the test); call render_full_report with forecast=that dict; assert `## Budget Forecast` substring present.
   - `test_forecast_section_position` — assert `report_md.index("## Ad Group Clusters") < report_md.index("## Budget Forecast") < report_md.index("## Negatives")` (use `.index` substring search).
   - `test_forecast_methodology_present` — assert `### How this is calculated` substring + presence of "transactional", "commercial", "informational", "navigational" CTR labels + `0.65` or `65%` or "avg" text + the disclaimer keyword "directional".
   - `test_no_forecast_section_when_data_absent` — call render_full_report with forecast=None; assert "Budget Forecast" NOT in returned markdown.
   - `test_report_json_forecast_populated` — call build_report_json with forecast={...}; assert returned["forecast"]["campaign_totals"]["daily_spend_mid_usd"] equals the input value.
  </action>
  <verify>
    <automated>cd .claude/skills/google-ad-research/scripts && uv run --with pytest --with tabulate --with python-dotenv --with python-slugify pytest tests/test_render_report.py -x -v -k "forecast" 2>&1 | tail -20</automated>
  </verify>
  <done>
    Budget Forecast section renders between Ad Group Clusters and Negatives. "How this is calculated" subsection names CTR anchors, avg-CPC ratio, band multipliers, and the disclaimer string. report.html has a renderForecast() JS function and forecast-data script block. When forecast.json absent, no section renders.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ⚠ Compliance Required block above Ranked Keywords (CMPL-03) + HTML warning section</name>
  <files>
    .claude/skills/google-ad-research/scripts/render_report.py
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  </files>
  <behavior>
    - `render_compliance_warning(compliance_dict)` returns a non-empty markdown blockquote when matched_verticals is non-empty; empty string otherwise.
    - Block starts with `> ## ⚠ Compliance Required` (markdown blockquote prefix).
    - Each matched vertical renders: name (bold), evidence_tokens (backtick-wrapped, comma-separated), matched_keyword_count, verification_url (as autolink `<url>`), policy_note.
    - All free-text cells (policy_note, evidence tokens) routed through `lib.io.escape_md_cell` to prevent pipe / quote injection.
    - render_full_report places the compliance block IMMEDIATELY AFTER the header/HOW_TO_READ and BEFORE any other section (verified by index: compliance index < Niche Pulse / Account Perf / Clusters / Ranked Keywords indices).
    - render_report.py's main auto-loads `{run_dir}/compliance-flags.json` via Path.exists() and passes the parsed dict.
    - HTML: warning-yellow `<section>` block (`background:#fef3c7`) renders at top of report body when compliance data has matches; hidden / not rendered otherwise.
  </behavior>
  <action>
1. Add `render_compliance_warning(compliance: dict | None) -> str` near the other section helpers. Body:
   - If compliance is None: return "".
   - matched = compliance.get("matched_verticals", []) or []
   - If not matched: return "".
   - Build header: `> ## ⚠ Compliance Required\n>\n> This campaign matches **{len(matched)}** regulated vertical(s). Verify compliance before launching.\n>\n`.
   - For each vertical in matched:
     - `> **{vertical['name'].title()}**\n`
     - `> - Evidence tokens: ` + ", ".join(f"`{escape_md_cell(t)}`" for t in vertical.get("evidence_tokens", [])) + `\n`
     - `> - Matched keywords: {vertical['matched_keyword_count']}\n`
     - `> - Verification: <{vertical['verification_url']}>\n`
     - `> - Policy note: {escape_md_cell(vertical.get('policy_note', ''))}\n`
     - `>\n` (blank quoted line between verticals)
   - Return assembled string.

2. In `render_full_report`, insert `render_compliance_warning(compliance)` into the sections list immediately after the header and HOW_TO_READ sections, BEFORE the niche-pulse / account-perf / clusters / ranked-keywords sections. If helper returns "", nothing appended.

3. CLI auto-detect: in main / runner, add `compliance` sidecar load (mirror forecast load from Task 2):
   ```python
   compliance = None
   compliance_path = run_dir / "compliance-flags.json"
   if compliance_path.exists():
       try:
           compliance = json.loads(compliance_path.read_text(encoding="utf-8"))
       except (json.JSONDecodeError, OSError):
           compliance = None
   ```
   Pass `compliance=compliance` to render_full_report and build_report_json.

4. HTML extension:
   - Inject `<script id="compliance-data" type="application/json">{compliance_json}</script>`.
   - Add `renderCompliance(complianceObj)` JS function:
     - If !complianceObj || !complianceObj.matched_verticals?.length: do nothing.
     - Build a `<section style="background:#fef3c7; border:1px solid #fbbf24; padding:16px; border-radius:6px; margin-bottom:24px;">` and prepend it to the report body (insertBefore the first existing section).
     - Inside: `<h2>⚠ Compliance Required</h2>`, one block per vertical with name, evidence tokens (as `<code>` chips), verification URL (as `<a target="_blank">`), and policy_note.
   - Wire renderCompliance() into the DOMContentLoaded chain alongside renderForecast().

5. Tests in test_render_report.py:
   - `test_compliance_block_renders_when_matched` — call render_full_report with compliance={"matched_verticals":[{"name":"medical","evidence_tokens":["clinic"],"evidence_sources":{"brief":["clinic"],"keywords":[]},"matched_keyword_count":3,"verification_url":"https://support.google.com/adspolicy/answer/176031","policy_note":"Healthcare verification..."}]}; assert "⚠ Compliance Required" substring in returned markdown; assert "medical" or "Medical" present; assert verification_url is in the output.
   - `test_compliance_block_position` — assert `report_md.index("⚠ Compliance Required") < report_md.index("## Ranked Keywords")` (CMPL-03 contract). Also assert it's before "## Ad Group Clusters" and any niche-pulse section.
   - `test_no_compliance_block_when_clean` — call render_full_report with compliance=None; assert "Compliance Required" NOT in returned markdown.
   - `test_no_compliance_block_when_empty_array` — call with compliance={"matched_verticals":[]}; assert "Compliance Required" NOT in returned markdown.
   - `test_compliance_block_escapes_policy_note` — pass policy_note containing `| pipe | injection`; assert the rendered policy_note in markdown has escaped pipes (or check via escape_md_cell expected output).
   - `test_report_json_compliance_array_from_real_flags` — pass a full compliance dict to build_report_json; assert returned["compliance"] is the matched_verticals array (CMPL-04).
  </action>
  <verify>
    <automated>cd .claude/skills/google-ad-research/scripts && uv run --with pytest --with tabulate --with python-dotenv --with python-slugify pytest tests/test_render_report.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    Compliance warning block renders when matched_verticals non-empty, with correct positioning above Ranked Keywords. No block renders on clean runs. report.json["compliance"] is the matched_verticals array (CMPL-04). Full test_render_report.py is GREEN; full v1.0 suite remains green.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_render_report.py -x -v` → all tests PASS (existing + new).
- `pytest .claude/skills/google-ad-research/scripts/tests/ -x` → full suite green.
- Smoke: pick an existing real run-folder under `.runs/` (or build one synthetically with ranked-enriched.json + clusters.json + forecast.json + compliance-flags.json) and run `uv run scripts/render_report.py --run-dir <path>`. Inspect:
  • report.md contains `## Budget Forecast` section between Ad Group Clusters and Negatives.
  • report.md contains Suggested CPC column in the enriched table.
  • report.md begins with the ⚠ block when compliance-flags.json has matches; report.md is unchanged-from-v1.0 baseline when compliance-flags.json has empty matched_verticals.
  • report.json has `forecast` object key and `compliance` array key.
- Line-count check: render_report.py grows by no more than ~250 lines (Pitfall 5: avoid choke-point bloat). If approaching that, extract helper logic.
</verification>

<success_criteria>
1. Suggested CPC column renders in the Volume-Enriched keyword table (BIDS-03) with USD formatting and em-dash fallback.
2. Budget Forecast section (FRCS-04) appears between Ad Group Clusters and Negatives, with the "How this is calculated" subsection naming CTR anchors, avg-CPC ratio, band multipliers, and the disclaimer (FRCS-05).
3. ⚠ Compliance Required block (CMPL-03) renders above all other sections (specifically above Ranked Keywords table) when matched_verticals is non-empty; nothing renders when empty/absent.
4. report.json gains `forecast` object and `compliance` array (CMPL-04) — Phase 10 will read both.
5. Auto-detect of forecast.json + compliance-flags.json via Path.exists() — no CLI flags required; missing files degrade gracefully.
6. Pre-existing test suite (Phases 1-8) remains green: no regressions in rendering of niche-pulse, account-perf, negatives-sync, or any other v1.0 section.
</success_criteria>

<output>
After completion, create `.planning/phases/09-campaign-economics-and-compliance/09-04-SUMMARY.md` with: render_report.py line-count delta, new sections / helpers committed, section-order proof (an actual rendered report.md snippet showing the compliance block before Ranked Keywords), report.json key additions, and test results (count of new + existing tests, all GREEN).
</output>
