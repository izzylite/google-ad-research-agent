---
phase: 09-campaign-economics-and-compliance
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/tests/test_bid_suggest.py
  - .claude/skills/google-ad-research/scripts/tests/test_forecast_budget.py
  - .claude/skills/google-ad-research/scripts/tests/test_compliance_check.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_with_cpc.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_no_cpc.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_partial_cpc.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase9.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/brief_medical.md
  - .claude/skills/google-ad-research/scripts/tests/fixtures/brief_neutral.md
  - .claude/skills/google-ad-research/references/compliance-verticals.json
autonomous: true
requirements:
  - BIDS-01
  - BIDS-02
  - BIDS-03
  - BIDS-04
  - FRCS-01
  - FRCS-02
  - FRCS-03
  - FRCS-04
  - FRCS-05
  - CMPL-01
  - CMPL-02
  - CMPL-03
  - CMPL-04
must_haves:
  truths:
    - "Three RED test files exist with MODULE_MISSING guards; collection passes, all tests skip pending Wave 1 implementation."
    - "Six fixture files cover the three input shapes (with-CPC, no-CPC, partial-CPC), the cluster mapping, and medical + neutral briefs."
    - "references/compliance-verticals.json contains five vertical entries (medical, legal, finance, gambling, crypto) each with name, tokens[], verification_url, policy_note."
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_bid_suggest.py"
      provides: "RED stubs for BIDS-01..04"
      contains: "MODULE_MISSING"
      min_lines: 80
    - path: ".claude/skills/google-ad-research/scripts/tests/test_forecast_budget.py"
      provides: "RED stubs for FRCS-01..05"
      contains: "MODULE_MISSING"
      min_lines: 80
    - path: ".claude/skills/google-ad-research/scripts/tests/test_compliance_check.py"
      provides: "RED stubs for CMPL-01..04"
      contains: "MODULE_MISSING"
      min_lines: 80
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/ranked_with_cpc.json"
      provides: "Ranked-enriched rows, all four intents represented, all rows have cpc_micros"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/ranked_no_cpc.json"
      provides: "Ranked rows with cpc_micros=null, forces cluster-median fallback path"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/ranked_partial_cpc.json"
      provides: "Mix of with/without cpc plus one orphan keyword (not in any cluster)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase9.json"
      provides: "clusters.json shape matching the ranked_*_cpc fixtures"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/brief_medical.md"
      provides: "Brief containing medical tokens (clinic, physician)"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/brief_neutral.md"
      provides: "Brief with no regulated tokens — must not trigger any vertical"
    - path: ".claude/skills/google-ad-research/references/compliance-verticals.json"
      provides: "Operator-editable token data for 5 verticals (CMPL-02)"
      contains: "medical"
  key_links:
    - from: "tests/test_bid_suggest.py"
      to: "bid_suggest module"
      via: "try/except ImportError → pytest.skip"
      pattern: "MODULE_MISSING"
    - from: "tests/test_compliance_check.py"
      to: "references/compliance-verticals.json"
      via: "fixture path resolution + schema assertions"
      pattern: "compliance-verticals\\.json"
---

<objective>
Stand up Wave 0 test scaffolding for Phase 9. Three RED pytest files (bid_suggest, forecast_budget, compliance_check) land with MODULE_MISSING guards so collection is clean. Six fixtures + the operator-editable `compliance-verticals.json` reference file are committed. This plan unblocks all three Wave 1 parallel plans (01, 02, 03).

Purpose: Lock the test contracts before implementation so Wave 1 can flip RED→GREEN with no contract drift. Ships `references/compliance-verticals.json` because CMPL-02 demands token lists be data not code — Wave 1's `compliance_check.py` reads this file directly.

Output: Three test files (each importing the not-yet-written module behind a guard), six fixtures (3 ranked variants + 1 clusters + 2 briefs), and the production reference JSON for vertical tokens.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-campaign-economics-and-compliance/09-RESEARCH.md
@.planning/phases/09-campaign-economics-and-compliance/09-VALIDATION.md
@.claude/skills/google-ad-research/scripts/tests/conftest.py
@.claude/skills/google-ad-research/scripts/volume_enrich.py
@.claude/skills/google-ad-research/scripts/render_report.py

<interfaces>
<!-- The not-yet-written Wave 1 modules. Tests must import these names verbatim. -->
<!-- Tests use try/except ImportError + pytestmark = pytest.mark.skipif(MODULE_MISSING, ...) -->

bid_suggest.py exports (Wave 1, plan 01):
- INTENT_MULTIPLIERS: dict[str, float]  # frozen config block (BIDS-04)
- compute_suggested_cpc(cpc_micros, intent, cluster_median_micros) -> tuple[int|None, bool]
- cluster_median_cpc(keyword_to_cluster, cluster_to_keywords, cluster_name) -> int|None
- enrich_with_bids(ranked_enriched: list[dict], clusters_data: dict) -> list[dict]
- main_with_args(argv: list[str]) -> int

forecast_budget.py exports (Wave 1, plan 02):
- INTENT_CTRS: dict[str, float]
- AVG_CPC_RATIO: float  # 0.65
- BAND_MULTIPLIERS: dict[str, float]  # low=0.5 mid=1.0 high=1.5
- compute_cluster_forecast(cluster, ranked_index) -> dict
- build_forecast(ranked_enriched, clusters_data, run_id) -> dict
- main_with_args(argv: list[str]) -> int

compliance_check.py exports (Wave 1, plan 03):
- COMPLIANCE_SCAN_TOP_N: int  # default 50
- load_verticals(reference_path: Path) -> list[dict]
- find_matches(text: str, tokens: list[str]) -> list[str]  # word-boundary, case-insensitive
- scan(brief_text, ranked_enriched, verticals, top_n) -> dict
- main_with_args(argv: list[str]) -> int

Existing fixture conventions (see tests/conftest.py + tests/fixtures/):
- Fixtures are plain JSON, loaded via Path(__file__).parent / "fixtures" / "<name>.json"
- Tests use tmp_run_dir fixture from conftest.py to get isolated run folder
- Ranked rows shape: {"keyword": str, "intent": str, "cpc_micros": int|null, "volume": int|null, "score": int, ...}
- Clusters shape: {"metadata": {...}, "clusters": [{"name": str, "intent": str, "keywords": [{"keyword": str, "score": int}]}], "orphans": []}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write three RED pytest files with MODULE_MISSING guards</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_bid_suggest.py
    .claude/skills/google-ad-research/scripts/tests/test_forecast_budget.py
    .claude/skills/google-ad-research/scripts/tests/test_compliance_check.py
  </files>
  <action>
Create three new pytest files. Each MUST follow the Phase 2-8 MODULE_MISSING pattern verbatim (see existing tests/test_volume_enrich.py and tests/test_perf_synth.py for the template) so that Wave 0 lands RED-not-error.

Header pattern for every file:
```python
import json
from pathlib import Path
import pytest

try:
    from <module> import <symbols>  # see <interfaces> block
    MODULE_MISSING = False
except ImportError:
    MODULE_MISSING = True

pytestmark = pytest.mark.skipif(MODULE_MISSING, reason="<module>.py not yet implemented")

FIXTURES = Path(__file__).parent / "fixtures"
```

**test_bid_suggest.py** — at least these test functions (BIDS-01..04):
- test_compute_suggested_cpc_transactional — cpc_micros=250000, intent="transactional" → 300000 (1.2× multiplier, stays in micros — never USD-convert in math; see RESEARCH Pitfall 8)
- test_compute_suggested_cpc_informational — verifies 0.4× multiplier
- test_all_four_intents_multiplied — parametrize over the four intents; assert each multiplier applied
- test_cluster_median_fallback — keyword has cpc_micros=null but cluster siblings have CPCs; assert median(siblings) × multiplier
- test_null_when_cluster_empty_cpc — all cluster siblings have null cpc → returns (None, True)
- test_orphan_returns_null — keyword not in any cluster (cluster_name=None) → returns (None, True) and is flagged no_cpc_data
- test_intent_multipliers_frozenset — assert frozenset(INTENT_MULTIPLIERS.keys()) == {"transactional","commercial","informational","navigational"} (BIDS-04 single-source-of-truth)
- test_enrich_with_bids_adds_field — load ranked_with_cpc.json + clusters_phase9.json; assert every row gains key "suggested_max_cpc_micros"
- test_enrich_with_bids_flags_no_cpc_data — load ranked_partial_cpc.json; assert rows missing CPC AND with empty cluster pool get a "no_cpc_data": true flag
- test_main_with_args_writes_file — invoke main_with_args(["--run-dir", str(tmp_run_dir)]) after seeding ranked-enriched.json + clusters.json; assert exit code 0 and file mutated additively (existing keys preserved)

**test_forecast_budget.py** — at least these (FRCS-01..05):
- test_intent_ctrs_frozenset — keys match 4-class rubric
- test_avg_cpc_ratio_constant — AVG_CPC_RATIO == 0.65
- test_band_multipliers — {"low":0.5,"mid":1.0,"high":1.5}
- test_click_estimates_use_intent_ctrs — fixture with known volume + intent; assert daily_clicks = volume × CTR / 30 (FRCS-02)
- test_band_arithmetic — assert spend_low = spend_mid × 0.5; spend_high = spend_mid × 1.5 (FRCS-03)
- test_forecast_json_schema — keys: metadata, methodology, clusters[], campaign_totals; per-cluster keys: name, intent, keyword_count, keywords_with_volume, daily_clicks_low/mid/high, daily_spend_low/mid/high_usd (FRCS-01)
- test_skips_keywords_without_volume_or_cpc — keyword with volume=null OR suggested_max_cpc=null contributes 0
- test_cluster_join_lowercase_strip — "Same-Day Delivery" in cluster matches "same-day delivery" in ranked (Pitfall 6)
- test_methodology_block_present — methodology.intent_ctrs and avg_cpc_ratio mirror the script constants exactly (FRCS-05)
- test_main_with_args_writes_forecast_json — invoke main_with_args; assert `{run_dir}/forecast.json` exists and parses

**test_compliance_check.py** — at least these (CMPL-01..04):
- test_word_boundary — find_matches("loaner mug for sale", ["loan"]) returns [] (Pitfall 3 — no false positive)
- test_word_boundary_positive — find_matches("personal loan rates", ["loan"]) returns ["loan"]
- test_case_insensitive — find_matches("CLINIC HOURS", ["clinic"]) returns ["clinic"]
- test_loads_from_json_reference — load_verticals(fixtures/compliance_verticals_phase9.json or references/compliance-verticals.json) returns list of 5 dicts; each dict has keys {name, tokens, verification_url, policy_note}
- test_scans_brief_and_keywords — brief_medical.md + ranked_with_cpc.json → at minimum medical vertical flagged with evidence_sources.brief non-empty (CMPL-01)
- test_neutral_brief_no_matches — brief_neutral.md + ranked_with_cpc.json → matched_verticals == [] (no false positives)
- test_scans_top_n_only — synthesize 100 ranked rows with regulated token at rank 80; with COMPLIANCE_SCAN_TOP_N=50, that token must NOT appear in evidence_sources.keywords
- test_verification_url_present_per_vertical — every matched vertical dict carries non-empty verification_url string (CMPL-05 contract for Phase 10)
- test_main_with_args_writes_compliance_flags — invoke main; assert `{run_dir}/compliance-flags.json` exists with metadata + matched_verticals[]
- test_emits_empty_array_on_no_match — neutral brief: file still written, matched_verticals = []

Each test_*.py file should be 80-150 lines.
  </action>
  <verify>
    <automated>uv run --with pytest --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_bid_suggest.py .claude/skills/google-ad-research/scripts/tests/test_forecast_budget.py .claude/skills/google-ad-research/scripts/tests/test_compliance_check.py -v 2>&1 | grep -E "(passed|skipped|error)"</automated>
  </verify>
  <done>
    All three test files import their respective modules behind try/except; MODULE_MISSING = True; pytestmark skipif fires; pytest collection succeeds with zero ERRORS; every test is SKIPPED (not failed, not errored). Three test files committed to repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write six fixtures + references/compliance-verticals.json</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_with_cpc.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_no_cpc.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/ranked_partial_cpc.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/clusters_phase9.json
    .claude/skills/google-ad-research/scripts/tests/fixtures/brief_medical.md
    .claude/skills/google-ad-research/scripts/tests/fixtures/brief_neutral.md
    .claude/skills/google-ad-research/references/compliance-verticals.json
  </files>
  <action>
Create the six fixtures + the production reference JSON. Shapes are dictated by RESEARCH.md §"Validation Architecture" Wave 0 Gaps and the cluster/ranked schemas already shipping in Phases 3-8 (see scripts/tests/fixtures/ranked_full.json + clusters_full.json for the existing shape).

**ranked_with_cpc.json** — 12 keywords, all four intents represented (3 transactional, 3 commercial, 3 informational, 3 navigational). Every row has cpc_micros (range 50000-450000 = $0.05-$0.45 in micros), volume populated (range 50-5000), keyword strings reflect a grocery-delivery campaign. Row shape:
```json
{
  "keyword": "same-day grocery delivery",
  "intent": "transactional",
  "match_type": "phrase",
  "theme": "delivery",
  "signal_count": 4,
  "source_diversity": 3,
  "sources": ["serper-organic","serper-paa","tavily-tesco"],
  "score": 71,
  "volume": 2400,
  "cpc_micros": 320000,
  "difficulty": 28,
  "parent_topic": "grocery delivery"
}
```

**ranked_no_cpc.json** — 10 keywords, all cpc_micros=null (forces cluster-median fallback to also be null → no_cpc_data flag path). volume still populated for some so forecast tests can still aggregate.

**ranked_partial_cpc.json** — 12 keywords: 6 with cpc_micros, 6 without. CRITICAL: include at least one keyword that does NOT appear in clusters_phase9.json (orphan path — RESEARCH Pitfall 1). Comment with `_comment` field at top is fine.

**clusters_phase9.json** — three clusters covering ranked_with_cpc.json keywords (e.g., same_day_delivery_transactional with 4 keywords, grocery_comparison_commercial with 4 keywords, grocery_delivery_basics_informational with 3 keywords). One cluster has all transactional rows with CPCs → median fallback path works. One cluster has all rows with cpc_micros=null in ranked_no_cpc.json → empty-pool path. Orphans array must contain the orphan keyword from ranked_partial_cpc.json. Match the schema from Phase 4 (see existing clusters_full.json):
```json
{
  "metadata": {"clustered_at": "2026-05-14T00:00:00Z", "method": "llm-driven", "model": "fixture", "ranked_input": "ranked-enriched.json", "total_keywords": 11, "total_clusters": 3},
  "clusters": [{"name": "...", "intent": "...", "keywords": [{"keyword":"...","score":71}]}],
  "orphans": ["orphan keyword from ranked_partial_cpc"]
}
```
IMPORTANT: cluster keyword strings should be exact-case but verify the lower+strip join in Wave 1 works — include one mixed-case keyword like "Same-Day Delivery" matching ranked row "same-day delivery" (Pitfall 6).

**brief_medical.md** — a minimal brief.md that includes the words "clinic", "physician", and "telehealth" naturally inside the Industry / Product / Audience fields. Mirror the brief.md shape from `tests/fixtures/brief_sample.md` if it exists, otherwise:
```markdown
# Campaign Brief

**Captured:** 2026-05-14 14:30 UTC

## Required

- **Industry:** telehealth and primary care
- **Product:** virtual clinic visits with a licensed physician
- **Location:** UK
- **Language:** en-GB
- **Audience:** working professionals 25-55

## Raw operator paste

> launch ads for online clinic appointments with telehealth physicians
```

**brief_neutral.md** — kitchenware brief that MUST NOT trigger any vertical. Avoid words "loan", "credit", "medical", "clinic", "casino", "crypto", "lawyer", "attorney". Example:
```markdown
# Campaign Brief

**Captured:** 2026-05-14 14:30 UTC

## Required

- **Industry:** premium kitchenware retail
- **Product:** non-stick frying pans and cookware sets
- **Location:** US
- **Language:** en-US
- **Audience:** home cooks 30-60

## Raw operator paste

> launch ads for our copper-bottom cookware line
```
Sanity check: re-read brief_neutral.md and search for any token from the five verticals — there must be ZERO matches.

**references/compliance-verticals.json** — production data file (CMPL-02). Five entries (medical, legal, finance, gambling, crypto) using the structure and tokens documented in RESEARCH.md §"compliance-verticals.json structure". Copy that JSON verbatim into the file. Verification URLs must be the stable Google Ads Policy Help Center URLs listed in the research (answer/176031, /2464998, /176019, /9870661). Include `$schema_version: "v1"` and `generated_at` keys at the top level. Total file should be valid JSON loaded by json.loads().

Note: the planner did not request a separate test-isolated copy (`compliance_verticals_phase9.json`) because Wave 1 tests can point load_verticals at the real references file (it's stable test data; operator-editable). If tests need isolation later, Wave 1 implementation can choose to copy at test time.
  </action>
  <verify>
    <automated>uv run --with pytest python -c "import json; from pathlib import Path; root = Path('.claude/skills/google-ad-research'); files = ['scripts/tests/fixtures/ranked_with_cpc.json','scripts/tests/fixtures/ranked_no_cpc.json','scripts/tests/fixtures/ranked_partial_cpc.json','scripts/tests/fixtures/clusters_phase9.json','references/compliance-verticals.json']; [json.loads((root/f).read_text(encoding='utf-8')) for f in files]; assert (root/'scripts/tests/fixtures/brief_medical.md').read_text(encoding='utf-8').lower().count('clinic') >= 1; neutral = (root/'scripts/tests/fixtures/brief_neutral.md').read_text(encoding='utf-8').lower(); assert not any(t in neutral for t in ['loan','credit','clinic','casino','crypto','lawyer','medical']); verts = json.loads((root/'references/compliance-verticals.json').read_text(encoding='utf-8'))['verticals']; assert len(verts) == 5 and {v['name'] for v in verts} == {'medical','legal','finance','gambling','crypto'}; print('OK')"</automated>
  </verify>
  <done>
    All seven files exist on disk, parse as valid JSON / markdown, brief_medical.md contains medical tokens, brief_neutral.md contains zero regulated tokens from the five verticals, compliance-verticals.json has exactly five named verticals each with name/tokens/verification_url/policy_note.
  </done>
</task>

</tasks>

<verification>
- Run the three Phase 9 test files: every test is SKIPPED (MODULE_MISSING). Zero collection errors. `pytest --collect-only` succeeds.
- Run `python -m json.tool` against each JSON fixture and references/compliance-verticals.json — all parse cleanly.
- `grep -E "(loan|credit|clinic|casino|crypto|lawyer|medical)" brief_neutral.md` returns no matches.
- File count check: 3 test files + 4 fixture JSONs + 2 fixture markdown briefs + 1 production reference = 10 new files committed (no modifications to existing files).
</verification>

<success_criteria>
1. Wave 1 plans (01, 02, 03) can be executed independently and in parallel because every test contract they must satisfy is committed.
2. CMPL-02 contract is observable: `references/compliance-verticals.json` exists with 5 verticals each having {name, tokens, verification_url, policy_note}; operator can extend by editing JSON, no Python.
3. Wave 0 RED state proven: pytest skips all 25-30 Phase 9 tests with `MODULE_MISSING` reason; pre-existing v1.0 tests remain green (no regression).
4. Fixtures cover three CPC scenarios (full / none / partial-with-orphan) plus the two compliance scenarios (regulated / neutral).
</success_criteria>

<output>
After completion, create `.planning/phases/09-campaign-economics-and-compliance/09-00-SUMMARY.md` documenting: files created, MODULE_MISSING guard pattern used, fixture row counts and intent coverage, and the contract Wave 1 plans must satisfy (function signatures, file paths read/written).
</output>
