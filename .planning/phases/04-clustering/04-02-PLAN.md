---
phase: 04-clustering
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
autonomous: false
requirements: [CLST-01, CLST-02]

must_haves:
  truths:
    - "SKILL.md Steps 14-16 instruct Claude to partition ranked.json by intent before any semantic clustering (hard pre-split)"
    - "Step 15 prompt tells Claude to target 5-15 keywords per cluster (min 3) and name each cluster {theme_slug}_{intent}"
    - "Step 16 runs validate_clusters.py after writing clusters.json, parses exit code, and loops up to 2 fix iterations on exit 3"
    - "SKILL.md stays under 500 lines after the addition"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Phase 4 clustering steps (Steps 14-16)"
      contains: "validate_clusters.py"
  key_links:
    - from: "Step 15 (cluster prompt)"
      to: "intent partition from Step 14"
      via: "per-partition loop — each intent class clustered independently"
      pattern: "partition|intent class"
    - from: "Step 16 (validate loop)"
      to: "validate_clusters.py CLI"
      via: "uv run validate_clusters.py --run-dir"
      pattern: "validate_clusters\\.py.*--run-dir"
    - from: "Step 16 fix loop"
      to: "violation JSON from stdout"
      via: "parse violations list, re-prompt for offending cluster names only"
      pattern: "violations"
---

<objective>
Add Steps 14-16 to SKILL.md, implementing the intent-pre-split + LLM cluster + validate loop pattern. This is the operator-facing workflow that produces clusters.json for every research run.

Purpose: Without these steps the skill halts after Phase 3 and never produces ad-group clusters.
Output: SKILL.md with 3 new steps + checkpoint verifying the end-to-end flow reads correctly.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-clustering/04-RESEARCH.md
@.planning/phases/04-clustering/04-01-SUMMARY.md
@.claude/skills/google-ad-research/SKILL.md

<interfaces>
<!-- validate_clusters.py CLI surface (implemented in 04-01) -->
```bash
uv run "${CLAUDE_SKILL_DIR}/scripts/validate_clusters.py" --run-dir "{run_dir}"
# Stdout (one JSON line):
# {"valid": bool, "cluster_count": N, "orphan_count": N, "violations": [...]}
# Exit codes: 0=valid, 1=warnings, 3=hard violations, 2=infra error
```

<!-- clusters.json write target -->
{run_dir}/clusters.json — schema from 04-RESEARCH.md:
{
  "metadata": {"clustered_at": "<ISO>", "method": "llm-driven", "model": "...",
               "ranked_input": "ranked.json", "total_keywords": N, "total_clusters": N},
  "clusters": [{"name": "{theme_slug}_{intent}", "intent": "...",
                "keywords": [{"keyword": "...", "score": N}]}],
  "orphans": []
}

<!-- Cluster naming rule (from 04-RESEARCH.md) -->
Format: {theme_slug}_{intent} — lowercase snake_case, 2-4 meaningful words before intent suffix.
Validator rejects: uppercase, hyphens, numeric/cluster/theme/topic prefix, single-word themes.

<!-- Phase 3 stop gate to replace (current last line of SKILL.md) -->
Current Step 13 ends with: "**STOP. Do not proceed to any Phase 4+ activity.**"
Phase 4 replaces that stop with a continue signal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Steps 14-16 to SKILL.md and remove Phase 3 stop gate</name>
  <files>
    .claude/skills/google-ad-research/SKILL.md
  </files>
  <action>
Read the current SKILL.md. Locate the final line of Step 13: "**STOP. Do not proceed to any Phase 4+ activity.**"

Replace that stop gate with: "Phase 4 (clustering) begins at Step 14 below."

Then append the following Phase 4 section after Step 13 (do NOT modify any existing step content — append only):

---

## Phase 4: Clustering

> Prerequisites: Phase 3 complete — `{run_dir}/ranked.json` exists.

### Step 14: Partition keywords by intent

Read `{run_dir}/ranked.json`. Partition all keywords into four lists by their `intent` field.

Print a count summary to confirm the split:
> "Intent partition: transactional=N, commercial=N, informational=N, navigational=N"

Store each partition in memory. **Do not proceed to Step 15 until you have printed this summary and all keywords from ranked.json are accounted for (sum of partition counts == total rows in ranked.json).**

### Step 15: Cluster semantically within each partition

For each intent class that has ≥ 1 keyword, perform semantic clustering:

1. Display the keyword list for that intent class (keyword + score, sorted by score descending).
2. Group the keywords into thematic clusters following these rules:
   - **Target size:** 5-15 keywords per cluster. Minimum 3.
   - If a class has < 3 keywords total: create a single `misc_{intent}` cluster containing all of them.
   - **Do NOT split keywords across intent classes.** Every cluster contains keywords of exactly one intent.
   - **Naming:** Each cluster name must be `{theme_slug}_{intent}` — derive the theme slug from the 2-3 most-frequent meaningful words in the group's keywords. Use lowercase snake_case only. No hyphens. No numbers in the slug. No generic prefixes (cluster, theme, topic, group).
     - Valid: `same_day_delivery_transactional`, `grocery_brand_comparison_commercial`
     - Invalid: `cluster_1_transactional`, `Grocery_Transactional`, `grocery_transactional` (single-word theme)
   - **Do NOT re-assign intent.** Do NOT create clusters that span more than one intent class.
   - **Fold fragments:** If after grouping any cluster would have < 3 keywords, merge it into the nearest thematic neighbor within the same intent class.

3. When all intent classes are clustered, write `{run_dir}/clusters.json` using the Write tool:

```json
{
  "metadata": {
    "clustered_at": "<ISO timestamp>",
    "method": "llm-driven",
    "model": "<your model name>",
    "ranked_input": "ranked.json",
    "total_keywords": <N>,
    "total_clusters": <N>
  },
  "clusters": [
    {
      "name": "<theme_slug>_<intent>",
      "intent": "<intent class>",
      "keywords": [
        {"keyword": "<keyword string>", "score": <score integer>}
      ]
    }
  ],
  "orphans": []
}
```

Every keyword from ranked.json must appear in exactly one cluster or in `orphans`. Do not include any ranked.json fields beyond `keyword` and `score` in the clusters array.

**Do not advance to Step 16 until `{run_dir}/clusters.json` exists.**

### Step 16: Validate and fix loop

Run the validator:

```bash
uv run "${CLAUDE_SKILL_DIR}/scripts/validate_clusters.py" --run-dir "{run_dir}"
```

Parse the stdout JSON and the exit code:

**Exit 0 — valid:** Continue to Step 17.

**Exit 1 — warnings only:** Surface the warnings to the operator:
> "Clustering complete with warnings: {violations list}. Proceed anyway or fix? (proceed/fix)"
If operator says fix: treat as exit 3 for the warned clusters and re-prompt. If proceed: continue to Step 17.

**Exit 3 — hard violations:** Read the `violations` list from stdout. For each violation, identify the offending cluster by name. Re-prompt for ONLY those clusters:
> "Validator found violations in clusters: {names}. Violation details: {violations}. Re-clustering only those groups now."

Re-cluster the offending clusters (keeping all other clusters unchanged), rewrite only those entries in `{run_dir}/clusters.json` (preserve the rest), then re-run the validator.

Maximum 2 fix iterations. If violations persist after 2 iterations:
> "Clustering could not satisfy all invariants after 2 fix attempts. Remaining violations: {violations}. Stopping — operator review required."
Do NOT proceed to Step 17.

**Exit 2 — infrastructure error:** Surface the error to the operator. Do not retry silently.

**Do not advance to Step 17 until validator exits 0 (or operator accepts exit 1 warnings).**

### Step 17: Confirm Phase 4 complete and stop

Tell the operator:

> "Phase 4 complete. Clustering summary:
> - {total_clusters} clusters created across {intent_count} intent classes
> - {total_keywords} keywords assigned
> - Orphans: {orphan_count}
> - Validator: {valid status}
>
> Clusters file: `{run_dir}/clusters.json`
>
> Phase 5 (competitor intel) is not yet available in this skill."

**STOP. Do not proceed to any Phase 5+ activity.**

---

After appending, verify SKILL.md line count is ≤ 500. If it exceeds 500 lines, extract the Step 15 cluster naming rubric into `.claude/skills/google-ad-research/references/cluster-naming.md` and replace the inline rules with: `> Naming rules: load @.claude/skills/google-ad-research/references/cluster-naming.md`
  </action>
  <verify>
    <automated>python -c "
lines = open('C:/Users/Izzy/Documents/Projects/google-ad-research-agent/.claude/skills/google-ad-research/SKILL.md').readlines()
count = len(lines)
text = ''.join(lines)
assert 'Step 14' in text, 'Step 14 missing'
assert 'Step 15' in text, 'Step 15 missing'
assert 'Step 16' in text, 'Step 16 missing'
assert 'validate_clusters.py' in text, 'validator invocation missing'
assert count <= 500, f'SKILL.md is {count} lines, exceeds 500'
print(f'OK: {count} lines, Steps 14-16 present, validator referenced')
"</automated>
  </verify>
  <done>SKILL.md contains Steps 14-17, references validate_clusters.py --run-dir in Step 16, and is ≤ 500 lines total. The Phase 3 stop gate is replaced with a continue signal to Phase 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Steps 14-16 added to SKILL.md. Intent pre-split (Step 14), semantic clustering within partitions (Step 15), and validate-and-fix loop (Step 16) are all present.</what-built>
  <how-to-verify>
    1. Open `.claude/skills/google-ad-research/SKILL.md`
    2. Confirm Steps 14, 15, 16, 17 are present after Step 13.
    3. Confirm Step 13 no longer says "STOP. Do not proceed to any Phase 4+ activity." — it should say "Phase 4 (clustering) begins at Step 14 below."
    4. Confirm Step 16 includes the `uv run ... validate_clusters.py --run-dir` invocation and the 2-iteration fix loop.
    5. Confirm SKILL.md is ≤ 500 lines.
    6. Optional smoke test — run a clustering pass with a real run folder that has ranked.json and confirm clusters.json is produced and the validator exits 0.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- SKILL.md line count ≤ 500 (automated check in Task 1 verify)
- Steps 14-17 present and structurally correct (human-verify checkpoint)
- Full test suite still green: `uv run --with pytest --directory .../scripts pytest tests/ -q`
</verification>

<success_criteria>
- SKILL.md contains Steps 14-17 with intent pre-split, size-bounded semantic clustering, and validate-and-fix loop
- Step 16 invokes `validate_clusters.py --run-dir` and handles exit codes 0, 1, 3, 2
- Fix loop capped at 2 iterations with operator surface on failure
- SKILL.md ≤ 500 lines
- No existing step content modified (append-only change)
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-clustering/04-02-SUMMARY.md`
</output>
