---
phase: 12-source-consolidation-drop-tavily
plan: 03
type: execute
wave: 1
depends_on: [12-00]
files_modified:
  - .claude/skills/google-ad-research/scripts/pulse_fetch.py
  - .claude/skills/google-ad-research/scripts/pulse_synth.py
autonomous: true
requirements: [PULSE-10, PULSE-11]
must_haves:
  truths:
    - "pulse_fetch.py contains zero references to 'tavily' (case-insensitive substring)"
    - "pulse_fetch.py does not write raw/tavily-news.json"
    - "pulse_fetch.py symbols fetch_tavily_news + normalise_tavily_news do not exist"
    - "pulse_synth.load_news_items accepts exactly one positional argument: serper_path"
    - "pulse_synth.py contains zero references to 'tavily' (case-insensitive substring)"
    - "Wave 0 tests test_only_serper_news_written + test_no_tavily_news_path_in_main + test_load_news_items_serper_only all GREEN"
    - "Existing test_pulse_synth.py Phase 7 tests still GREEN against updated single-source signature"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/pulse_fetch.py"
      provides: "Single-source niche pulse — Serper /news only"
      contains: "fetch_serper_news + main_with_args; no fetch_tavily_news"
    - path: ".claude/skills/google-ad-research/scripts/pulse_synth.py"
      provides: "Single-source load_news_items + simplified themes loop"
      contains: "def load_news_items(serper_path: Path) -> list[dict]"
  key_links:
    - from: "pulse_fetch.py:main"
      to: "raw/serper-news.json"
      via: "json.dump"
      pattern: "single write per run; no raw/tavily-news.json"
    - from: "pulse_synth.py:load_news_items"
      to: "raw/serper-news.json"
      via: "single Path argument"
      pattern: "def load_news_items\\(serper_path: Path\\)"
---

<objective>
Drop the Tavily news call from pulse_fetch.py. Simplify pulse_synth.load_news_items to a single Serper source.

Purpose: Owns the Phase 7 (niche pulse) Tavily removal. Independent of competitor_intel + merge_signals (Plan 02) and the documentation scrub (Plan 04). Parallel-safe in Wave 1.

Output: 2 scripts edited. PULSE-10 + PULSE-11 audit tests flip GREEN. Phase 7 pulse flow still produces niche-pulse.json (now from Serper /news only).
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-consolidation-drop-tavily/12-RESEARCH.md
@.planning/phases/12-source-consolidation-drop-tavily/12-VALIDATION.md

# Files this plan modifies (READ IN FULL before editing)
@.claude/skills/google-ad-research/scripts/pulse_fetch.py
@.claude/skills/google-ad-research/scripts/pulse_synth.py

# Audit gates
@.claude/skills/google-ad-research/scripts/tests/test_pulse_fetch.py
@.claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py
@.claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py

<interfaces>
<!-- Concrete edits -->

pulse_fetch.py — per RESEARCH.md, current state:
- Lines 118-151: define fetch_tavily_news() + normalise_tavily_news()
- Lines 234-261: Tavily call loop in main flow
- Lines 264-265: write raw/tavily-news.json

DELETE all three blocks plus:
- `from tavily import ...` imports
- TAVILY_API_KEY check in load_env(require=...) — change to ("SERPER_API_KEY",)
- PEP 723 dependencies block: remove tavily-python
- Module docstring + exit-code docstring lines mentioning Tavily quota
- Any TavilyClient instantiation
- Any Tavily-specific log strings

PRESERVE:
- fetch_serper_news() and its caller chain
- raw/serper-news.json write
- Locale plumbing
- Retry / exit-code semantics for Serper paths

pulse_synth.py — per RESEARCH.md, current state:
- Line 24: reference to tavily-news.json constant or default path
- Lines 166-188: `def load_news_items(serper_path, tavily_path)` reads BOTH files
- Lines 446-453: caller passes tavily_path

Replace load_news_items with:
```python
def load_news_items(serper_path: Path) -> list[dict]:
    """Read serper-news.json; return flat list of news items."""
    items: list[dict] = []
    if serper_path.exists():
        try:
            data = json.loads(serper_path.read_text(encoding="utf-8"))
            for block in data.get("by_seed", []):
                for item in block.get("items", []):
                    items.append(item)
        except (json.JSONDecodeError, OSError):
            pass
    return items
```

Update all callers (lines 446-453 area) to pass single Path. Remove tavily_path variable declarations. Remove the `tavily-news.json` constant on line 24 if it's only used for the deleted branch.

NOTE: Pitfall 4 (RESEARCH.md): theme threshold may need re-tune in single-source mode. Phase 12 e2e smoke (Plan 05) will exercise this. If Plan 05 smoke shows zero themes on a known-active vertical, Plan 05 will tune MIN_THEME_MENTIONS_FLOOR. NOT this plan's responsibility — leave threshold at current value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor pulse_fetch.py — strip Tavily news call (PULSE-10)</name>
  <files>.claude/skills/google-ad-research/scripts/pulse_fetch.py</files>
  <behavior>
    After this task:
    - `import pulse_fetch; inspect.getsource(pulse_fetch).lower().count('tavily') == 0`
    - `hasattr(pulse_fetch, 'fetch_tavily_news') is False`
    - `hasattr(pulse_fetch, 'normalise_tavily_news') is False`
    - String literal 'tavily-news.json' absent from module source
    - Running pulse_fetch.main with a synthetic run_dir produces only raw/serper-news.json — no raw/tavily-news.json
    - Wave 0 tests test_only_serper_news_written + test_no_tavily_news_path_in_main flip GREEN
  </behavior>
  <action>
    Step 1 — Map all Tavily references:
    ```
    grep -n -i tavily .claude/skills/google-ad-research/scripts/pulse_fetch.py
    ```

    Step 2 — Surgical deletion:
    - Remove `from tavily import ...` imports
    - Remove `def fetch_tavily_news(...)` function entirely
    - Remove `def normalise_tavily_news(...)` function entirely
    - Remove the main-loop block that calls fetch_tavily_news (RESEARCH.md cites lines 234-261)
    - Remove the `(raw_dir / "tavily-news.json").write_text(...)` write (lines 264-265)
    - Remove TavilyClient instantiation if present
    - Remove `tavily_client.close()` calls
    - Update load_env: `load_env(require=("SERPER_API_KEY",))`
    - Update PEP 723 metadata: remove `tavily-python` from the inline deps list

    Step 3 — Update docstrings and exit-code documentation:
    - Module docstring: remove any "Tavily news" mention; replace with "Fetches niche pulse news from Serper /news (PULSE-01)."
    - Exit-code docstring: remove "Tavily quota → exit 2" line (line 31 per RESEARCH.md). Retain Serper exit semantics: 0 success, 1 input error, 2 retryable Serper HTTP, 3 fatal auth.

    Step 4 — Verify Wave 0 + Phase 7 existing tests:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_fetch.py -x
    ```
    Expected: both new tests PASS.

    Step 5 — Quick smoke: import the refactored module in Python and confirm no SyntaxError, no ImportError:
    ```
    uv run --project .claude/skills/google-ad-research/scripts python -c "import pulse_fetch; print('ok', hasattr(pulse_fetch, 'main_with_args'))"
    ```
    Expected stdout: `ok True`
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_fetch.py -x</automated>
  </verify>
  <done>pulse_fetch.py imports without errors. Zero 'tavily' substrings in source. fetch_tavily_news + normalise_tavily_news symbols absent. test_only_serper_news_written + test_no_tavily_news_path_in_main both GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor pulse_synth.py — single-source load_news_items + caller chain (PULSE-11)</name>
  <files>.claude/skills/google-ad-research/scripts/pulse_synth.py</files>
  <behavior>
    After this task:
    - `inspect.signature(pulse_synth.load_news_items).parameters` is exactly `OrderedDict([('serper_path', ...)])`
    - `inspect.getsource(pulse_synth).lower().count('tavily') == 0`
    - All callers of load_news_items pass a single Path argument
    - Existing Phase 7 tests in test_pulse_synth.py still GREEN — any test that called load_news_items with 2 args must be updated to single-arg (these may exist; the Wave 0 plan flagged test_pulse_synth.py for modifications)
    - Wave 0 test_load_news_items_serper_only flips GREEN
  </behavior>
  <action>
    Step 1 — Read pulse_synth.py end-to-end (RESEARCH.md cites it as a large file with the change concentrated at lines 24, 166-188, 446-453).

    Step 2 — Replace the load_news_items function:
    ```python
    def load_news_items(serper_path: Path) -> list[dict]:
        """Read serper-news.json; return flat list of news items.

        Phase 12: single-source. Tavily news removed per PULSE-11.
        """
        items: list[dict] = []
        if serper_path.exists():
            try:
                data = json.loads(serper_path.read_text(encoding="utf-8"))
                for block in data.get("by_seed", []):
                    for item in block.get("items", []):
                        items.append(item)
            except (json.JSONDecodeError, OSError):
                pass
        return items
    ```

    Step 3 — Update all callers. Find every call site:
    ```
    grep -n "load_news_items" .claude/skills/google-ad-research/scripts/pulse_synth.py
    ```
    RESEARCH.md cites line 446-453 area. Each caller currently passes (serper_path, tavily_path) → reduce to (serper_path) only. Delete the local `tavily_path = raw_dir / "tavily-news.json"` declaration (or wherever the path is built).

    Step 4 — Remove the line-24 constant if it's `TAVILY_NEWS_FILENAME = "tavily-news.json"` or similar single-purpose constant. Keep SERPER_NEWS_FILENAME (used by surviving code).

    Step 5 — Update test_pulse_synth.py helpers IF the existing tests called load_news_items with 2 args:
    - Per RESEARCH.md the helper `_items_from_fixtures()` at lines 23-28 passes both paths. Update it to pass only serper_path. This is a test-update task, but it lives in the same plan because it's the immediate caller — without this update, the new test_load_news_items_serper_only would be the only GREEN test while pre-existing tests turn RED.
    - Verify by reading test_pulse_synth.py and locating any `load_news_items(` call. Update each to single-arg.

    Step 6 — Verify:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py -x
    ```
    Expected: full suite GREEN (existing Phase 7 tests still pass under single-arg signature, plus new test_load_news_items_serper_only PASS).
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py -x</automated>
  </verify>
  <done>pulse_synth.py contains zero 'tavily' substrings. load_news_items signature is (serper_path: Path) -> list[dict]. All call sites updated. Test helper _items_from_fixtures updated to pass single arg. Full test_pulse_synth.py suite GREEN including the new test_load_news_items_serper_only.</done>
</task>

</tasks>

<verification>
1. Targeted Wave 0 subset:
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_fetch.py .claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py::test_load_news_items_serper_only -x
   ```
   Expected: all PASS.

2. Phase 7 regression check:
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py -x
   ```
   Expected: full file GREEN.

3. File ownership respected:
   - Plan 03 touches only pulse_fetch.py + pulse_synth.py
   - Does NOT modify SKILL.md, references/, render_report.py (Plan 04 owns those)
   - Does NOT modify competitor_intel.py, merge_signals.py (Plan 02 owns those)
</verification>

<success_criteria>
- PULSE-10, PULSE-11 requirements flip from Pending to Complete-by-test
- pulse_fetch.py + pulse_synth.py 'tavily' substring count == 0 each
- load_news_items signature is single-arg
- 3 Wave 0 tests flipped GREEN (test_only_serper_news_written, test_no_tavily_news_path_in_main, test_load_news_items_serper_only)
- Pre-existing Phase 7 tests in test_pulse_synth.py remain GREEN under updated signature
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-consolidation-drop-tavily/12-03-SUMMARY.md` recording:
- Exact line ranges deleted from each file
- Existing test_pulse_synth.py tests that needed signature updates
- Caller chain updates in pulse_synth.py
- Status: PULSE-10 + PULSE-11 GREEN; Wave 0 audit residual: WFCH-01, WFCH-02, PULSE-12, repo-grep — these flip in Plan 04
</output>
