---
phase: 12-source-consolidation-drop-tavily
plan: 05
type: execute
wave: 3
depends_on: [12-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Full pytest suite is GREEN against post-Phase-12 codebase"
    - "Fresh end-to-end skill run on a sample brief produces report.md with non-empty competitor section AND zero 'tavily' strings"
    - "Operator can run `grep -rni tavily .` (excluding .planning/.git/uv.lock) against the skill tree and get zero matches"
    - "Niche Pulse section still produces at least one trending theme on a known-active vertical (PULSE-tuning sanity per Pitfall 4)"
    - "REQUIREMENTS.md TVLY-01..04, WFCH-01..04, PULSE-10..12 all marked Complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "11 Phase 12 requirements flipped from Pending to Complete"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.3 complete; Phase 12 closed; performance metrics updated"
  key_links:
    - from: "operator skill session"
      to: "report.md competitor section + niche pulse section"
      via: "real Serper API + Claude WebFetch tool"
      pattern: "report contains headline/CTA/offer from WebFetch; no tavily strings"
---

<objective>
Final phase gate. Run full pytest suite + end-to-end smoke against a real brief. Verify zero Tavily residue on a fresh run folder. Update REQUIREMENTS.md + STATE.md to close Milestone v1.3.

Purpose: Phase 12 is a deletion phase — its biggest risk is incompleteness (Pitfall 1). This plan is the human-verified backstop that confirms the skill still works end-to-end after a vendor is removed. Single checkpoint plan, not autonomous.

Output: Phase closeout. All 11 v1.3 requirements marked Complete. STATE.md milestone v1.3 closed.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-consolidation-drop-tavily/12-RESEARCH.md
@.planning/phases/12-source-consolidation-drop-tavily/12-VALIDATION.md
@.planning/phases/12-source-consolidation-drop-tavily/12-00-SUMMARY.md
@.planning/phases/12-source-consolidation-drop-tavily/12-01-SUMMARY.md
@.planning/phases/12-source-consolidation-drop-tavily/12-02-SUMMARY.md
@.planning/phases/12-source-consolidation-drop-tavily/12-03-SUMMARY.md
@.planning/phases/12-source-consolidation-drop-tavily/12-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full pytest suite + repo-wide grep audit</name>
  <files>.planning/phases/12-source-consolidation-drop-tavily/12-05-SUMMARY.md (will be created in output step)</files>
  <action>
    Step 1 — Run the full suite:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ -x 2>&1 | tee /tmp/phase12-full-suite.log
    ```
    Capture: total tests collected, passed, failed, skipped, duration.

    Expected: 245+ passed, 0 failed, ≤5 skipped (intentional skips like manual-verify checkpoints in older plans).

    Step 2 — Run the repo-wide grep audit one more time, manually (not just via pytest), to catch anything the pytest audit might miss:
    ```
    grep -rni tavily .claude/skills/google-ad-research/ 2>&1 | grep -v -E "(__pycache__|\.pytest_cache)" | tee /tmp/phase12-grep.log
    ```
    Expected output: empty (zero matches). If any non-empty line surfaces, classify:
      - In SKILL.md / references/*.md / scripts/*.py / scripts/lib/*.py → MUST fix before checkpoint
      - In tests/fixtures/*.json → MUST delete fixture file or rename
      - In .planning/ or .git/ → out of scope (excluded by audit test)

    Also grep the .env.example:
    ```
    grep -i tavily .env.example
    ```
    Expected: empty.

    Step 3 — Verify uv environment resolves cleanly:
    ```
    cd .claude/skills/google-ad-research/scripts && uv lock --check
    ```
    Expected: uv.lock is up-to-date with pyproject.toml (no tavily-python entries in uv.lock).

    Step 4 — Stage findings into a draft summary section. Save numbers and grep output for the checkpoint task.
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ -x</automated>
  </verify>
  <done>Full pytest suite GREEN (zero failures). Repo-wide grep returns zero 'tavily' matches outside .planning/.git. uv.lock matches pyproject.toml.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verified end-to-end smoke on real brief</name>
  <files>(manual verification — no file modifications by this checkpoint task itself)</files>
  <action>Pause for human verification. The human operator runs the e2e smoke described in how-to-verify and types "approved" to resume. See how-to-verify block for the 9 verification steps.</action>
  <verify>Manual e2e (no automated command — WebFetch is Claude-only and unavailable to pytest). Human operator confirms each of the 9 steps in how-to-verify produces the expected result.</verify>
  <done>Human operator types "approved" or describes specific failure that needs a 12-06 follow-up.</done>
  <what-built>
    Phase 12 deletion of Tavily across all 11 surfaces. Replacement: Claude WebFetch for Phase 5 LP extraction; Serper /news single-source for Phase 7 niche pulse. All upstream/downstream contracts preserved.

    Code changes (automated tests cover these):
    - scripts/tavily_extract.py deleted
    - scripts/competitor_intel.py refactored (Serper-only advertisers)
    - scripts/merge_signals.py refactored (5-source taxonomy)
    - scripts/pulse_fetch.py refactored (Serper /news only)
    - scripts/pulse_synth.py refactored (single-source load_news_items)
    - scripts/render_report.py extended (_load_competitor_landing_pages JOIN)
    - SKILL.md: WebFetch added to allowed-tools
    - references/phase5-competitor-intel.md: Step 19 rewritten for WebFetch
    - references/phase7-niche-pulse.md: Tavily mentions stripped
    - lib/config.py: REQUIRED_KEYS=("SERPER_API_KEY",)
    - pyproject.toml: tavily-python removed
    - .env.example: TAVILY_API_KEY line removed
    - REQUIREMENTS.md: PULSE-02 marked DEPRECATED

    What automated tests CANNOT verify:
    - WebFetch invocation actually works against real advertiser URLs from inside a Claude session (WebFetch is a Claude-only tool — pytest cannot call it)
    - Real Serper /news pulse produces non-empty themes on a known-active vertical (Pitfall 4 — single-source threshold tuning)
    - report.md competitor section renders WebFetch-sourced headline/CTA/offer correctly with real data
  </what-built>
  <how-to-verify>
    Manual e2e steps:

    1. **Environment setup:**
       - Copy `.env.example` → `.env` if not already present
       - Ensure only `SERPER_API_KEY` is set in `.env` (Ahrefs + Google Ads keys may also be present for Phase 8 — that's fine)
       - Confirm TAVILY_API_KEY is NOT in `.env` (or is fine to keep but unused — the skill should not reference it)
       - Run `uv run --project .claude/skills/google-ad-research/scripts python -c "from lib.config import load_env; load_env(require=('SERPER_API_KEY',)); print('env ok')"` and confirm `env ok` appears (no error about TAVILY_API_KEY)

    2. **Skill discovery:** Open a fresh Claude Code session in this repo. Trigger the skill: "I want keyword research for [your brief]". Confirm the skill auto-discovers via SKILL.md frontmatter description.

    3. **Phase 1-2:** Paste a brief (use the urgent-care Lake Worth brief from Phase 11 closeout, or any real brief). Confirm intake captures 5 required fields and run_init.py creates a sealed `.runs/<timestamp>-<slug>/` folder.

    4. **Phase 3-4:** Let the skill run signal collection + ranking + clustering through Step 13. Confirm `keywords.json` and `clusters.json` produced.

    5. **Phase 5 (the critical verification):**
       - Skill runs `competitor_intel.py` → confirm `raw/competitor-intel.json` produced with advertisers carrying `{domain, url, title, description, position}` only (no `raw_content`, no `tavily_fetched_at`).
       - Skill reaches Step 19 (in `references/phase5-competitor-intel.md`). Confirm:
         - Claude invokes WebFetch on top 3-5 advertiser URLs in one cluster (does NOT call any Tavily-shaped function)
         - WebFetch handles redirects per the single-retry rule
         - Claude writes `raw/competitor-landing-pages.json` via the Write tool with the `{clusters: {cluster_name: {advertisers: [{domain, url, headline, cta, offer, extract_status}]}}}` schema
       - Repeat for at least 2 clusters to confirm pattern stability

    6. **Phase 6 (render):** Skill runs `render_report.py` → confirm:
       - `report.md` competitor section shows headline/CTA/offer per advertiser when WebFetch extraction succeeded
       - Failed extractions show "_(landing page extraction failed)_" fallback (or equivalent)
       - No "(no headline extracted)" cosmetic regressions

    7. **Phase 7 (optional but recommended):** Run pulse_fetch + pulse_synth → confirm:
       - Only `raw/serper-news.json` written (no `raw/tavily-news.json`)
       - `niche-pulse.json` produced with at least 1 trending theme (assuming the brief vertical has news activity)
       - If themes count is zero on a known-active vertical → Pitfall 4 has materialized. Tune `MIN_THEME_MENTIONS_FLOOR` in pulse_synth.py from 3 to 2 as a hot-fix, re-run pulse_synth, confirm themes surface. Document the tune in the SUMMARY.

    8. **Final grep on the run folder:**
       ```
       grep -rni tavily .runs/<your-run-folder>/
       ```
       Expected: empty (no Tavily strings in any output JSON or report.md).

    9. **Final repo grep:**
       ```
       grep -rni tavily .claude/skills/google-ad-research/
       ```
       Expected: empty.

    Verification of WebFetch permission behavior (Open Question #4 from RESEARCH.md):
    - First-run experience: Claude Code may prompt for permission on each new advertiser domain (5-25 prompts)
    - In `mode: yolo` (per .planning/config.json), check whether prompts auto-approve or still surface
    - Document the observed behavior in the SUMMARY. If prompts are disruptive, recommend adding `WebFetch(domain:*)` to project settings — documented as a follow-up, not a Phase 12 blocker
  </how-to-verify>
  <resume-signal>Type "approved" if e2e smoke passes all 9 checks. If any check fails, describe the failure and which surface needs a Plan 12-06 follow-up.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md + STATE.md — close Milestone v1.3</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Step 1 — Update REQUIREMENTS.md:
    - Flip all 11 v1.3 requirement checkboxes from `[ ]` to `[x]` (TVLY-01..04, WFCH-01..04, PULSE-10..12) in the v1.3 section
    - Update the Traceability table rows for Phase 12 entries: change status from `Pending` to `Complete`
    - Update the coverage summary at the bottom: `v1.3 requirements: 11 / 11 Complete`
    - Total requirements line: `78 + 11 = 89 / 89 Complete`
    - Update the trailing `*Last updated: ...*` line to the current date and Phase 12 closeout note

    Step 2 — Update STATE.md:
    - Frontmatter `status`: change from `defining_requirements` to `complete` (or to next-milestone-defining state — use `awaiting_next_milestone` if no v1.4 defined)
    - Frontmatter `progress`: bump completed_phases to 12, total_plans to (current total + 6 plans for Phase 12), completed_plans likewise
    - `stopped_at` line: replace with Phase 12 ship note dated today (2026-05-15 per system clock, or current date if drifted)
    - `last_updated`: today
    - Current Position table: Phase → "12 — Complete"; Status → "Milestone v1.3 shipped"
    - Add a Previous Milestone v1.3 row above the v1.2 row, mirroring its structure (Phase count, requirement count, ship date)
    - Performance Metrics: add a `v1.3 requirements complete | 11 / 11 (TVLY 4/4, WFCH 4/4, PULSE 3/3)` row
    - Execution History: add 6 rows for Phase 12 plans 00-05 with their durations from each plan SUMMARY
    - Accumulated Context > Decisions: append Phase 12 key decisions extracted from each SUMMARY (e.g., "Phase 12: render_report.py JOINs competitor-intel.json + competitor-landing-pages.json on case-insensitive domain match with www.-prefix stripped", "Phase 12: SKILL.md allowed-tools gains WebFetch without domain restriction (operator briefs name arbitrary advertiser URLs)")
    - Session Continuity > Stopped at + Next session: update to reflect Phase 12 closeout and propose v1.4 scoping or open-question triage as next action

    Step 3 — Update ROADMAP.md Phase 12 section:
    - Change status from "Not started" to "Complete    | <today's date>"
    - Mark Plans list as `[x]` for all 6 plans (12-00 through 12-05)
    - Update the Progress table row for Phase 12: `6/6 plans complete | Complete | <today>`
    - Update the coverage line at top from `Coverage: 35/35 v1 requirements` to reflect full v1.3 inclusion (already at 89/89 per current ROADMAP.md so likely no change)

    Step 4 — Git commit all three files in one final commit:
    ```
    git add .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(12-05): close Milestone v1.3 — Phase 12 (Drop Tavily) shipped"
    ```
  </action>
  <verify>
    <automated>grep -c "^\| TVLY-0[1-4] | Phase 12 | Complete |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md TVLY-01..04, WFCH-01..04, PULSE-10..12 all marked `[x]`. Traceability rows show Complete. STATE.md milestone v1.3 reflected as shipped. ROADMAP.md Phase 12 marked Complete with plan checkboxes filled. Three docs files committed in a closing commit.</done>
</task>

</tasks>

<verification>
1. Final full suite — last green before closing:
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ -x 2>&1 | tail -5
   ```
   Expected: zero failures.

2. Coverage table sanity:
   ```
   grep -E "v1\.3 requirements: 11 / 11" .planning/REQUIREMENTS.md
   ```
   Expected: one match.

3. STATE.md milestone closeout sanity:
   ```
   grep -E "Milestone v1\.3.*ship" .planning/STATE.md
   ```
   Expected: at least one match in the Previous Milestone section.
</verification>

<success_criteria>
- Full pytest suite GREEN (verified in Task 1)
- E2E human smoke approved (verified in Task 2)
- 11 v1.3 requirements marked Complete in REQUIREMENTS.md (verified in Task 3)
- STATE.md + ROADMAP.md show Milestone v1.3 shipped
- Final commit lands all closeout docs
- Phase 12 plan SUMMARYs exist for all 6 plans (00, 01, 02, 03, 04, 05)
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-consolidation-drop-tavily/12-05-SUMMARY.md` recording:
- Final pytest count (passed/failed/skipped)
- Repo grep result (zero matches confirmed)
- E2E smoke result (which brief used, themes count from pulse_synth, MIN_THEME_MENTIONS_FLOOR tune if applied)
- WebFetch permission behavior observed (auto-approved in yolo mode, or prompted per domain)
- Phase 12 milestone v1.3 closeout note
- Total requirements: 89 / 89 Complete
- Any Phase 12 follow-up items for v1.4 backlog (e.g., theme threshold re-tune, WebFetch domain-allowlist setting)
</output>
