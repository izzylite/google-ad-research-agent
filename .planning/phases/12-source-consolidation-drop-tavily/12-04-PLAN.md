---
phase: 12-source-consolidation-drop-tavily
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
  - .claude/skills/google-ad-research/references/phase5-competitor-intel.md
  - .claude/skills/google-ad-research/references/phase7-niche-pulse.md
  - .claude/skills/google-ad-research/scripts/render_report.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WFCH-01, WFCH-02, PULSE-12]
must_haves:
  truths:
    - "references/phase5-competitor-intel.md Step 19 instructs Claude to use WebFetch (not Tavily) for landing-page extraction"
    - "SKILL.md frontmatter allowed-tools includes WebFetch"
    - "references/phase7-niche-pulse.md contains zero 'tavily' substrings (case-insensitive)"
    - "render_report.py reads both raw/competitor-intel.json AND raw/competitor-landing-pages.json and JOINs on (cluster_name, domain) at render time"
    - "render_report.py exposes _load_competitor_landing_pages helper (sentinel that flips the Wave 0 skip-guard)"
    - "Competitor section in report.md shows headline/CTA/offer per advertiser when competitor-landing-pages.json is present, falls back to ad title/description when absent"
    - "REQUIREMENTS.md PULSE-02 marked DEPRECATED with strikethrough and pointer to PULSE-10"
    - "Wave 0 tests test_skill_md_uses_webfetch_for_step19 + test_phase7_docs_tavily_free + test_competitor_section_joins_webfetch_results + test_repo_grep_tavily_clean all GREEN"
  artifacts:
    - path: ".claude/skills/google-ad-research/references/phase5-competitor-intel.md"
      provides: "Step 19 rewrite — WebFetch invocation pattern mirroring Step 7 WebSearch baseline"
      contains: "WebFetch + extraction prompt + raw/competitor-landing-pages.json schema"
    - path: ".claude/skills/google-ad-research/references/phase7-niche-pulse.md"
      provides: "Steps 27-30 stripped of Tavily mentions"
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "allowed-tools frontmatter includes WebFetch; Phase 5 pointer updated; line count ≤ 500"
    - path: ".claude/skills/google-ad-research/scripts/render_report.py"
      provides: "_load_competitor_landing_pages helper + JOIN logic in competitor section renderer"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PULSE-02 marked DEPRECATED in body section; traceability table row preserved as historical"
  key_links:
    - from: "render_report.py:_render_competitor_section"
      to: "raw/competitor-landing-pages.json + raw/competitor-intel.json"
      via: "JOIN on (cluster_name, advertiser.domain)"
      pattern: "_load_competitor_landing_pages\\("
    - from: "SKILL.md:frontmatter"
      to: "Claude Code permission system"
      via: "allowed-tools field"
      pattern: "allowed-tools:.*WebFetch"
    - from: "references/phase5-competitor-intel.md:Step 19"
      to: "raw/competitor-landing-pages.json"
      via: "Write tool invocation in skill prompt"
      pattern: "WebFetch.*competitor-landing-pages\\.json"
---

<objective>
Rewrite Phase 5 + Phase 7 documentation to remove Tavily and instruct Claude to use WebFetch for landing-page extraction. Update render_report.py to JOIN the new WebFetch-produced JSON with the existing competitor-intel.json. Mark PULSE-02 deprecated.

Purpose: Single Wave-2 plan that integrates the new documentation contract with the rendering layer. Depends on Plans 01-03 because: (a) SKILL.md cannot reference WebFetch invocation until competitor_intel.py advertisers shape is locked (Plan 02), (b) Phase 7 docs cannot strip Tavily mentions while pulse_fetch.py still calls Tavily (Plan 03), (c) the audit grep test cannot pass until all upstream tavily references are gone (Plans 01-03 land first).

Output: 5 files edited (3 docs + 1 script + REQUIREMENTS.md). Wave 0 audit fully GREEN (test_repo_grep_tavily_clean PASSES because every code + doc surface is now clean). WFCH-01 + WFCH-02 + PULSE-12 + CMPL-03 sibling tests untouched.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-consolidation-drop-tavily/12-RESEARCH.md
@.planning/phases/12-source-consolidation-drop-tavily/12-VALIDATION.md

# Files this plan modifies
@.claude/skills/google-ad-research/SKILL.md
@.claude/skills/google-ad-research/references/phase5-competitor-intel.md
@.claude/skills/google-ad-research/references/phase7-niche-pulse.md
@.claude/skills/google-ad-research/scripts/render_report.py
@.planning/REQUIREMENTS.md

# Audit gates
@.claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py
@.claude/skills/google-ad-research/scripts/tests/test_render_report.py

<interfaces>
<!-- render_report.py JOIN helper contract -->

Add to render_report.py:
```python
def _load_competitor_landing_pages(run_dir: Path) -> dict:
    """Load raw/competitor-landing-pages.json (Phase 12 WFCH-02).

    Returns {} if file absent — graceful degrade. Wave 0 test_competitor_section_joins_webfetch_results
    asserts on hasattr(render_report, '_load_competitor_landing_pages') to lift its skip-guard.
    """
    path = run_dir / "raw" / "competitor-landing-pages.json"
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return {}


def _join_advertisers_with_landing_pages(
    advertisers: list[dict],
    cluster_name: str,
    landing_pages_doc: dict,
) -> list[dict]:
    """Per (cluster, domain) JOIN — overlay headline/cta/offer onto Serper advertiser entries.

    Each output entry is the original Serper advertiser dict augmented with optional
    'headline', 'cta', 'offer', 'extract_status' keys. Domain match is case-insensitive,
    leading 'www.' stripped on both sides for robustness.
    """
    lp_cluster = landing_pages_doc.get("clusters", {}).get(cluster_name, {})
    lp_advertisers = lp_cluster.get("advertisers", [])

    def _norm(d: str | None) -> str:
        if not d:
            return ""
        return d.lower().removeprefix("www.")

    lp_by_domain = {_norm(a.get("domain")): a for a in lp_advertisers}

    out = []
    for adv in advertisers:
        joined = dict(adv)
        lp = lp_by_domain.get(_norm(adv.get("domain")))
        if lp:
            joined["headline"] = lp.get("headline")
            joined["cta"] = lp.get("cta")
            joined["offer"] = lp.get("offer")
            joined["extract_status"] = lp.get("extract_status")
        out.append(joined)
    return out
```

Call site: in the competitor-section render function (the one currently reading `cluster.advertisers[].title/description/domain/url`), add at the top:

```python
landing_pages = _load_competitor_landing_pages(run_dir)
# inside the per-cluster loop:
advertisers_joined = _join_advertisers_with_landing_pages(
    cluster.get("advertisers", []), cluster.get("cluster_name", ""), landing_pages,
)
# Render headline/cta/offer when present; fall back to title/description otherwise.
```

The markdown rendering should display headline as the bold first line when present; CTA + offer on follow-up lines; fall back to `(no landing page data)` when extract_status == "failed" or no lp_by_domain match.

<!-- SKILL.md frontmatter -->

Current line 3 (per RESEARCH.md): `allowed-tools: Bash(uv run *) Read Write WebSearch`
New: `allowed-tools: Bash(uv run *) Read Write WebSearch WebFetch`

No domain restriction — operator's brief names arbitrary competitor URLs.

<!-- references/phase5-competitor-intel.md Step 19 — full rewrite per RESEARCH.md Pattern 1 -->

Section header today: `### Step 19: Extract landing page value props with Tavily`
New: `### Step 19: Extract landing-page value props via WebFetch (COMP-03 + WFCH-01..02)`

Body — follow RESEARCH.md Pattern 1 verbatim (the proposed Step 19 markdown block). Includes:
- Read competitor-intel.json
- WebFetch per cluster top 3-5 advertisers
- Extraction prompt asking for headline / cta / offer JSON
- Aggregate schema with extract_status field
- Write via Write tool to raw/competitor-landing-pages.json
- Rules: verbatim extraction, single redirect-follow cap, max 5 advertisers/cluster, failures normal
- STOP gate: "Do not advance to Step 20 until {run_dir}/raw/competitor-landing-pages.json exists"

Also rewrite Step 18 exit-code section per RESEARCH.md Pitfall 5: drop "Tavily quota" mention from `references/phase5-competitor-intel.md` line 27-29. Keep "Exit 2 = retryable Serper HTTP" semantics.

<!-- references/phase7-niche-pulse.md Steps 27-30 — strip Tavily mentions -->

Find every 'tavily' substring (case-insensitive). Locations per RESEARCH.md:
- Step 27 prompt: "~12 Tavily credits"
- Step 28 exit-code handling: "Tavily quota"

Replace mentions of Tavily news with single-source (Serper /news only) phrasing. Update any cost estimate ("~$0.02 per run" or similar) — Serper /news cost only.

<!-- REQUIREMENTS.md PULSE-02 deprecation per RESEARCH.md Pitfall 6 -->

REQUIREMENTS.md v1.0 PULSE-02 line (currently line 86 in the source):
```
- [x] **PULSE-02**: `pulse_fetch.py` calls Tavily `search` with `topic="news"` and `days=7` per seed keyword and persists `raw/tavily-news.json`
```

Replace with:
```
- [x] ~~**PULSE-02**: `pulse_fetch.py` calls Tavily `search` with `topic="news"` and `days=7` per seed keyword and persists `raw/tavily-news.json`~~ **DEPRECATED in v1.3 Phase 12 — superseded by PULSE-10.**
```

Traceability table row `| PULSE-02 | Phase 7 | Complete |` — LEAVE AS-IS (historical fact about Phase 7's original scope per RESEARCH.md Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite references/phase5-competitor-intel.md Step 18-19 (WFCH-01) + references/phase7-niche-pulse.md Steps 27-30 (PULSE-12)</name>
  <files>.claude/skills/google-ad-research/references/phase5-competitor-intel.md, .claude/skills/google-ad-research/references/phase7-niche-pulse.md</files>
  <action>
    Step 1 — Read references/phase5-competitor-intel.md fully. Note current Step 18 + Step 19 line ranges.

    Step 2 — Replace Step 19 entirely. Use RESEARCH.md Pattern 1 as the source-of-truth body. The full Step 19 markdown (heading + body + rules + STOP gate) goes in verbatim. Make sure JSON example uses non-fence-breaking syntax (e.g., escape with backticks where the example shows json fences inside the heredoc-style content).

    Step 3 — Update Step 18 exit-code prose. Find the section that says "exit 2 → prompt operator to continue with partial data due to Tavily quota". Replace with: "exit 2 = retryable Serper HTTP error (rate limit / 5xx). No Tavily quota path remains in Phase 12+."

    Step 4 — Final grep on phase5-competitor-intel.md:
    ```
    grep -i tavily .claude/skills/google-ad-research/references/phase5-competitor-intel.md
    ```
    Expected: zero matches. If matches found, edit them out (likely doc cleanup of cost estimates, anti-patterns, failure-mode tables).

    Step 5 — Read references/phase7-niche-pulse.md fully. Strip every 'tavily' / 'Tavily' substring. Likely targets per RESEARCH.md:
    - Step 27 prompt section: cost estimate, dual-source mention, "Tavily credits" line
    - Step 28: Tavily quota exit-code branch
    - Anti-patterns section: any "do not call Tavily twice" or similar
    - Failure-modes table: Tavily-specific row

    Replace dual-source mentions with single-source (Serper /news only). For cost estimate, remove or update to Serper-only.

    Step 6 — Final grep on phase7-niche-pulse.md:
    ```
    grep -i tavily .claude/skills/google-ad-research/references/phase7-niche-pulse.md
    ```
    Expected: zero matches.

    Step 7 — Verify Wave 0 audit subset:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_skill_md_uses_webfetch_for_step19 .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_phase7_docs_tavily_free -x
    ```
    Expected: both PASS.
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_skill_md_uses_webfetch_for_step19 .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_phase7_docs_tavily_free -x</automated>
  </verify>
  <done>references/phase5-competitor-intel.md Step 19 fully rewritten using RESEARCH.md Pattern 1; zero 'tavily' substrings remain. references/phase7-niche-pulse.md contains zero 'tavily' substrings. Both audit tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add WebFetch to SKILL.md allowed-tools; verify SKILL.md ≤500 lines + REQUIREMENTS.md PULSE-02 deprecation</name>
  <files>.claude/skills/google-ad-research/SKILL.md, .planning/REQUIREMENTS.md</files>
  <action>
    Step 1 — Read SKILL.md frontmatter (top ~20 lines).

    Step 2 — Edit the `allowed-tools:` line:
    ```yaml
    # BEFORE
    allowed-tools: Bash(uv run *) Read Write WebSearch
    # AFTER
    allowed-tools: Bash(uv run *) Read Write WebSearch WebFetch
    ```

    Step 3 — Scan SKILL.md body for any inline mention of Tavily. Per RESEARCH.md, Phase 5 details live in references/phase5-competitor-intel.md (already cleaned in Task 1), and SKILL.md has only pointer lines. Verify by grep:
    ```
    grep -i tavily .claude/skills/google-ad-research/SKILL.md
    ```
    If matches found (e.g., a Step 18 summary line, a Phase 5 pointer description), edit them out. Update Phase 5 pointer description to mention WebFetch if currently mentions Tavily.

    Step 4 — Verify line count ≤500 (per CLAUDE.md NON-NEGOTIABLE):
    ```
    wc -l .claude/skills/google-ad-research/SKILL.md
    ```
    Expected: ≤500. Phase 11 closed at 499/500 per STATE.md. If WebFetch addition + Tavily removal nets a small positive line delta, compact whitespace or further extract content into references/. If under 500, no action needed.

    Step 5 — Edit .planning/REQUIREMENTS.md:
    - Locate PULSE-02 in the v1.0 requirements section (~line 86 per RESEARCH.md).
    - Wrap text in strikethrough markdown and append the deprecation note per the interfaces block.
    - Do NOT modify the Traceability table row `| PULSE-02 | Phase 7 | Complete |` (historical truth per RESEARCH.md Pitfall 6).
    - In the Phase 12 v1.3 requirements section, the PULSE-10 entry already exists. No change there.
    - In Traceability table for Phase 12 rows, ensure PULSE-10..12 rows are present (already exist per current REQUIREMENTS.md line 305-307).

    Step 6 — Re-run repo-wide audit grep test:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py -x
    ```
    Expected: all 8 audit tests GREEN — Plans 01-03 cleaned scripts/config, Task 1 of this plan cleaned references/, this task cleans SKILL.md. Repo-wide grep test should now PASS because every source surface (excluding .planning/) is tavily-free.

    Note: The audit's `_iter_source_files()` includes references/ + SKILL.md + scripts/ but excludes .planning/ — so REQUIREMENTS.md's `~~PULSE-02~~` strikethrough containing the word "Tavily" inside the strikethrough does NOT trigger the repo-wide audit. Verify by re-reading test_audit_tavily_removed.py::test_repo_grep_tavily_clean: SKIP_DIRS includes the .planning/ path indirectly because REPO_ROOT walk starts from the SKILL_DIR, not the repo root. Confirm the audit walks only the skill subtree — re-check the test source: `roots = [SCRIPTS_DIR, REFERENCES_DIR, SKILL_DIR / "SKILL.md"]`. .planning/ is OUT of scope. GREEN.
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py -x</automated>
  </verify>
  <done>SKILL.md allowed-tools line includes WebFetch. SKILL.md line count ≤500. SKILL.md contains zero 'tavily' substrings. REQUIREMENTS.md PULSE-02 marked with strikethrough + deprecation pointer; traceability row preserved. All 8 Wave 0 audit tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add render_report.py competitor-landing-pages JOIN (WFCH-02)</name>
  <files>.claude/skills/google-ad-research/scripts/render_report.py</files>
  <action>
    Step 1 — Read render_report.py fully. Locate the competitor-section render function (RESEARCH.md cites lines 242-278 for the advertisers rendering block).

    Step 2 — Add _load_competitor_landing_pages helper near the top of the file (after imports, before main render functions). Use the implementation from the interfaces block above.

    Step 3 — Add _join_advertisers_with_landing_pages helper directly after _load_competitor_landing_pages.

    Step 4 — Modify the competitor section renderer:
    - At the top of the section function (or main() before calling it), call `landing_pages = _load_competitor_landing_pages(run_dir)`.
    - Inside the per-cluster loop, before iterating advertisers, replace `advertisers = cluster.get("advertisers", [])` with `advertisers = _join_advertisers_with_landing_pages(cluster.get("advertisers", []), cluster.get("cluster_name", ""), landing_pages)`.
    - In the markdown emission, where headline/CTA/offer should appear, render conditionally:
      - If advertiser.get("headline"): show as bold first line, then CTA line, then offer line.
      - Else if advertiser.get("extract_status") == "failed": show `_(landing page extraction failed)_`
      - Else: fall back to title + description (legacy Serper-only path).

    Step 5 — Schema: do NOT change report.json output. The JOIN is render-time only (markdown surface). report.json continues to carry competitor data per its existing Phase 6 schema unless WFCH-02 explicitly requires JSON extension — re-check WFCH-02 spec. WFCH-02 says "Skill writes extracted {headline, cta, offer} per advertiser to raw/competitor-landing-pages.json via Write tool". It does NOT require report.json extension. Keep report.json unchanged unless test_render_report.py demands otherwise.

    Step 6 — Verify Wave 0 JOIN test:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_render_report.py::test_competitor_section_joins_webfetch_results -x
    ```
    Expected: PASS (skip-guard lifts because _load_competitor_landing_pages exists; assertions on "Fresh groceries delivered today" + "Order now" + "Free delivery over £40" all pass).

    Step 7 — Full render_report regression:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_render_report.py -x
    ```
    Expected: all GREEN. The JOIN is additive — when competitor-landing-pages.json absent, legacy fallback path renders title/description (no regression on Phase 6/9/10/11 fixtures).
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_render_report.py -x</automated>
  </verify>
  <done>render_report.py exposes _load_competitor_landing_pages + _join_advertisers_with_landing_pages helpers. Competitor section JOINs by (cluster, domain). When competitor-landing-pages.json present, headline/CTA/offer render in markdown. When absent or extract_status=failed, legacy title/description fallback. test_competitor_section_joins_webfetch_results GREEN. Full test_render_report.py suite GREEN.</done>
</task>

</tasks>

<verification>
1. Full Wave 0 audit (all 8 tests must be GREEN by now):
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py -x
   ```
   Expected: 8 PASSED.

2. Full Phase 12 RED-to-GREEN flip check:
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py .claude/skills/google-ad-research/scripts/tests/test_pulse_fetch.py .claude/skills/google-ad-research/scripts/tests/test_competitor_intel.py::test_advertisers_shape_post_phase12 .claude/skills/google-ad-research/scripts/tests/test_merge_signals.py::test_valid_sources_post_phase12 .claude/skills/google-ad-research/scripts/tests/test_merge_signals.py::test_read_tavily_removed .claude/skills/google-ad-research/scripts/tests/test_pulse_synth.py::test_load_news_items_serper_only .claude/skills/google-ad-research/scripts/tests/test_render_report.py::test_competitor_section_joins_webfetch_results -x
   ```
   Expected: all Phase 12 RED tests now GREEN.

3. Full suite regression (must hit Phase 11 baseline + Phase 12 additions, no losses outside intentional Tavily test deletion):
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/ 2>&1 | tail -10
   ```
   Expected: green count >= 245 (Phase 11 had 239; minus ~4 deleted Tavily tests = 235; plus ~12 new Phase 12 tests = 247+).

4. SKILL.md line count check:
   ```
   wc -l .claude/skills/google-ad-research/SKILL.md
   ```
   Expected: ≤500.
</verification>

<success_criteria>
- WFCH-01, WFCH-02, PULSE-12 requirements flip from Pending to Complete-by-test
- All 8 Wave 0 audit tests in test_audit_tavily_removed.py PASS
- test_competitor_section_joins_webfetch_results PASS (skip-guard lifted)
- SKILL.md ≤500 lines and allowed-tools includes WebFetch
- references/phase5-competitor-intel.md Step 19 uses WebFetch pattern (RESEARCH.md Pattern 1 prose)
- references/phase7-niche-pulse.md contains zero Tavily references
- REQUIREMENTS.md PULSE-02 strikethrough + deprecation pointer
- render_report.py exposes _load_competitor_landing_pages (lifts Wave 0 skip-guard sentinel)
- Full pytest suite GREEN — pre-existing tests preserved, new Phase 12 tests all GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-consolidation-drop-tavily/12-04-SUMMARY.md` recording:
- SKILL.md final line count (before/after)
- Exact Step 19 rewrite (paste full markdown)
- render_report.py helper functions added (line numbers + signatures)
- REQUIREMENTS.md PULSE-02 final markdown line
- Wave 0 audit status: all 8 GREEN
- Full suite test count delta vs Phase 11 baseline
</output>
