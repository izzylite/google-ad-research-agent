---
phase: 12-source-consolidation-drop-tavily
plan: 01
type: execute
wave: 1
depends_on: [12-00]
files_modified:
  - .claude/skills/google-ad-research/scripts/tavily_extract.py
  - .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py
  - .claude/skills/google-ad-research/scripts/tests/conftest.py
  - .claude/skills/google-ad-research/scripts/pyproject.toml
  - .claude/skills/google-ad-research/scripts/lib/config.py
  - .env.example
autonomous: true
requirements: [TVLY-01, TVLY-02, TVLY-03, TVLY-04]
must_haves:
  truths:
    - "scripts/tavily_extract.py no longer exists on disk"
    - "tests/test_tavily_extract.py no longer exists on disk"
    - "tavily-python is absent from scripts/pyproject.toml deps; uv.lock regenerated"
    - "TAVILY_API_KEY is absent from .env.example and lib/config.py REQUIRED_KEYS"
    - "conftest.py no longer defines tavily_fixture"
    - "No tavily-* fixture files remain in tests/fixtures/"
    - "test_audit_tavily_removed.py TVLY-section tests all GREEN"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/lib/config.py"
      provides: "REQUIRED_KEYS reduced to ('SERPER_API_KEY',)"
      contains: "REQUIRED_KEYS: tuple[str, ...] = (\"SERPER_API_KEY\",)"
    - path: ".claude/skills/google-ad-research/scripts/pyproject.toml"
      provides: "project.dependencies without tavily-python"
    - path: ".env.example"
      provides: "Single SERPER_API_KEY line; no TAVILY_API_KEY"
  key_links:
    - from: "test_audit_tavily_removed.py"
      to: "scripts/tavily_extract.py + .env.example + lib/config.py + pyproject.toml + conftest.py"
      via: "filesystem + substring scan"
      pattern: "all 4 TVLY-* audit tests GREEN"
---

<objective>
Delete every Tavily artifact this plan owns: the script, the env key, the dep, the test file, the conftest fixture, the fixture files. Flip the TVLY-* audit tests from RED to GREEN.

Purpose: Smallest, cleanest deletion plan in Phase 12. Owns ONLY the artifacts where deletion is total (no refactor — those go to plans 02 + 03). Parallel-safe with plans 02 and 03 because file ownership does not overlap.

Output: 1 file deleted (tavily_extract.py), 1 test file deleted (test_tavily_extract.py), conftest.py + pyproject.toml + lib/config.py + .env.example edited. Wave 0 audit tests TVLY-01..04 flip GREEN.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-consolidation-drop-tavily/12-RESEARCH.md
@.planning/phases/12-source-consolidation-drop-tavily/12-VALIDATION.md

# Files this plan modifies (must read before editing)
@.claude/skills/google-ad-research/scripts/lib/config.py
@.claude/skills/google-ad-research/scripts/pyproject.toml
@.claude/skills/google-ad-research/scripts/tests/conftest.py

# Wave 0 audit test that gates this plan
@.claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py

<interfaces>
<!-- Concrete deletions / edits required -->

scripts/tavily_extract.py: DELETE entire file (162 lines, PEP 723 stdlib + tavily-python).

tests/test_tavily_extract.py: DELETE entire file (131 lines, 4 tests).

tests/conftest.py — current state around line 53-56:
```python
@pytest.fixture
def tavily_fixture(tmp_path):
    ...  # delete this fixture function
```
Remove the entire fixture function (def + body + decorator) and any `tavily_fixture` references in other conftest entries.

scripts/pyproject.toml — current state at line 8:
```toml
"tavily-python>=0.7.24",
```
Remove that line from `project.dependencies` array. Run `uv lock --project .claude/skills/google-ad-research/scripts` to regenerate uv.lock.

scripts/lib/config.py — current state at line 19:
```python
REQUIRED_KEYS: tuple[str, ...] = ("SERPER_API_KEY", "TAVILY_API_KEY")
```
Change to:
```python
REQUIRED_KEYS: tuple[str, ...] = ("SERPER_API_KEY",)
```

.env.example — current state likely contains:
```
SERPER_API_KEY=...
TAVILY_API_KEY=tvly-...
```
Remove the TAVILY_API_KEY line. Locate file at repo root OR at .claude/skills/google-ad-research/.env.example (research says `.env.example line 2`; verify location during execution).

tests/fixtures/ — search for any file matching `*tavily*` (e.g., tavily_extract_2urls.json, tavily_lp_response.json, tavily_news.json per RESEARCH.md). Delete all matches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tavily_extract.py + test_tavily_extract.py + tavily fixture files; strip tavily_fixture from conftest.py</name>
  <files>.claude/skills/google-ad-research/scripts/tavily_extract.py, .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py, .claude/skills/google-ad-research/scripts/tests/conftest.py, .claude/skills/google-ad-research/scripts/tests/fixtures/*tavily*</files>
  <action>
    Step 1 — Verify the Wave 0 audit tests are RED before any deletion:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_extract_deleted .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_test_artifacts_stripped -x
    ```
    Expected: 2 FAILURES (RED state from Wave 0). If GREEN already, the audit isn't actually checking — investigate before proceeding.

    Step 2 — Delete files:
    - `del .claude/skills/google-ad-research/scripts/tavily_extract.py` (PowerShell: `Remove-Item`)
    - `del .claude/skills/google-ad-research/scripts/tests/test_tavily_extract.py`
    - Use Glob tool to find tests/fixtures/*tavily* — delete each match. Per RESEARCH.md the candidates are: tavily_extract_2urls.json, tavily_lp_response.json, tavily_news.json. If file doesn't exist, skip (no error).

    Step 3 — Strip tavily_fixture from conftest.py:
    Read conftest.py first. Locate the `tavily_fixture` function (decorator `@pytest.fixture` + def + body). Remove the entire block including any surrounding blank lines. Also remove any helper imports used ONLY by tavily_fixture (e.g., specific Tavily SDK imports if present — but per RESEARCH.md the fixture is monkeypatch-based, no SDK imports there).

    Step 4 — Re-run Wave 0 audit:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_extract_deleted .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_test_artifacts_stripped -x
    ```
    Expected: 2 PASSED (RED → GREEN flip).

    Do NOT delete other scripts or tests in this task. Plan 02 and Plan 03 own their own deletions.
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_extract_deleted .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_test_artifacts_stripped -x</automated>
  </verify>
  <done>tavily_extract.py absent on disk. test_tavily_extract.py absent on disk. conftest.py no longer defines tavily_fixture. No tests/fixtures/*tavily* files remain. Audit tests test_tavily_extract_deleted and test_tavily_test_artifacts_stripped both PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Strip TAVILY_API_KEY from .env.example + lib/config.py; remove tavily-python from pyproject.toml; regenerate uv.lock</name>
  <files>.env.example, .claude/skills/google-ad-research/scripts/lib/config.py, .claude/skills/google-ad-research/scripts/pyproject.toml, .claude/skills/google-ad-research/scripts/uv.lock</files>
  <action>
    Step 1 — Locate .env.example. Likely at repo root (`.env.example`) per CLAUDE.md's "`.env.example` is committed". If absent at root, check `.claude/skills/google-ad-research/.env.example`. Use Glob with pattern `**/.env.example` if uncertain.

    Step 2 — Edit .env.example: remove the line containing `TAVILY_API_KEY` (typically `TAVILY_API_KEY=tvly-...`). Leave `SERPER_API_KEY` and any other keys intact. Preserve trailing newline.

    Step 3 — Edit lib/config.py line 19:
    Change `REQUIRED_KEYS: tuple[str, ...] = ("SERPER_API_KEY", "TAVILY_API_KEY")` to `REQUIRED_KEYS: tuple[str, ...] = ("SERPER_API_KEY",)`. Note the trailing comma to preserve tuple syntax. Do not modify any other line.

    Step 4 — Edit scripts/pyproject.toml:
    Find `project.dependencies` array. Remove the line `"tavily-python>=0.7.24",`. Preserve order and trailing commas of other deps. Do not modify dev dependencies, tool config, or build-system.

    Step 5 — Regenerate uv.lock:
    ```
    uv lock --project .claude/skills/google-ad-research/scripts
    ```
    This rewrites uv.lock removing tavily-python and any transitive deps no longer needed (httpx is retained — used by serp_fetch). If `uv lock` fails because tavily-python is still referenced somewhere (e.g., an import in scripts that Plan 02 will delete), that's a Plan 02 ordering issue — defer regeneration to Plan 02 closing task. For now, attempt the lock; if it fails with `tavily-python` import resolution error, commit the pyproject.toml change without uv.lock and document the deferral in the task summary.

    NOTE: competitor_intel.py and pulse_fetch.py still import tavily as of this plan's start. They are refactored in Plan 02 and Plan 03 (parallel Wave 1). Running uv sync now would fail. THAT IS FINE — uv lock is a metadata operation, not a sync. If uv lock complains, accept the pyproject.toml edit and let Plan 02/03 close the import path; uv.lock can be regenerated by any subsequent plan task that needs it.

    Step 6 — Re-run audit:
    ```
    uv run --project .claude/skills/google-ad-research/scripts --with pytest pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_env_keys_stripped .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_deps_and_fixtures_stripped -x
    ```
    Expected: both PASS.
  </action>
  <verify>
    <automated>uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_env_keys_stripped .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py::test_tavily_deps_and_fixtures_stripped -x</automated>
  </verify>
  <done>.env.example contains no TAVILY_API_KEY line. lib/config.py REQUIRED_KEYS == ("SERPER_API_KEY",). scripts/pyproject.toml project.dependencies has no `tavily-python` entry. Audit tests test_tavily_env_keys_stripped + test_tavily_deps_and_fixtures_stripped both PASS. uv.lock either regenerated successfully OR documented as deferred to Plan 02 closing (operator-acceptable either way at this plan boundary).</done>
</task>

</tasks>

<verification>
1. Run full TVLY-* audit subset:
   ```
   uv run --project .claude/skills/google-ad-research/scripts --with pytest --with respx --with python-dotenv --with python-slugify --with tabulate pytest .claude/skills/google-ad-research/scripts/tests/test_audit_tavily_removed.py -k "tavily_extract_deleted or tavily_env_keys_stripped or tavily_deps_and_fixtures_stripped or tavily_test_artifacts_stripped" -x
   ```
   Expected: 4 PASSED.

2. Confirm non-overlap with Plan 02 + Plan 03:
   - `scripts/competitor_intel.py` is NOT in this plan's files_modified — Plan 02 owns it.
   - `scripts/pulse_fetch.py` + `scripts/pulse_synth.py` are NOT in this plan's files_modified — Plan 03 owns them.
   - `scripts/merge_signals.py` is NOT in this plan's files_modified — Plan 02 owns it.

3. Verify other Wave 0 audit tests still RED (Plans 02/03/04 haven't run yet):
   - test_competitor_intel_no_tavily_import: still RED
   - test_skill_md_uses_webfetch_for_step19: still RED
   - test_phase7_docs_tavily_free: still RED
   - test_repo_grep_tavily_clean: still RED (competitor_intel.py + pulse_fetch.py + pulse_synth.py + SKILL.md + references still contain `tavily`)
   This is correct — Plan 01 only owns the TVLY-* subset.
</verification>

<success_criteria>
- All 4 requirements (TVLY-01, TVLY-02, TVLY-03, TVLY-04) flip from Pending to Complete-by-test
- 4 Wave 0 audit tests (test_tavily_extract_deleted, test_tavily_env_keys_stripped, test_tavily_deps_and_fixtures_stripped, test_tavily_test_artifacts_stripped) PASS
- 1 script file deleted (tavily_extract.py)
- 1 test file deleted (test_tavily_extract.py)
- 0-3 fixture files deleted (whatever `*tavily*` matched in tests/fixtures/)
- 3 edited files committed (.env.example, lib/config.py, scripts/pyproject.toml)
- uv.lock either regenerated cleanly OR documented as deferred (Plan 02 will retry)
- Pre-existing GREEN test suite unaffected outside the deleted test_tavily_extract.py
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-consolidation-drop-tavily/12-01-SUMMARY.md` recording:
- Files deleted (exact paths)
- Files edited (exact paths + the precise diff for lib/config.py and pyproject.toml)
- uv.lock outcome (regenerated cleanly vs deferred — if deferred, name which Plan 02/03 task will retry)
- Audit test status: TVLY-01..04 GREEN, repo-wide audit still RED (expected pending Plans 02-04)
</output>
