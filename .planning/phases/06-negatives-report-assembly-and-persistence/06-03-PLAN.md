---
phase: 06-negatives-report-assembly-and-persistence
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - .claude/skills/google-ad-research/scripts/render_report.py
autonomous: true
requirements:
  - RPRT-01
  - RPRT-02
  - RPRT-03
  - RPRT-05
  - PRST-01

must_haves:
  truths:
    - "render_report.py writes report.md containing all 5 required section headings"
    - "report.md 'How to Read This Report' section contains verbatim disclaimer text referencing signal_count and source_diversity"
    - "All keyword table cells are routed through escape_md_cell() — no raw string from upstream JSON appears directly in a table row"
    - "render_report.py writes report.json with the canonical v1 schema (meta, brief, keywords, clusters, competitor_intel, negatives)"
    - "competitor_intel section in report.md is optional — if raw/competitor-intel.json absent, section renders as empty gracefully"
    - "After CLI run, run_dir contains both report.md and report.json alongside pre-existing brief.md and raw/"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/render_report.py"
      provides: "Assembles report.md + report.json from all upstream JSONs"
      exports: ["render_full_report", "build_report_json"]
  key_links:
    - from: "render_report.py"
      to: "lib/io.py"
      via: "from lib.io import escape_md_cell"
      pattern: "escape_md_cell"
    - from: "render_report.py"
      to: "{run_dir}/ranked.json"
      via: "json.loads((run_dir / 'ranked.json').read_text(...))"
      pattern: "ranked\\.json"
    - from: "render_report.py"
      to: "{run_dir}/report.md"
      via: "(run_dir / 'report.md').write_text(..., newline='\\n')"
      pattern: "report\\.md"
    - from: "render_report.py"
      to: "{run_dir}/report.json"
      via: "(run_dir / 'report.json').write_text(json.dumps(..., indent=2), newline='\\n')"
      pattern: "report\\.json"
---

<objective>
Implement render_report.py: reads ranked.json, clusters.json, negatives.json, brief.md, and optional raw/competitor-intel.json from a run directory; writes report.md and report.json.

Purpose: Produce the two operator-facing deliverables. All markdown table cells must pass through escape_md_cell(). report.json uses the canonical v1 schema for future run-diff tooling.
Output: scripts/render_report.py with render_full_report(), build_report_json(), and CLI entry point.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-RESEARCH.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-01-SUMMARY.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-02-SUMMARY.md

<interfaces>
<!-- Functions this script must export (imported by test_render_report.py): -->
```python
def render_full_report(
    ranked: list[dict],
    clusters_data: dict,       # clusters.json top-level object
    competitor_intel: dict,    # raw/competitor-intel.json (may be {})
    negatives: list[dict],
    brief_text: str,
    run_dir: Path,
    *,
    top_n: int = 100,
) -> str:
    """Returns full report.md content as a string."""
    ...

def build_report_json(
    ranked: list[dict],
    clusters_data: dict,
    competitor_intel: dict,
    negatives: list[dict],
    brief_text: str,
    run_dir: Path,
) -> dict:
    """Returns canonical v1 report.json dict (not serialized)."""
    ...
```

<!-- report.md section headings (must appear in this order): -->
<!-- 1. "## How to Read This Report" -->
<!-- 2. "## Ranked Keywords" -->
<!-- 3. "## Ad Group Clusters" -->
<!-- 4. "## Competitor Ad Copy" -->
<!-- 5. "## Negative Keywords" -->
<!--    with subsections: "### Strong Negatives", "### Considered Negatives", "### Investigate Negatives" -->

<!-- report.json canonical schema top-level keys: -->
<!-- meta (run_id, brief_slug, generated_at, version:"v1") -->
<!-- brief (industry, product, location, language, audience — parsed from brief.md) -->
<!-- keywords (ranked list with cluster_id added at render time) -->
<!-- clusters -->
<!-- competitor_intel -->
<!-- negatives -->

<!-- cluster_id derivation: for each keyword in ranked, scan clusters_data["clusters"] -->
<!-- to find which cluster.keywords list contains it; assign that cluster's name as cluster_id -->
<!-- If not found, cluster_id = null -->

<!-- brief.md parsing: extract field values from "**Field:** value" markdown pattern -->
<!-- Fields: industry, product, location, language, audience -->
<!-- Use regex: r"\*\*(\w+)\*\*:\s*(.+)" to extract key-value pairs -->

<!-- tabulate import: from tabulate import tabulate (tablefmt="github") -->
<!-- escape_md_cell import: from lib.io import escape_md_cell -->

<!-- HOW_TO_READ verbatim constant (copy exactly from research Pattern 3 code examples): -->
<!-- starts with "## How to Read This Report" -->
<!-- mentions "signal_count" and "source_diversity" and "Google Keyword Planner" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement render_report.py (sections + JSON schema + CLI)</name>
  <files>.claude/skills/google-ad-research/scripts/render_report.py</files>
  <behavior>
    - render_full_report() output contains "## How to Read This Report"
    - render_full_report() output contains "signal_count" (from the disclaimer)
    - render_full_report() output contains "## Ranked Keywords"
    - render_full_report() output contains "## Ad Group Clusters"
    - render_full_report() output contains "## Competitor Ad Copy"
    - render_full_report() output contains "## Negative Keywords"
    - render_full_report() with a keyword containing "|" → output contains r"\|" in a table row
    - build_report_json() returns dict with all keys: meta, brief, keywords, clusters, competitor_intel, negatives
    - build_report_json() meta["version"] == "v1"
    - build_report_json() keywords[0] has "cluster_id" key (value may be null if cluster lookup fails)
    - CLI with valid --run-dir → exits 0, report.md and report.json both written
    - CLI with missing ranked.json → exits 3
  </behavior>
  <action>
Write render_report.py at scripts/render_report.py with PEP 723 inline metadata:
```
# /// script
# requires-python = ">=3.13"
# dependencies = [
#     "tabulate>=0.9.0",
#     "python-dotenv>=1.0",
# ]
# ///
```

Implement these functions in order:

1. HOW_TO_READ constant (verbatim from research Code Examples section)

2. TIER_ORDER, TIER_DESCRIPTIONS constants (from research)

3. render_keyword_table(ranked, top_n=100) → str
   - Uses tabulate with tablefmt="github"
   - Passes keyword and intent through escape_md_cell(); match_type, signal_count, source_diversity, score as-is (numeric strings safe)
   - Headers: ["Keyword", "Intent", "Match Type", "Signals", "Src Div", "Score"]

4. render_clusters_section(clusters_data) → str
   - Heading "## Ad Group Clusters"
   - One "### {cluster_name}" subsection per cluster
   - Keywords as bullet points: "- keyword"
   - Each keyword and cluster name escaped via escape_md_cell()

5. render_competitor_section(competitor_intel) → str
   - Heading "## Competitor Ad Copy"
   - If competitor_intel is empty dict → "No competitor ad copy extracted for this run."
   - Otherwise: one "### {cluster_name}" subsection per cluster key in competitor_intel.get("clusters", {})
   - Ads as sub-bullets with headline, CTA, offer if available; each string through escape_md_cell()

6. render_negatives_section(negatives) → str (exact pattern from research Pattern code example)

7. _build_cluster_index(clusters_data) → dict[str, str]
   - Returns mapping of keyword.lower() → cluster_name for cluster_id lookup

8. _parse_brief_fields(brief_text) → dict[str, str]
   - Extracts industry, product, location, language, audience from brief.md markdown

9. render_full_report(ranked, clusters_data, competitor_intel, negatives, brief_text, run_dir, *, top_n=100) → str
   - Assembles: header block + HOW_TO_READ + "## Ranked Keywords\n" + render_keyword_table + render_clusters_section + render_competitor_section + render_negatives_section
   - Header block: run_dir name, generated_at UTC ISO timestamp, brief_slug (from run_dir.name parsing)

10. build_report_json(ranked, clusters_data, competitor_intel, negatives, brief_text, run_dir) → dict
    - Builds canonical v1 dict per research Pattern 5 schema
    - Adds cluster_id to each keyword using _build_cluster_index(); null if not found

11. main() → int
    - argparse --run-dir (required), --top-n (default 100)
    - Loads required files (ranked.json, clusters.json, negatives.json, brief.md) — exit 3 if any missing or invalid JSON
    - Loads raw/competitor-intel.json if present (optional)
    - Calls render_full_report() and build_report_json()
    - Writes report.md (LF newlines, utf-8) and report.json (indent=2, LF newlines, utf-8)
    - Prints JSON with report_md path, report_json path, keywords_in_report count
    - Returns 0

Anti-pattern: Do NOT mutate ranked.json. cluster_id enrichment is report.json only.
Anti-pattern: Every table cell string MUST pass through escape_md_cell() — no raw JSON strings in tabulate rows.
  </action>
  <verify>
    <automated>cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_render_report.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test_render_report.py tests GREEN (test_run_folder_complete and test_index_append may still skip if update_index not yet available — that is acceptable). render_full_report() and build_report_json() tests all pass.</done>
</task>

</tasks>

<verification>
```bash
cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_render_report.py -v 2>&1 | tail -15
```
RPRT-01, RPRT-02, RPRT-03, RPRT-04 tests GREEN. PRST-01 test GREEN (report.md + report.json written to tmp_run_dir).
</verification>

<success_criteria>
- render_report.py exists with render_full_report() and build_report_json() exported
- All 5 section headings present in render_full_report() output
- HOW_TO_READ verbatim text present (signal_count, source_diversity, Google Keyword Planner)
- report.json has version:"v1" and all required top-level keys
- All table cells route through escape_md_cell()
- CLI exits 0 on success, 3 on missing files
</success_criteria>

<output>
After completion, create `.planning/phases/06-negatives-report-assembly-and-persistence/06-03-SUMMARY.md`
</output>
