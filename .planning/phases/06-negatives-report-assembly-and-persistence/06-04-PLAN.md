---
phase: 06-negatives-report-assembly-and-persistence
plan: "04"
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - .claude/skills/google-ad-research/scripts/update_index.py
autonomous: true
requirements:
  - PRST-02

must_haves:
  truths:
    - "update_index.py appends one correctly-formatted row to .runs/INDEX.md on each invocation"
    - "update_index.py creates INDEX.md with header when the file does not yet exist"
    - "Calling update_index.py twice appends two rows but the header appears exactly once"
    - "Date and slug are parsed from run_dir.name — no re-derivation from brief fields"
    - "Industry field in INDEX.md row is sanitized via escape_md_cell()"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/update_index.py"
      provides: "Append-only .runs/INDEX.md updater"
      exports: ["append_run_to_index"]
  key_links:
    - from: "update_index.py"
      to: ".runs/INDEX.md"
      via: "index_path.open('a', encoding='utf-8') — append mode only"
      pattern: "open.*\"a\""
    - from: "update_index.py"
      to: "lib/io.py"
      via: "from lib.io import escape_md_cell"
      pattern: "escape_md_cell"
---

<objective>
Implement update_index.py: appends one row to .runs/INDEX.md after each completed run, creating the file with a header on first use.

Purpose: Provide operators with a durable, human-readable audit trail of past research runs without requiring directory listing.
Output: scripts/update_index.py with append_run_to_index() and CLI.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-negatives-report-assembly-and-persistence/06-RESEARCH.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-02-SUMMARY.md

<interfaces>
<!-- Function this script must export (imported by test_render_report.py::test_index_append): -->
```python
def append_run_to_index(
    runs_root: Path,
    run_dir: Path,
    industry: str,
    status: str = "complete",
) -> None:
    """Appends one row to runs_root/INDEX.md. Creates file + header on first call."""
    ...
```

<!-- INDEX.md header constant (exact format — do not alter column names): -->
```markdown
# Run History

| Date | Slug | Industry | Status |
|------|------|----------|--------|
```

<!-- Row parsing from run_dir.name: -->
<!-- run_dir.name format: "2026-05-08T143024Z-grocery-delivery-uk" -->
<!-- date = name[:10]               → "2026-05-08" -->
<!-- slug = name[18:] if len(name) > 18 else name   → "grocery-delivery-uk" -->
<!-- (This is the same derivation as Pattern 6 in the research doc — do not re-derive) -->

<!-- CLI contract: -->
<!-- argv: --run-dir PATH (required) -->
<!-- reads: {run_dir}/brief.md to extract industry field -->
<!-- writes: {run_dir.parent}/INDEX.md  (runs_root = run_dir.parent) -->
<!-- stdout: JSON {"index_path": "...", "run_slug": "..."} -->
<!-- exit 0: always (non-fatal; missing brief.md → industry = "unknown") -->

<!-- Pitfall to avoid: do NOT write header on every call. -->
<!-- Check index_path.exists() before writing header. -->
<!-- If file exists: open in "a" mode, write row only. -->
<!-- If file does not exist: write_text(HEADER + row) then close. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement update_index.py (append-only INDEX.md writer)</name>
  <files>.claude/skills/google-ad-research/scripts/update_index.py</files>
  <behavior>
    - First call with a new INDEX.md path: file created with header (4 lines) + 1 data row
    - Second call appending to same INDEX.md: header still appears exactly once; file now has 5 lines (header + separator + 2 data rows)
    - Row contains correct date (first 10 chars of run_dir.name) and slug (chars 18+ of run_dir.name)
    - Industry column passes through escape_md_cell() — a pipe in industry string becomes \|
    - Missing brief.md: industry falls back to "unknown", exit 0 (non-fatal)
    - run_dir.name shorter than 18 chars: slug = full name (no IndexError)
  </behavior>
  <action>
Write update_index.py at scripts/update_index.py with PEP 723 inline metadata (`dependencies = []`).

Implement:

1. INDEX_HEADER constant (exact markdown from research Pattern 6 — 4 lines including trailing newline)

2. append_run_to_index(runs_root, run_dir, industry, status="complete"):
   - Derive date = run_dir.name[:10]
   - Derive slug = run_dir.name[18:] if len(run_dir.name) > 18 else run_dir.name
   - Build row: f"| {date} | {slug} | {escape_md_cell(industry)} | {status} |\n"
   - index_path = runs_root / "INDEX.md"
   - If not index_path.exists(): index_path.write_text(INDEX_HEADER + row, encoding="utf-8")
   - Else: open in "a" mode and write row only

3. _extract_industry(brief_path) → str:
   - Reads brief.md, extracts "**industry:** value" or "**Industry:** value" via regex
   - Returns value stripped; returns "unknown" if not found or file missing

4. main() → int:
   - argparse --run-dir (required)
   - run_dir = Path(args.run_dir)
   - runs_root = run_dir.parent
   - industry = _extract_industry(run_dir / "brief.md")
   - append_run_to_index(runs_root, run_dir, industry)
   - Print JSON {"index_path": str(runs_root / "INDEX.md"), "run_slug": ...}
   - Return 0

Anti-pattern: Do NOT use read-modify-write on INDEX.md. Open in "a" mode for appends.
Anti-pattern: Do NOT write header when file already exists.
  </action>
  <verify>
    <automated>cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_render_report.py::test_index_append -v 2>&1 | tail -10</automated>
  </verify>
  <done>test_index_append passes GREEN. Header appears once after two invocations. Date and slug parsed correctly from run_dir.name.</done>
</task>

</tasks>

<verification>
```bash
cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_render_report.py -v 2>&1 | tail -15
```
test_index_append GREEN. All other test_render_report.py tests remain GREEN (no regressions).
</verification>

<success_criteria>
- update_index.py exists with append_run_to_index() exported
- INDEX.md header written exactly once regardless of invocation count
- Date/slug parsed from run_dir.name without re-deriving from brief fields
- escape_md_cell() applied to industry column
- CLI exits 0 always; non-fatal on missing brief.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-negatives-report-assembly-and-persistence/06-04-SUMMARY.md`
</output>
