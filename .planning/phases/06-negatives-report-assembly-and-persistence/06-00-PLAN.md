---
phase: 06-negatives-report-assembly-and-persistence
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/google-ad-research/scripts/pyproject.toml
  - .claude/skills/google-ad-research/scripts/tests/test_negatives.py
  - .claude/skills/google-ad-research/scripts/tests/test_render_report.py
  - .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_valid.json
  - .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_with_collision.json
autonomous: true
requirements:
  - NEGT-01
  - NEGT-02
  - NEGT-03
  - RPRT-01
  - RPRT-02
  - RPRT-03
  - RPRT-04
  - PRST-01
  - PRST-02
# NOTE: This is the test-infra plan. It creates RED stubs that verify the above
# requirements via automated tests. No production code is written here — the
# requirements are satisfied in full by plans 06-01 through 06-04, but the stubs
# are the test contract for all of them and therefore listed here too.

must_haves:
  truths:
    - "pytest --collect-only finds all 14 RED stub test functions across both new test files"
    - "All new tests skip (not error) when generate_negatives.py and render_report.py do not exist"
    - "tabulate>=0.9.0 is declared in scripts/pyproject.toml dependencies"
    - "Both fixture files parse as valid JSON"
  artifacts:
    - path: ".claude/skills/google-ad-research/scripts/tests/test_negatives.py"
      provides: "RED stubs for NEGT-01/02/03 and escape_md_cell (RPRT-04)"
      contains: "MODULE_MISSING guard, 8 test functions"
    - path: ".claude/skills/google-ad-research/scripts/tests/test_render_report.py"
      provides: "RED stubs for RPRT-01/02/03, PRST-01, PRST-02"
      contains: "MODULE_MISSING guard, 6 test functions"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/negatives_valid.json"
      provides: "6-row fixture spanning all 3 tiers and all 6 categories"
    - path: ".claude/skills/google-ad-research/scripts/tests/fixtures/negatives_with_collision.json"
      provides: "Fixture with one keyword that also appears in ranked_phase3.json"
  key_links:
    - from: "test_negatives.py"
      to: "generate_negatives.py"
      via: "MODULE_MISSING guard using try/import"
      pattern: "MODULE_MISSING"
    - from: "test_render_report.py"
      to: "render_report.py"
      via: "MODULE_MISSING guard using try/import"
      pattern: "MODULE_MISSING"
---

<objective>
Create the Wave 0 test scaffold for Phase 6: RED stub test files, fixtures, and the tabulate dependency addition.

Purpose: Establish Nyquist-compliant test coverage before any production code exists. All tests must skip (not fail with ImportError) while generate_negatives.py and render_report.py are absent, then turn RED when those modules exist but functions are stubs.
Output: 2 test files (14 stub functions total), 2 fixture files, updated pyproject.toml.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-RESEARCH.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-VALIDATION.md

<interfaces>
<!-- Existing test pattern from the project (from test_validate_clusters.py / test_rank_keywords.py) -->
<!-- All prior phase tests use a MODULE_MISSING guard like this: -->

```python
# Guard pattern used in every prior phase test file
try:
    sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
    import <module_name>
    MODULE_MISSING = False
except ImportError:
    MODULE_MISSING = True

pytestmark = pytest.mark.skipif(MODULE_MISSING, reason="<module>.py not yet implemented")
```

<!-- escape_md_cell tests do NOT use a module guard because lib.io already exists -->
<!-- They import directly: from lib.io import escape_md_cell -->

<!-- Existing fixture in tests/fixtures/: ranked_phase3.json -->
<!-- ranked_phase3.json rows have shape: {"keyword": "...", "score": N, ...} -->
<!-- At minimum: one row with "keyword": "grocery delivery" (or similar) -->
<!-- negatives_with_collision.json must include a keyword that matches one in ranked_phase3.json -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabulate to pyproject.toml</name>
  <files>.claude/skills/google-ad-research/scripts/pyproject.toml</files>
  <action>
Add `"tabulate>=0.9.0"` to the `dependencies` list in scripts/pyproject.toml. The current list ends with `"python-slugify>=8.0"`. Add tabulate as the next entry. Do not change any other field.

After editing, run:
```bash
cd ".claude/skills/google-ad-research/scripts" && uv lock
```
to update uv.lock so the dependency is resolvable.
  </action>
  <verify>
    <automated>cd ".claude/skills/google-ad-research" && uv run --project scripts/ python -c "import tabulate; print(tabulate.__version__)"</automated>
  </verify>
  <done>tabulate version string prints without error; pyproject.toml contains tabulate>=0.9.0 in dependencies list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED stub test files and fixtures</name>
  <files>
    .claude/skills/google-ad-research/scripts/tests/test_negatives.py,
    .claude/skills/google-ad-research/scripts/tests/test_render_report.py,
    .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_valid.json,
    .claude/skills/google-ad-research/scripts/tests/fixtures/negatives_with_collision.json
  </files>
  <behavior>
    - test_negatives.py::test_validate_negatives_accepts_valid_rows — calls validate_negatives() with 6-row valid fixture; asserts len(valid) == 6 and len(errors) == 0
    - test_negatives.py::test_validate_negatives_rejects_bad_tier — calls validate_negatives() with one row having tier="Bad"; asserts row lands in errors
    - test_negatives.py::test_validate_negatives_rejects_bad_category — calls validate_negatives() with one row having category="made-up"; asserts row lands in errors
    - test_negatives.py::test_three_tiers_present — calls validate_negatives() with negatives_valid.json; asserts all 3 tiers appear in valid output
    - test_negatives.py::test_category_enum_valid — asserts all categories in negatives_valid.json are members of VALID_CATEGORIES
    - test_negatives.py::test_dedupe_removes_collision — loads negatives_with_collision.json + ranked_phase3.json; calls dedupe_negatives(); asserts the colliding keyword is absent from deduped list and present in collisions list
    - test_negatives.py::test_dedupe_no_false_positives — dedupes a list where no keyword overlaps positives; asserts deduped length equals input length
    - test_negatives.py::test_escape_md_cell_pipe — imports from lib.io (no module guard); asserts escape_md_cell("Free | Same") == r"Free \| Same" (this test must FAIL until Task in 06-02 adds the function)
    - test_render_report.py::test_report_md_sections — calls render_full_report() with all fixtures; asserts all 5 required section headings present in output string
    - test_render_report.py::test_report_json_schema — calls build_report_json() with fixtures; asserts top-level keys: meta, brief, keywords, clusters, competitor_intel, negatives; asserts meta["version"] == "v1"
    - test_render_report.py::test_how_to_read_present — asserts "## How to Read This Report" and "signal_count" both appear in render_full_report() output
    - test_render_report.py::test_pipe_escaped — render_full_report() called with a keyword containing a literal pipe; assert r"\|" appears in output and "|" does not appear inside a table row
    - test_render_report.py::test_run_folder_complete — after render_report.main() runs against a tmp_run_dir fixture, assert report.md and report.json both exist in tmp_run_dir
    - test_render_report.py::test_index_append — calls update_index.append_run_to_index(); asserts INDEX.md contains the date and slug; calls again; asserts header appears exactly once
  </behavior>
  <action>
Write test_negatives.py with 8 test functions. Apply the MODULE_MISSING guard (try: import generate_negatives / except ImportError: MODULE_MISSING = True) and `pytestmark = pytest.mark.skipif(MODULE_MISSING, ...)`. The escape_md_cell tests do NOT use the module guard — they import `from lib.io import escape_md_cell` directly; add a separate skipif for ImportError from lib.io using pytest.importorskip pattern or a second guard constant.

Write test_render_report.py with 6 test functions. Apply MODULE_MISSING guard for both `render_report` and `update_index` (two separate guards; skip if either is missing). Add a `tmp_run_dir` pytest fixture (tmp_path-based) that creates a minimal run folder with all required input files copied from fixtures/.

Write negatives_valid.json: JSON array of 6 objects. Must contain exactly 2 Strong rows, 2 Considered rows, 2 Investigate rows. Must cover all 6 valid categories exactly once (distribute the 6th across tiers as needed). Every row has "keyword", "tier", "category", "justification".

Write negatives_with_collision.json: Same schema. Include at least 4 rows. One row's "keyword" field must exactly match (case-insensitive) a keyword present in tests/fixtures/ranked_phase3.json (read that file to find a valid keyword to reuse). The other rows must not collide.

Verify collect-only shows all 14 tests before committing.
  </action>
  <verify>
    <automated>cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_negatives.py scripts/tests/test_render_report.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest --collect-only shows 14 test items across both files. Running the suite shows all tests either SKIP (module missing) or FAIL on escape_md_cell import (lib.io does not yet have it). Zero ERROR outcomes.</done>
</task>

</tasks>

<verification>
```bash
cd ".claude/skills/google-ad-research" && uv run --project scripts/ -m pytest scripts/tests/test_negatives.py scripts/tests/test_render_report.py -v 2>&1 | tail -20
```
Expected: 14 items collected, all SKIP or XFAIL — no ERRORs.
</verification>

<success_criteria>
- tabulate>=0.9.0 in pyproject.toml and importable via uv run
- 14 test stubs collected, all skipping cleanly (no ImportError tracebacks leaking)
- negatives_valid.json: 6 rows, 3 tiers, 6 categories, valid JSON
- negatives_with_collision.json: ≥4 rows, exactly 1 keyword matches ranked_phase3.json
</success_criteria>

<output>
After completion, create `.planning/phases/06-negatives-report-assembly-and-persistence/06-00-SUMMARY.md`
</output>
