---
phase: 06-negatives-report-assembly-and-persistence
plan: "05"
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - .claude/skills/google-ad-research/SKILL.md
  - .claude/skills/google-ad-research/references/phase6-negatives-report.md
autonomous: true
requirements:
  - NEGT-01
  - NEGT-02
  - NEGT-03
  - RPRT-01
  - RPRT-02
  - RPRT-03
  - RPRT-04
  - RPRT-05
  - PRST-01
  - PRST-02

must_haves:
  truths:
    - "SKILL.md Phase 5 stop message no longer says 'Phase 6 is not yet available' — it now points to Phase 6"
    - "SKILL.md contains a Phase 6 entry that loads references/phase6-negatives-report.md via Read tool"
    - "references/phase6-negatives-report.md contains Steps 21-26 with exact CLI commands and gate conditions"
    - "Step 21 instructs the LLM to generate negatives as a JSON array and write via Write tool"
    - "Step 22 runs generate_negatives.py with --run-dir and surfaces exit 1 warnings to operator"
    - "Step 23 runs render_report.py with --run-dir and verifies report.md + report.json exist"
    - "Step 24 runs update_index.py with --run-dir and verifies INDEX.md updated"
    - "Step 26 issues a hard STOP with final summary — no further steps follow"
    - "SKILL.md remains at or below 500 lines after the edit"
  artifacts:
    - path: ".claude/skills/google-ad-research/SKILL.md"
      provides: "Phase 6 entry pointing to references/phase6-negatives-report.md"
    - path: ".claude/skills/google-ad-research/references/phase6-negatives-report.md"
      provides: "Steps 21-26 full operator instructions"
  key_links:
    - from: "SKILL.md Phase 6 section"
      to: "references/phase6-negatives-report.md"
      via: "Read tool instruction: 'Load it with the Read tool when entering Phase 6'"
      pattern: "phase6-negatives-report\\.md"
    - from: "Step 22"
      to: "generate_negatives.py"
      via: "uv run CLI command with --run-dir"
      pattern: "generate_negatives\\.py"
    - from: "Step 23"
      to: "render_report.py"
      via: "uv run CLI command with --run-dir"
      pattern: "render_report\\.py"
    - from: "Step 24"
      to: "update_index.py"
      via: "uv run CLI command with --run-dir"
      pattern: "update_index\\.py"
---

<objective>
Wire Phase 6 into SKILL.md by updating the Phase 5 stop message and adding a Phase 6 section that lazy-loads references/phase6-negatives-report.md. Write Steps 21-26 in that reference file.

Purpose: Make the skill runnable end-to-end. Without this plan, an operator completes Phase 5 and hits a dead end. Steps 21-26 are the operator-facing instructions for negatives generation, validation, report rendering, index update, and final STOP.
Output: Updated SKILL.md (≤500 lines) + new references/phase6-negatives-report.md with Steps 21-26.
</objective>

<execution_context>
@C:/Users/Izzy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Izzy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-RESEARCH.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-03-SUMMARY.md
@.planning/phases/06-negatives-report-assembly-and-persistence/06-04-SUMMARY.md
@.claude/skills/google-ad-research/SKILL.md
@.claude/skills/google-ad-research/references/phase5-competitor-intel.md

<interfaces>
<!-- SKILL.md line budget rule from CLAUDE.md: SKILL.md must stay ≤500 lines -->
<!-- Current SKILL.md: 473 lines -->
<!-- Phase 6 section in SKILL.md must be SHORT — just enough to trigger the lazy-load -->
<!-- Full step text goes in references/phase6-negatives-report.md -->

<!-- Pattern to follow (identical to how Phase 5 is handled at the bottom of SKILL.md): -->
<!-- Phase 5 in SKILL.md: one paragraph + "See .../phase5-competitor-intel.md ... Load it with the Read tool" -->
<!-- Phase 6 should follow the same pattern -->

<!-- End of Phase 5 step in references/phase5-competitor-intel.md (Step 20 stop message): -->
<!-- Currently says: "Phase 6 (report assembly) is not yet available in this skill." -->
<!-- MUST be updated to: "Phase 6 (report assembly) begins at Step 21. Load references/phase6-negatives-report.md." -->

<!-- Step 21 (negatives generation) exact gate condition: -->
<!-- "Do not advance to Step 22 until {run_dir}/negatives.json exists with ≥ 1 entry per tier." -->

<!-- Step 22 CLI: -->
<!-- uv run "${CLAUDE_SKILL_DIR}/scripts/generate_negatives.py" --run-dir "{run_dir}" -->
<!-- Parse stdout JSON and exit code. Exit 0: continue. Exit 1: surface warnings, ask operator to proceed/fix. Exit 3: stop. -->

<!-- Step 23 CLI: -->
<!-- uv run "${CLAUDE_SKILL_DIR}/scripts/render_report.py" --run-dir "{run_dir}" -->
<!-- Gate: both {run_dir}/report.md and {run_dir}/report.json exist -->

<!-- Step 24 CLI: -->
<!-- uv run "${CLAUDE_SKILL_DIR}/scripts/update_index.py" --run-dir "{run_dir}" -->
<!-- Gate: .runs/INDEX.md updated (check via read or existence) -->

<!-- Step 25 (operator review): -->
<!-- LLM reads report.md with Read tool, confirms all sections present -->

<!-- Step 26 (final summary + STOP): -->
<!-- LLM summarizes: run path, report.md path, keyword count, cluster count, negative count -->
<!-- Hard STOP — no further steps -->

<!-- Negatives JSON schema reminder for Step 21 prompt: -->
<!-- Array of objects with: keyword, tier (Strong|Considered|Investigate), -->
<!-- category (6 valid values), justification -->
<!-- Must include ≥1 row per tier, ≥1 row per category -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write references/phase6-negatives-report.md (Steps 21-26)</name>
  <files>.claude/skills/google-ad-research/references/phase6-negatives-report.md</files>
  <action>
Create references/phase6-negatives-report.md with the full Phase 6 step instructions. Structure mirrors phase5-competitor-intel.md.

Include these 6 steps in order:

**Step 21: Generate negative keywords**
Instruction to the LLM: Read {run_dir}/ranked.json to identify keyword themes; read {run_dir}/brief.md for brand positioning. Generate a JSON array of negative keyword objects. Each object must have: keyword, tier (Strong/Considered/Investigate), category (one of the 6 valid categories), justification. Rules: ≥1 row per tier, ≥1 row per category, no keyword from the ranked.json positive pool. Write the array to {run_dir}/negatives.json using the Write tool.
Gate: "Do not advance to Step 22 until {run_dir}/negatives.json exists with ≥ 1 entry per tier."

**Step 22: Validate and deduplicate negatives**
Run: `uv run "${CLAUDE_SKILL_DIR}/scripts/generate_negatives.py" --run-dir "{run_dir}"`
Parse stdout JSON. Exit 0: continue. Exit 1: surface warnings (enum errors fixed, collisions removed, missing categories) and ask operator: "Proceed with these warnings, or fix? (proceed/fix)". If fix: re-run Step 21 for affected rows. Exit 3: surface fatal error; do not proceed.
Gate: "Do not advance to Step 23 until validator exits 0 (or operator accepts exit 1 warnings)."

**Step 23: Render report**
Run: `uv run "${CLAUDE_SKILL_DIR}/scripts/render_report.py" --run-dir "{run_dir}"`
Parse stdout JSON for report_md path, report_json path, keywords_in_report count.
Gate: "Do not advance to Step 24 until {run_dir}/report.md and {run_dir}/report.json both exist."

**Step 24: Update run index**
Run: `uv run "${CLAUDE_SKILL_DIR}/scripts/update_index.py" --run-dir "{run_dir}"`
Gate: "Do not advance to Step 25 until .runs/INDEX.md has been updated."

**Step 25: Operator review**
Load report.md via Read tool. Confirm these sections are present: "How to Read This Report", "Ranked Keywords", "Ad Group Clusters", "Competitor Ad Copy", "Negative Keywords". Summarize findings to operator.
Gate: "Operator confirms report is satisfactory."

**Step 26: Final summary and STOP**
Tell the operator the complete run summary:
- Run path: `{run_dir}`
- Report: `{run_dir}/report.md`, `{run_dir}/report.json`
- Keywords in report: {keywords_in_report}
- Clusters: {cluster_count}
- Negatives: {total negatives — Strong: N, Considered: N, Investigate: N}
- Index updated: `.runs/INDEX.md`
"**STOP. The Google Ad Research skill workflow is complete for this run.**"
  </action>
  <verify>
    <automated>python -c "import pathlib; content = pathlib.Path('.claude/skills/google-ad-research/references/phase6-negatives-report.md').read_text(); steps = [f'Step {i}' for i in range(21, 27)]; missing = [s for s in steps if s not in content]; print('Missing:', missing or 'None')"</automated>
  </verify>
  <done>references/phase6-negatives-report.md exists and contains all 6 step headings (Step 21 through Step 26). Step 22 contains the generate_negatives.py CLI command. Step 26 contains "STOP".</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md — fix Phase 5 stop + add Phase 6 lazy-load entry</name>
  <files>
    .claude/skills/google-ad-research/SKILL.md,
    .claude/skills/google-ad-research/references/phase5-competitor-intel.md
  </files>
  <action>
Two edits:

**Edit 1 — references/phase5-competitor-intel.md (Step 20 stop message):**
Find the line: `"Phase 6 (report assembly) is not yet available in this skill."`
Replace with: `"Phase 6 (report assembly) begins at Step 21. Load \`.claude/skills/google-ad-research/references/phase6-negatives-report.md\` with the Read tool when entering Phase 6."`
Remove the `**STOP. Do not proceed to any Phase 6+ activity.**` line that follows (Phase 6 is now available).

**Edit 2 — SKILL.md:**
At the bottom of SKILL.md, find the Phase 5 section entry (currently the last section). Add a new section immediately after:

```markdown
## Phase 6: Negatives, Report Assembly, and Persistence

> See `.claude/skills/google-ad-research/references/phase6-negatives-report.md` for full step instructions (Steps 21-26). Load it with the Read tool when entering Phase 6.
```

This follows the exact same lazy-load pattern used for Phase 5.

After both edits, count SKILL.md lines and confirm ≤500.
  </action>
  <verify>
    <automated>python -c "lines = open('.claude/skills/google-ad-research/SKILL.md').readlines(); count = len(lines); has_phase6 = any('Phase 6' in l for l in lines); has_ref = any('phase6-negatives-report' in l for l in lines); print(f'Lines: {count}, has_phase6: {has_phase6}, has_ref: {has_ref}, ok: {count <= 500}')"</automated>
  </verify>
  <done>SKILL.md ≤500 lines, contains "Phase 6" heading and reference to phase6-negatives-report.md. Phase 5 stop message in phase5-competitor-intel.md no longer says "not yet available".</done>
</task>

</tasks>

<verification>
```bash
cd ".claude/skills/google-ad-research" && python -c "
skill = open('SKILL.md').read()
ref = open('references/phase6-negatives-report.md').read()
p5 = open('references/phase5-competitor-intel.md').read()
print('SKILL lines:', len(skill.splitlines()))
print('SKILL has Phase 6:', 'Phase 6' in skill)
print('SKILL has ref link:', 'phase6-negatives-report' in skill)
print('Steps 21-26 in ref:', all(f'Step {i}' in ref for i in range(21,27)))
print('P5 no longer blocked:', 'not yet available' not in p5)
"
```
All checks print True/correct values.
</verification>

<success_criteria>
- references/phase6-negatives-report.md exists with Steps 21-26
- SKILL.md ≤500 lines with Phase 6 lazy-load section
- Phase 5 stop message updated — no longer blocks Phase 6
- Step 22 contains generate_negatives.py CLI with --run-dir
- Step 23 contains render_report.py CLI with --run-dir
- Step 24 contains update_index.py CLI with --run-dir
- Step 26 issues hard STOP
</success_criteria>

<output>
After completion, create `.planning/phases/06-negatives-report-assembly-and-persistence/06-05-SUMMARY.md`
</output>
